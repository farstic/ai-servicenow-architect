{
  "_rule": "GENERATED by scripts/gen-required-contexts.mjs from .github/workflows/ci.yml. This is the list of check names a pull request must produce, and the file main's branch protection is set from. Do not hand-edit: run `npm run gen`. A job the generator cannot classify is a hard failure rather than an omission, because an omitted name is a required check nobody ever notices is missing.",
  "_notRequired": {
    "docs-real.yml": "path-filtered: runs only when the docs corpus tooling changes, so a PR that touches nothing else never produces these",
    "docs-bump.yml": "scheduled and manual: it opens the corpus-pin PR, it does not gate one",
    "release.yml": "tags only (`v*`), never a pull request",
    "e2e-live.yml": "manual, and it needs a real instance and a credential"
  },
  "count": 54,
  "contexts": [
    "actionlint",
    "bootstrap (macos-latest, node 20)",
    "bootstrap (macos-latest, node 22)",
    "bootstrap (macos-latest, node 24)",
    "bootstrap (no-gitbash, windows-latest)",
    "bootstrap (no-node, macos-latest)",
    "bootstrap (no-node, ubuntu-latest)",
    "bootstrap (no-node, windows-latest)",
    "bootstrap (ubuntu-latest, node 20)",
    "bootstrap (ubuntu-latest, node 22)",
    "bootstrap (ubuntu-latest, node 24)",
    "bootstrap (windows-latest, node 20)",
    "bootstrap (windows-latest, node 22)",
    "bootstrap (windows-latest, node 24)",
    "commitlint",
    "contract (macos-latest, node 20)",
    "contract (macos-latest, node 22)",
    "contract (macos-latest, node 24)",
    "contract (ubuntu-latest, node 20)",
    "contract (ubuntu-latest, node 22)",
    "contract (ubuntu-latest, node 24)",
    "contract (windows-latest, node 20)",
    "contract (windows-latest, node 22)",
    "contract (windows-latest, node 24)",
    "docs-check",
    "eol (ubuntu-latest)",
    "eol (windows-latest)",
    "footprint (production install)",
    "launcher (macos-latest)",
    "launcher (ubuntu-latest)",
    "no-build handshake (macos-latest)",
    "no-build handshake (ubuntu-latest)",
    "no-build handshake (windows-latest)",
    "plugin validate",
    "release-dryrun (macos-latest)",
    "release-dryrun (ubuntu-latest)",
    "release-dryrun (windows-latest)",
    "secret scan",
    "test (macos-latest, node 20)",
    "test (macos-latest, node 22)",
    "test (macos-latest, node 24)",
    "test (ubuntu-latest, node 20)",
    "test (ubuntu-latest, node 22)",
    "test (ubuntu-latest, node 24)",
    "test (windows-latest, node 20)",
    "test (windows-latest, node 22)",
    "test (windows-latest, node 24)",
    "upgrade-e2e (macos-latest)",
    "upgrade-e2e (ubuntu-latest)",
    "upgrade-e2e (windows-latest)",
    "windows launcher",
    "windows-native (node 20, no Git Bash)",
    "windows-native (node 22, no Git Bash)",
    "windows-native (node 24, no Git Bash)"
  ]
}
