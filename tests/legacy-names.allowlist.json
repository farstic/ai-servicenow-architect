{
  "_rule": "A file may appear here only while it still contains a forbidden pattern AND a named ARC owns its rewrite. The test fails in both directions: a non-listed file that matches, and a listed file that no longer matches (remove the entry). Reduced at ARC-10-S03 from thirteen to three: ten were files whose JOB is to carry the dead names — detector data, the tests asserting it, the publish guard that must spell what it refuses (D-01), and the frozen changelog history — and a permanent carrier belongs in EXEMPT_FILES with its reason, not on a list of rewrites somebody still owes.",
  "files": {
    "README.md": "ARC-10",
    "docs/CONTRIBUTING.md": "ARC-10",
    "docs/INSTALL.md": "ARC-10"
  }
}
