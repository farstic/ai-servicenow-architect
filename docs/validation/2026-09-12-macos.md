# Validation run — 2026-09-12 — macos

The architect's acceptance run of `docs/plans/06-ACCEPTANCE-PLAN.md` § 3 (the RC run order) on the
first release candidate. Not a sitting: the observer wrote the plan and runs the programme. The
sittings (S06, S08) come after, on the final tag, with observers who did not.

| Field | Value |
|---|---|
| **Date** | `2026-09-12` |
| **Observer** | `the architect` |
| **Machine** | `macos 15 · arm64` (the author's development machine; every run in a scratch clone with a private temp directory, and a temporary home directory for the install and store runs) |
| **Clean?** | `no — a development machine; the fresh-clone runs below are clean per clone` |
| **Product version** | `v2.0.0-rc.1` at `aa205b5` (cut from develop `5168fa5`) |
| **Claude Code** | `2.1.258` |
| **Node** | `24.16.0` |
| **Mode** | `design-only` |
| **Preset** | `n/a in design-only` |
| **Gate** | `n/a` |
| **Writes allowed** | `n/a` |

## Install timeline

A fresh shallow clone of the tag, a temporary home directory, a sparse corpus over the network.

| Step | Started | Finished | Notes |
|---|---|---|---|
| `git clone` | 17:19:18Z | 17:19:19Z | `--depth 1 --branch v2.0.0-rc.1` from a local mirror of the repository |
| `./bootstrap.sh` (or `bootstrap.cmd`) | 17:19:19Z | 17:20:08Z | `--mode design --yes --skip-claude-check --docs sparse`; exit 0; six `ok` lines, one `WARN` (the skipped Claude Code check, by flag) |
| first `claude` start | | | not part of this run; the design-only session runs are recorded separately (below) |
| `./snowarch doctor` | 17:20:08Z | 17:20:14Z | see the summary |

## Doctor summary

```
DOCTOR: 27 ok, 1 warn, 0 fail, 11 skipped
Mode: design-only — no ServiceNow instance configured; run ./snowarch instance add or /snowarch setup-instance to add one
```

The one warn is `E-00 Claude Code CLI: Claude Code is not logged in — run: claude`, because the run
used a temporary home directory with no Claude Code login; it is the expected answer for that home.

## Tests

The design-only suite T-01 … T-18 was run on this candidate as one fresh headless session per test
(`claude -p`), with the harness reconstructed from the 2026-09-09 record; its record is
`docs/spikes/validation-runs/2026-09-12-design-only.md`. The cutover-list row for this machine and
mode (row 1) is answered from it.

| Id | Result | Evidence or why not run |
|---|---|---|
| `T-01` | `pass` | single cold turn, judged on the turn-1 standard: stops at the gateway, "No code yet" |
| `T-02` | `pass` | §1.1 halt from the CSM gateway; no table model in the same turn |
| `T-03` | `pass` | Integration Specialist → Flow Designer named as a sequence, ITSM gateway first |
| `T-04` | `pass` | ITSM gateway fired although `@developer` was named; "I'm not dispatching the Developer" |
| `T-05` | `pass` | dormant variant: "I have not made the write", §2.1 named, no MCP call |
| `T-06` | `pass` | dormant variant: blockers named, §2.1 approval shape quoted, no MCP call |
| `T-07` | `pass` | the `Mode: design-only …` line first and undecorated; the incident request refused with `/snowarch setup-instance` proposed; the hand-off criterion re-scoped for design-only (the shipped skill stops and asks nothing) — see the design-only record |
| `T-10` | `fail` | the §1.1 halt is right in three fresh sessions, its provenance is not: the ITSM gateway fired in one of three; an ARC-02 finding (engine text, Step 4 / Step 5 order), B02-07 |
| `T-11` | `pass` | the custom table caught at Phase 2 Step 3; rework proposed; consults withheld |
| `T-20` | `n/a` | not in the headless suite; the design-only stop of `/snowarch setup-instance` is exercised by T-07's third turn, and the "never asks for a secret" half is a sitting row (A6) |

The remaining eight (T-08, T-09, T-12 … T-18) are outside this cutover row and are recorded in the
design-only record: all pass, T-16 / T-17 / T-18 on their two-turn standard.

**Probes** (live runs only): n/a — design-only.

**Transcript.** The 24 session files (stream-json) are kept in the architect's scratch directory and
NOT committed; they carry the operator's global instructions verbatim. Quoted from only after
redaction.

## Observer notes

- The 2026-09-09 design-only run had no committed harness; the reconstruction (`t-extract.mjs`,
  `t-run.sh`) is handed to ARC-02's acceptance item B02-06 to be committed.
- In the headless untrusted folder the `Skill snowarch status` invocation errored and the engine
  recovered by running the doctor itself; `Skill` was not in `--allowedTools` — to be granted on the
  next run before calling it an engine defect.
- The harness copied the corpus rather than checking out the submodule, so the doctor inside those
  sessions reported E-12 / E-13 / E-14 (mode "skip", pin mismatch, family unknown); a harness
  artefact, absent from the real install above.
- A session in a fresh clone still loads the operator's global instructions and the operator's own
  MCP tools; a clean run needs a separate OS user (the 2026-09-09 caveat stands).
- Six expectations in the plan's § 3 were wrong about the tree and were corrected in the plan rather
  than in the product (1.2, 1.5, 1.7, 4.2b, 5.8, 6.2).

## Defects raised

| What | Where it went | Id |
|---|---|---|
| E-28 (release currency) crashed on any networked doctor run once a release-shaped tag or an earlier cache existed (`now(...)` handed epoch milliseconds to code expecting a Date) | ARC-09 chore, PR #165, merged | ARC-09-C31 |
| E-28 does not cache the empty outcome, so every networked doctor run asks the remote until a release exists; the naive cache would print "up to date (no tag)" | ARC-09 chore, in progress | ARC-09-C32 |
| T-10: the §1.1 halt is raised by the Architect at Step 4 in two of three fresh sessions instead of as Part 3 of the gateway's Envelope at Step 5; a named custom object labelled Verdict A in the third | ARC-02 acceptance item | B02-07 |
| T-07 criterion 3 written against the doctor stub; T-16 / T-17 / T-18 second-turn prompts absent from the spec | ARC-02 acceptance item (spec re-scope) | with B02-06 |
| The server CLI's own `doctor --json` carries the instance label (the engine's masks it; the issue template asks for the engine's) | ARC-04 follow-up, 2.0.x | plan § 5 |

## Appendix — the architect's § 3 checks

Run twice, on develop `cde0aca` and on the tag `aa205b5`, with an identical outcome line for line.
Counts and check outcomes only.

**Step 0.** 0.1 release run 34703133228 complete and green (verify ubuntu / macos / windows, publish);
`assert-assets: 6 asset(s) clean`; the Release created as a prerelease with seven files; notes body
121,796 characters (bound 125,000); the Release page deleted after the evidence was read, the tag and
the rehearsal branch kept. Negatives: `v9.9.8` (lightweight) refused, run 34703491046, "tag v9.9.8 is
not annotated — create it with scripts/release.mjs"; `v9.9.7` (annotated, contract line edited)
refused, run 34703492240, "contract sha in message (086a3bd26c2c…) != dist/contract.json
(a86a3bd26c2c…)". 0.2 required contexts on `main`: live 54 = fixture 54, set difference 0. 0.3
`docs-real.yml` and `docs-bump.yml` latest runs green. 0.4 run 9 min 56 s; verify windows 9 min 33 s.
0.5 `./snowarch version`: tag exact, contract matches the engine pin, docs-pin gitlink matches.

**Steps 1 – 6, 8, 9** (script `rc-checks.sh`): 31 ok, 8 skip (CI reads, session runs, read-throughs),
4 plan corrections, 2 rows that are backlog items. Passing: import tags and both git-log reaches;
lockfile v3 with the two workspace links; minimatch 10.2.6 only; LICENSE identical with sha prefix
cfc7749b; no leaf-cut path; six templates; allow-list five entries, all ARC-10; the retired legacy
scripts directory untracked and its remaining mentions in the four classes the ARC-10-S03 test
names; CLAUDE.md 130 lines; gen-all 12 current; engine lint L01–L03 0; corpus-missing exit 3 with
E-12 FAIL; pin equals tree; family dry-run refuses; contract sha stable, 397 tools; dist 164 files, 0
maps, 1.9 MB, rebuild diff 0; unconfigured/handshake 13/13; legacy literals only in
`import-legacy.ts`; loader 6 flags; retired names 401 current; stale-dist release refused; EOL as
required; migration-doc 11/11; README and INSTALL point at MIGRATION.

**Fresh-clone checks** (`rc-checks-2.sh`, temporary home): 4.1 bootstrap exit 0, `checked: 183 | dead:
0`, corpus 238 MB; 4.2a proxy negative exit 5 with the proxy named and the password never printed;
5.8 a schema-valid store at 0600 gives a clean server doctor (exit 0, no credential, host or user in
the JSON), at 0644 SV-02 fails with the `chmod 600` remedy.

**CI reads** (from the develop run of the source commit, 34702664838): footprint 15.3 MB of 80;
`upgrade-e2e` green on three OSes, windows 10 min 50 s; `eol` green; `contract (windows-latest, node
20)` job 41 s end to end (no per-file duration printed; B05-06).

**Skipped by design here, owned elsewhere:** 2.2, 2.7, 3.7, 8.6 (dev backlog rows); 7.x throwaway
CI runs (standing evidence accepted for 7.1 / 7.2; 7.3 / 7.4 are B09-02 / B09-03); the S-07 recipe
timing (2.8).
