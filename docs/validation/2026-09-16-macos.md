# Validation run — 2026-09-16 — macos

Sitting A of ARC-10-S06: the first fifteen minutes on a machine that already had earlier versions of
this product on it. The observer is the owner; the sitting script is `docs/spikes/OWNER-SITTING.md`.

| Field | Value |
|---|---|
| **Date** | `2026-09-16` |
| **Observer** | `the owner` |
| **Machine** | `macos 15 · arm64` |
| **Clean?** | `no — earlier registrations of this product under other project folders (three under the retired server name, two under the current one)` |
| **Product version** | `v2.0.0-rc.2` at `a1340a6` |
| **Claude Code** | `2.1.258` |
| **Node** | `24.16.0` (a separate pass ran step B5 with Node off `PATH`) |
| **Mode** | `design-only` |
| **Preset** | `n/a in design-only` |
| **Gate** | `n/a` |
| **Writes allowed** | `n/a` |

## Install timeline

| Step | Started | Finished | Notes |
|---|---|---|---|
| `git clone` | A1 | A1 | clean clone of the tag |
| `./bootstrap.sh` (or `bootstrap.cmd`) | A2 | A2 | Path A of `docs/INSTALL.md`. `Mode: design-only`; B02 corpus 35 s — 35,192 files, 174 MB, pin `11b39be`; citations `183 | dead: 0`; the bootstrap's own summary read `6 ok, 0 warn, 0 fail` |
| first `claude` start | B1 | B1 | dialogs seen: `1` — the workspace-trust dialog, which reads all 236 allow rules aloud. A second dialog was expected and not seen; whether one exists is unconfirmed, not disproved |
| `./snowarch doctor` | A3 | A3 | see below |

## Doctor summary

```
30 ok, 4 warn, 0 fail
```

No FAIL lines. The four warnings are the expected design-only set.

**One line that was wrong, and it is the sitting's most useful finding.** The bootstrap's B09 summary
reported `1 fail` while the doctor it was summarising reported none, and named nothing — a count
with no sentence under it. Raised as ARC-08-C3 (and the cause, a PATH-gated counter, as ARC-08-C6).
The stale tally was still present at step H, on the rc.2 build this sitting ran.

## Tests

One row per cutover-list id (`tests/VALIDATION-TESTS.md` § *Cutover test list*, row 1 — macOS ·
design-only). `n/a` needs a reason; a bare `n/a` is not a result.

| Id | Result | Evidence or why not run |
|---|---|---|
| `T-01` | `n/a` | routing behaviour; not exercised at this sitting. The sitting's subject was install, mode reporting and registration (steps A–D) |
| `T-02` | `n/a` | as `T-01` |
| `T-03` | `n/a` | as `T-01` |
| `T-04` | `n/a` | as `T-01` |
| `T-05` | `n/a` | the dormant variant was not run as a test. Step B2 did establish its precondition — the server absent from `/mcp` with no *Project MCPs* section — but that is one criterion, not the test |
| `T-06` | `n/a` | as `T-05`: nothing to capture into an update set in design-only, and the test was not run |
| `T-07` | **pass** | steps B1–B5. `/mcp` shows the server disabled with no prompt (B2); the `Mode` line is quoted verbatim and undecorated, template lines in order (B3/B4); in DEFAULT mode `/snowarch status` ran `./snowarch doctor --quick --json` with **no permission prompt**, so the S-16 allow rule held on first use (B4b); and with Node off `PATH` the fallback read `Mode: design-only — from bootstrap state (…); doctor unavailable, until Node 20+ is installed` (B5) |
| `T-10` | `n/a` | run against `v2.0.0-rc.3` in the headless harness instead — six runs, recorded in `docs/spikes/validation-runs/2026-09-17-rc3-design-only.md` |
| `T-11` | `n/a` | post-build routing behaviour; not exercised at this sitting |
| `T-20` | `n/a` | `/snowarch setup-instance` was not run. The related surface that WAS exercised is `mode live`, and it could not reach the wizard at all — ARC-06-C6 |

**Probes** (live runs only): `n/a — design-only`.

**Transcript.** Kept locally and not committed. Grepped at step H for `password|passwd|token`: two
hits, both benign — the words appear in the product's own help text, not as values.

## Observer notes

Everything the observer had to guess, look up elsewhere, or scroll back for.

- **`docs/INSTALL.md` was not found at first.** The reader went looking for an install document and
  did not land on it; `README.md` is generated from it, which is not apparent from either.
- **`mode live` never reached the wizard.** Not a usability note — a defect on every machine, and the
  sitting is what found it: the probe read an asynchronous child process as if it had already
  finished. ARC-06-C6.
- **A user-scope registration survived `mode design`.** After `mode live --register user`, `/mcp`
  showed the server connected under *User MCPs* and the project entry unlisted; returning to design
  mode did not remove the user entry. Closed by hand at step C7.
- **`doctor --json` named the observer's other project folders.** A report meant to be pasted into an
  issue carried directories that have nothing to do with this product. ARC-08-C9.
- **`--quick` appeared to skip E-27.** Recorded and then withdrawn: the cause was the architect's own
  script, not the product. It is listed here because an observer note that quietly disappears is
  worse than one that says it was wrong.

## Defects raised

| What | Where it went | Id |
|---|---|---|
| The wizard probe printed one sentence for four different failure classes | PR #183 | `ARC-06-C5` |
| `mode live` could not reach the wizard on any machine | PR #183 | `ARC-06-C6` |
| A failed mode switch recorded a mode the checkout never reached | PR #183 | `ARC-06-C7` |
| The bootstrap summary printed a failure count and named nothing | PR #183 | `ARC-08-C3` |
| …and the count itself came from a PATH-gated check | PR #183 | `ARC-08-C6` |
| E-27's remedy did not match the scope it was reporting on | PR #183 | `ARC-08-C4` |
| `doctor --json` published other project folders | PR #183 | `ARC-08-C9` |
| Further doctor findings from the same sitting | PR #183 | `ARC-08-C5`, `ARC-08-C7`, `ARC-08-C8` |
| The eight Accepted ADRs' Decision-owner lines initialled | PR #184 | — |

**One attribution corrected against the tree.** The architect's fact sheet lists `ARC-02-C3` among
this sitting's defects. It is not one: the row in `docs/plans/ARC-02-engine-consolidation/README.md`
records the Domain Expert gateway being readable as conditional, found by T-10 judging on rc.1 and
fixed in PR #188 on 2026-09-17. It is not in this table.

**Steps E1–E4.** E1–E3 were delegated by the owner to the architect and run as a reader sweep —
61 findings raised, 6 refuted, 55 kept (26 *fix*, 29 *consider*) — which became PR #183. The GitHub
half of E4 is `n/a` with a reason: `/issues/new/choose` redirects to a blank issue because the
default branch carries no issue templates until the 2.0.0 milestone merge. That is a post-tag check,
not a defect.
