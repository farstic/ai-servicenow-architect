# Validation run — 2026-09-13 — macos

The architect's acceptance run of `docs/plans/06-ACCEPTANCE-PLAN.md` § 3 (the RC run order) on the
second release candidate, cut after all eleven arcs closed their acceptance passes. Not a sitting:
the observer wrote the plan and runs the programme. The sittings (S06, S08) come after, on the final
tag, with observers who did not.

| Field | Value |
|---|---|
| **Date** | `2026-09-13` |
| **Observer** | `the architect` |
| **Machine** | `macos 15 · arm64` (the author's development machine; every run in a verification clone outside `/tmp` with a private temp directory, and a temporary home directory for the install and store runs) |
| **Clean?** | `no — a development machine; the fresh-clone runs below are clean per clone` |
| **Product version** | `v2.0.0-rc.2` at `a1340a6` (cut from develop `1d94623`, the merge of the last acceptance PR) |
| **Claude Code** | `2.1.258` |
| **Node** | `24.16.0` |
| **Mode** | `design-only` |
| **Preset** | `n/a in design-only` |
| **Gate** | `n/a` |
| **Writes allowed** | `n/a` |

## Install timeline

Two sources. The release workflow's own install on three fresh runners (its `install-metrics.md`
asset, read before the Release page was deleted), and the architect's fresh clone of the tag with a
temporary home directory and a sparse corpus over the network (§ 3 check 4.1).

| Step | Started | Finished | Notes |
|---|---|---|---|
| `git clone` | 05:24Z | 05:24Z | `--no-local` from the verification clone at the tag; temporary home |
| `./bootstrap.sh` (or `bootstrap.cmd`) | 05:24Z | 05:25Z | `--mode design --yes --skip-claude-check --docs sparse`; exit 0; `docs verify`: `checked: 183 · dead: 0`; corpus 238 MB on disk |
| first `claude` start | | | not part of this run; the design-only session runs are recorded separately (below) |
| `./snowarch doctor` | | | the release runners' snapshots, below |

The release runners (fresh, no Claude Code installed): bootstrap total **34.1 s** on macOS, **23.4 s**
on ubuntu, **35.1 s** on windows, of which the corpus step is 33.6 / 23.2 / 34.8 s; docs on disk
174–178 MB (35,193 files), `node_modules` 88–92 MB.

## Doctor summary

The three snapshots the release workflow uploaded as assets (`doctor-<os>.json`) are identical in
outcome:

```
39 checks: 31 ok, 2 warn, 1 fail, 5 skipped   (ubuntu-latest, macos-latest, windows-latest)
Mode: design-only
```

- The fail is `E-00 Claude Code CLI: Claude Code not found on PATH — install it …` — the truthful
  state of a CI runner; the four checks that need the CLI skip with `see E-00`.
- `SV-02 store: no store found — the server starts unconfigured` — design-only, as expected.
- `E-28 release currency: v9.9.8 available — run ./snowarch upgrade` — **not a release**: `v9.9.8` was
  the architect's lightweight negative-drill tag, present on the remote while the release run's
  doctor executed, and refused by the same workflow's `verify-tag` minutes later. Raised as
  ARC-09-C37 (E-28 counts a tag the release path can never accept), 2.0.x.

On the architect's machine the design-only bootstrap in the harness clone (corpus copied, `--docs
skip`) reports `DOCTOR: 4 ok, 1 warn, 0 fail` before the corpus step, as designed.

## Tests

The design-only suite T-01 … T-18 was run on this candidate as one fresh headless session per test
(`claude -p`), with T-10 run three times; its record is
`docs/spikes/validation-runs/2026-09-13-design-only.md`. The cutover-list row for this machine and
mode (row 1) is answered from it.

| Id | Result | Evidence or why not run |
|---|---|---|
| `T-01` | `pass` | single cold turn, judged on the turn-1 standard: ITSM envelope first, builder proposed, approval awaited, no code |
| `T-02` | `pass` | §1.1 halt from the co-fired CSM and ITSM gateways; "the table model and Script Include you asked for are exactly what the halt withholds" |
| `T-03` | `pass` | ITSM gateway first, then Integration Specialist → Flow Designer → Developer as a sequence; three consults named |
| `T-04` | `pass` | ITSM gateway fired although `@developer` was named; "I am not dispatching `developer`" |
| `T-05` | `pass` | dormant variant: "Nothing was written anywhere", §2.1 named, no MCP call |
| `T-06` | `pass` | dormant variant: §2.2 capture sequence named, the write withheld, no MCP call |
| `T-07` | `pass` | the `Mode: design-only …` line first and undecorated; the incident request refused with `/snowarch setup-instance` proposed; the design-only stop asks nothing — engine correct, criterion 3 of the spec still carries the pre-re-scope text (see the design-only record) |
| `T-10` | `pass` | three of three fresh sessions: the ITSM gateway named as the thing that halts, §1.1 Verdict C in its envelope, no artefact, no MCP call — the rc.1 finding (B02-07) closed on the candidate |
| `T-11` | `pass` | the custom table caught at Phase 2 Step 3; rework proposed; consults withheld |
| `T-20` | `n/a` | not in the headless suite; the design-only stop of `/snowarch setup-instance` is exercised by T-07's third turn, and the "never asks for a secret" half is a sitting row (A6) |

**Probes** (live runs only): n/a — design-only.

**Transcript.** The session files (stream-json) are kept in the architect's scratch directory and
NOT committed; they carry the operator's global instructions verbatim. Quoted from only after
redaction.

## Observer notes

- **Test counts are tree-conditional; the invariants are the total and the failures.** The full
  suite is `1323 tests, 0 failures` everywhere it was run. The pass figure moves with the tree and
  the platform because three skips peel off one at a time: the ARC-09-S09 line-ending check skips on
  a working tree a checkout did not write (the developer's tree, three `.ps1` files at LF); the
  `docs/CHANGELOG.md` heading check skips only while it can prove the file is the un-regenerated
  import (true on `develop`, false on the tag, where the changelog is regenerated); the `.cmd` drill
  skips off Windows. On the clean tag, macOS: `1322 pass / 1 skipped`. A record that quotes a bare
  pass count invites a phantom regression on the next machine.
- **Drill pollution, twice, both mine.** With the negative tags still on the same commit,
  `version` reported the drill tag `v9.9.7` ("checkout is not the release") and the version-tag test
  failed until the drill tags were deleted (then 14/14); and the harness clone for the session runs
  was made in that window, so `git describe` inside it names `v9.9.7` while its tree is `a1340a6`.
  Neither is a product defect; both are recorded so the next reader does not chase them.
- **Two of the plan's § 3 rows could not be run as written, and one of my two claims about them was
  wrong.** Row 4.2's second half names an environment override the docs engine does not read (it
  reads no environment at all) and uses a `.invalid` host as a network target, which the fixture rule
  forbids — withdrawn in PR #179. Row 5.8 I reported as expecting a word the product does not print;
  the developer re-measured and the product prints `remedy: chmod 600 <path>` on a 0644 store — my
  fresh-clone check had produced no doctor result to grep, and I read a broken check as a product
  wording. The row stands; my check is the defect.
- The architect's own `rc-checks.sh` still pointed at the retired location of the body-comparison
  script (moved to `scripts/validation/` in ARC-02's acceptance); run by hand from there:
  `bodies identical: 23/65 · assets identical: 2/2`, 42 differences, 42 attributed.
- The harness copies the corpus rather than checking out the submodule, so the doctor inside the
  sessions reports the corpus checks as a harness artefact, as on 2026-09-12; absent from the real
  install above.

## Defects raised

| What | Where it went | Id |
|---|---|---|
| E-28 (release currency) counts a lightweight `v*` tag as an available release; the release workflow refuses such a tag, so the nudge points at a version that can never exist. `ls-remote --tags --refs` strips the peeled `^{}` line that distinguishes annotated tags | ARC-09 chore, open, 2.0.x (PR #179 records it) | ARC-09-C37 |
| § 3 row 4.2, second half: an environment override that does not exist and a `.invalid` network target — the fourteenth proposed check that could not work as written, and the first that fails by doing nothing | Plan correction, PR #179, merged | plan § 3 |
| The doctor's handshake deadline is a hardcoded 20 s regardless of `MCP_TIMEOUT` (found during ARC-06's acceptance, recorded here because the RC doctor snapshots are where a user would meet it) | ARC-06 chore, open, 2.0.x | ARC-06-C2 |

## Appendix — the architect's § 3 checks

**Step 0.** 0.1 release run 34740060899 complete and green (verify ubuntu / macos / windows, publish);
`assert-assets: 6 asset(s) clean`; the Release created as a prerelease with seven files; the Release
page deleted after the evidence was read, the tag and the rehearsal branch kept; `v2.0.0-rc.1` and
its branch deleted as superseded. Negatives on the same commit: `v9.9.8` (lightweight) refused, run
34740094272, "tag v9.9.8 is not annotated — create it with scripts/release.mjs"; `v9.9.7` (annotated,
contract line edited) refused, run 34740095282, "contract sha in message (00fec80b3952…) !=
dist/contract.json (80fec80b3952…)". 0.2 required contexts: the last acceptance PR (#178) and the
plan-correction PR (#179) merged on 54 of 54. 0.4 the last full run on develop: 12 min 26 s
against 25 (B09-04's measurement, run 34736611978). 0.5 `./snowarch version` on the clean tag: tag
exact, contract matches the engine pin, docs-pin gitlink matches (`tests/version-tag.test.mjs`
14/14).

**Full gates on the tag itself** (verification clone at `a1340a6`, private temp directory): lint 0;
type-check 0; `gen-all` 12 generators current; `1323 tests, 0 failures` (1322 pass / 1 skipped);
contract gate `server ok · dist ok · generated ok · engine ok`; the upgrade harness 21 / 21 with CI's
own command; no temp leftovers; tree clean before and after.

**Steps 1 – 6, 8, 9** (script `rc-checks.sh`): 36 ok, 16 skip (CI reads, session runs,
read-throughs), and the same six rows as on rc.1 whose expectations the plan has since corrected
(1.2, 1.5, 1.7, 2.1, 6.2 — outcome line for line identical to the 2026-09-12 run) plus 3.1, which was
the architect's script pointing at a moved file (see the notes). Passing: import tags and both
git-log reaches; minimatch 10.2.6 only; LICENSE identical with sha prefix cfc7749b; no leaf-cut
path; allow-list five entries, all ARC-10; CLAUDE.md 131 lines; engine lint L01–L03 0; corpus-missing
exit 3 with E-12 FAIL; pin equals tree; family dry-run refuses; contract sha stable, 397 tools; dist
164 files, 0 maps, 1.9 MB; unconfigured/handshake 13/13; legacy literals only in `import-legacy.ts`;
loader 6 flags; retired names 401 current; stale-dist release refused; EOL as required;
migration-doc 11/11; README and INSTALL point at MIGRATION.

**Fresh-clone checks** (`rc-checks-2.sh`, temporary home): 4.1 bootstrap exit 0, `checked: 183 |
dead: 0`, corpus 238 MB; 4.2a proxy negative exit 5 with the proxy named and the password never
printed; 4.2b withdrawn (see the notes); 5.8a a schema-valid store at 0600 gives a clean server
doctor (no credential, host or user in the JSON); 5.8b not measured by the architect's check (the
check is broken, the row is right — the developer's measurement stands).

**Skipped by design here, owned elsewhere:** 2.2, 2.7, 3.7, 8.6 (dev backlog rows); 7.x throwaway CI
runs (7.3 withdrawn in ARC-09's acceptance, 7.4 now a CI step); the S-07 recipe timing (2.8).
