# 03 — Risks and Unknowns

Status: **Draft** · Integration pass 2026-09-04: names swept to D-01 / R-1 / R-2, S-19 and S-20 added to §A, §E maps every spike to its ARC-00 runner story · Every unverified assumption raised by any of the three candidate designs or the three judges is listed here with the concrete check that retires it, the design's fallback if the check fails, and where the check is executed (ARC-00 unless stated). "Evidence so far" quotes what the docs or the CLI already say; it is not a substitute for the check.

Legend — **Impact** on the chosen design if the assumption is false: H = changes a user-visible promise · M = changes an implementation detail · L = cosmetic or roadmap-only.

## A. Spikes that gate the implementation ARCs (run in ARC-00)

| ID | Assumption | Impact | Evidence so far | Check that retires it | Fallback already designed |
|----|-----------|--------|-----------------|-----------------------|---------------------------|
| S-01 | `.claude/settings.local.json` containing `{"enabledMcpjsonServers":["servicenow"]}` (live) or `{"disabledMcpjsonServers":["servicenow"]}` (design-only), written by the bootstrap **before** the first `claude`, suppresses the per-server approval prompt once the workspace-trust dialog is accepted | H (one extra click) | `docs:settings-reference`: this key is exactly what Claude Code writes to that file when the user approves a server; in an *untrusted* folder only the shared project file is ignored; `docs:mcp` (fetched 2026-09-04): "Claude Code also applies approvals from an untracked `.claude/settings.local.json`, but it runs git to check whether the file is tracked, and it runs that check only in a trusted folder. In a folder you've never trusted, Claude Code waits for the trust dialog before applying the file's approvals" (≥ 2.1.207) — so the expected count is exactly one dialog (trust); the file must stay untracked (gitignored) | Fresh clone on macOS and Windows; run `./bootstrap.sh --mode live`; start `claude`; accept trust; count dialogs; confirm `claude mcp get servicenow` shows approved. Repeat with `--mode design` and confirm `claude mcp get servicenow` shows `✘ Rejected (see disabledMcpjsonServers in settings)` and `/mcp` shows the server disabled with no prompt | Document one approval click; the summary line already says "answer Yes" |
| S-02 | After `snow_core_instances_reload` the server's `notifications/tools/list_changed` makes the newly advertised tools callable in the same session (stdio server, default v1 runtime) | M | `docs:mcp` "Dynamic tool updates": supported; refresh failures keep the previous list (≥ 2.1.214); runtime notes mention v2 streams for HTTP servers | Implement reload in ARC-04; in a session, add an instance from the terminal, call the reload tool, then call `snow_core_capabilities_read` and a read tool without `/mcp` reconnect | The `/snowarch setup-instance` skill prints "run `/mcp` → servicenow → reconnect" |
| S-03 | `${CLAUDE_PROJECT_DIR:-.}` expansion in `.mcp.json` `args` and exec-form `node` hooks with `${CLAUDE_PROJECT_DIR}` in `args` behave identically on native Windows (cmd / PowerShell, no Git Bash) | H | `docs:mcp` and `docs:hooks` describe both without platform caveats; exec form on Windows requires a real executable — `node.exe` is one | Windows 10/11 VM without Git Bash: run both flows; inspect the spawned server's env and the hook's stdout | Bootstrap writes the resolved absolute path into `.claude/settings.local.json` (`env`) and a local `.mcp.json` override generated per machine |
| S-04 | `process.stdin.setRawMode` masked input works in Windows Terminal and conhost under PowerShell 5.1 and cmd | M | Node docs: TTY raw mode is supported on Windows consoles | Run `./snowarch instance add` on the Windows VM; verify no echo, backspace handling, Ctrl-C | `--password-stdin`; later the `--web` one-shot form (roadmap item 5) |
| S-05 | When `node` is absent, an exec-form SessionStart hook fails with a non-blocking notice and the session continues | M | not stated in `docs:hooks` | Remove Node from PATH; start `claude` in a design-only checkout; observe | Launcher writes `"disableAllHooks": true` into `.claude/settings.local.json` when Node is absent (docs-verified key) and prints why — noting that in a non-managed file the key also disables the user's personal and plugin hooks for this project (`docs:settings-reference`); the cleaner alternative, if S-05 shows a blocking error, is to keep the committed `.claude/settings.json` hook-free and have the bootstrap write the SessionStart hook into `.claude/settings.local.json` only when Node ≥ 20 is present |
| S-06 | `MCP_TIMEOUT` set through the committed `.claude/settings.json` `env` block governs the 394-tool server's startup on first launch; the cold start fits the value | M | `docs:settings-reference` `env`: timeout-class variables apply at startup from every settings file — strong evidence; cold-start time never measured | Measure `time node packages/snowarch/dist/server.js` to `initialize` on a slow laptop; start `claude` with a deliberately low value and confirm the failure, then with 120000 | Raise the value; the launcher can also export it in the user's shell profile |
| S-07 | `git submodule update --init --depth 1` followed by cone `sparse-checkout set` yields roughly the measured 299 MB / ~55 s; long paths (35k files) are fine on Windows git | M | Direct blobless sparse clone measured 299 MB; the submodule path with `--filter=blob:none` was not exercised | Time and `du` the B02 step on macOS, Linux, Windows (enable `core.longpaths`); compare three recipes — the naive one (full 49k-file checkout, then `sparse-checkout set` narrows it), `git submodule update --init --depth 1 --filter=blob:none` (option verified on git 2.39.5) + sparse set, and a direct `git clone --filter=blob:none --no-checkout --depth 1 --sparse` into the submodule path + `sparse-checkout set` + `checkout <pin>` + `git submodule absorbgitdirs`; confirm the pin is still fetchable once it is no longer the branch tip (`--depth 1` clones the tip; git then fetches the pinned SHA by hash, which GitHub permits) | Accept up to ~350 MB (depth-1 without blobless); print the size in the README |
| S-08 | `bootstrap.cmd` running `powershell -ExecutionPolicy Bypass -File bootstrap.ps1` works under the default Restricted policy; behaviour under a GPO-locked policy | H (Windows first sitting) | Standard technique; per-process Bypass is documented by Microsoft; GPO "MachinePolicy" cannot be overridden | Test on default policy and on a GPO-locked VM | GPO-locked: document the Git Bash path (`./bootstrap.sh`) and ship a pure-batch minimal design-only path (git commands only) |
| S-09 | Claude-first path: `git clone <url> .` into the folder Claude was started in, then one restart, loads `CLAUDE.md`, `.mcp.json`, `.claude/settings.json` and skills; no mid-session loading is needed | M | `docs:skills` says skill file changes are detected live; nothing says the same for `.mcp.json` / settings | Run the recipe; check Claude Code's built-in `/status`, `/mcp` and `/help` before and after the restart | Always instruct the restart (already in the design) |
| S-10 | After the SCRIPTING read/write gate split, the `read-only` preset is sufficient for the Code Reviewer and Developer *read* workflows (list/read Script Includes, Business Rules, ACLs) | M | Gate placement analysed (`00` §4.3) | Run VALIDATION-TESTS T-01…T-18 in `read-only` and `pdi-developer` on a PDI — deferred to ARC-04-S05 (needs the gate split; ARC-00-S14 records the deferral) | Document that Code Review of live scripts needs `pdi-developer` until fixed |
| S-11 | Claude Code floor 2.1.214 is sufficient for every mechanism used | L | Version notes collected: 2.1.196 approval semantics, 2.1.198 hook placeholders, 2.1.214 `list_changed` resilience | Install exactly 2.1.214 (native installer with a version argument) in a VM; run design-only and live flows | Raise the floor in `engine.config.json` |
| S-12 | Permission allow rules with a middle wildcard (`mcp__servicenow__snow_*_read`) are honoured | L | `docs:permissions`: globs accepted after the literal `mcp__<server>__` prefix (examples show trailing `*`) | Add the rule; call a read tool; observe whether a prompt appears | Generate an explicit per-tool allow list from the contract (~250 entries) |
| S-13 | Capping descriptions at ≤ 500 chars makes all 28 skills appear with descriptions in the listing (the mechanism that dropped 7 today is unknown) | M | `docs:skills`: 1,536-char truncation; `docs:sub-agents`: 15,000-token combined-description warning (agents) | After ARC-02, start `claude --debug` and inspect the listing; confirm 28 names with descriptions — deferred to ARC-02-S03 (needs the rewritten descriptions; ARC-00-S14 records the deferral) | Investigate the per-listing budget; shorten further or move persona routing into `CLAUDE.md` |
| S-15 | `npm ci --omit=dev --ignore-scripts` at the monorepo root installs only the server's runtime dependencies (~72 MB) and nothing for `tools/snowarch`; no hoisting surprise breaks `dist/server.js` module resolution | M | Server production tree measured at 72 MB `du` / 57 MB content / 176 lock packages (the earlier "155" was `ls node_modules`, which varies by npm major: 167 on npm 10, 155–156 on npm 11) with no install scripts; **after the D-03 cut: 27 MB / 17 MB / 135** (ARC-00-S08, reproduced by the architect) | Run it on a clean checkout on all three OSes; run the MCP handshake | Pin `--workspace packages/snowarch` or install inside the package directory |
| S-16 | `permissions.allow` entries such as `Bash(./snowarch doctor*)` in the committed `.claude/settings.json` are honoured right after trust so `/snowarch setup-instance` runs the doctor without prompts | L | `docs:settings`: shared project settings apply after trust | Invoke `/snowarch setup-instance` in a fresh trusted checkout; count prompts | The skill declares the same commands in its `allowed-tools` (turn-scoped, not trust-gated — `docs:skills`) |
| S-17 | A stdio server that starts with **zero** instances and advertises five tools (`snow_core_instances_index`, `snow_core_instances_reload`, `snow_core_current_instance_read`, `snow_core_capabilities_read`, `snow_core_status_read` — `01` §9, ARC-04-S04) is accepted by Claude Code as "connected" and shows no error in `/mcp` | M | Nothing contrary in `docs:mcp`; a placeholder-URL start already advertises tools today | Start the ARC-04 server unconfigured under Claude Code; check `/mcp` and `claude mcp list` | Advertise the status tool only and log a clear line |
| S-18 | Generated `permissions.ask` rules for every `mutates:true` tool in the committed `.claude/settings.json` produce a user prompt in **auto mode** — the built-in starting mode on Pro, Max and Team plans — so the §2.1 "write approved" gate cannot be bypassed by the classifier | H (a write without user consent) | `docs:permission-modes` "How the classifier evaluates actions": actions matching allow, ask or deny rules resolve immediately, before the classifier; `dontAsk` mode denies ask-rule matches rather than prompting; `bypassPermissions` skips checks | Start a session in auto mode in a live checkout; call `snow_core_record_add` and confirm a prompt appears; call a `mutates:false` tool and confirm none does; repeat in `default` and `plan` modes | Generate `deny` for mutating tools plus a PreToolUse hook (exec-form `node`) that allows a call only while a per-session approval marker written by an explicit user step exists |
| S-19 | `claude plugin validate` runs on a headless CI runner (no login, no TTY), so the skills/agents lint can call it in CI | L | Nothing in `docs:plugins` states a login requirement; never exercised headless (minor fold in `02`) | Run it in a `ubuntu-latest` job after `npm i -g @anthropic-ai/claude-code` (ARC-00-S12) | The lint runs `validate` locally only; the CI job stays advisory (ARC-01-S11, ARC-05-S04) |
| S-20 | Project stdio servers spawned from `.mcp.json` inherit `HTTPS_PROXY` / `HTTP_PROXY` / `NO_PROXY` / `NODE_EXTRA_CA_CERTS` from the shell that started `claude` (macOS, Windows cmd and PowerShell 5.1) | M (proxied laptops, R-15) | No statement in `docs:mcp`; raised by ARC-04-S11's risk note | Stub server echoes its environment on both platforms (ARC-00-S06) | ARC-06-S01 forwards the four variables in `.mcp.json` `env` as `${VAR:-}` (the server deletes empty strings at start-up, ARC-04-S11); ARC-08 E-26 reports "as seen by this shell" until answered |

## B. Unknowns tied to the roadmap plugin channel (run only if D-06 chooses plugins first, or before roadmap item 1)

| ID | Assumption | Check |
|----|-----------|-------|
| S-14a | `userConfig` fields with `sensitive: true` produce a masked dialog for both `claude plugin install` in a terminal and `/plugin install` in a session, on macOS, Windows and Linux; which store backs "secure storage" on Windows/Linux; the ~2 KB Keychain budget behaviour | Install a test plugin from a local marketplace on all three OSes; inspect `~/.claude/.credentials.json` vs Keychain |
| S-14b | A plugin-bundled stdio MCP server starts without an additional per-server approval prompt (the docs say it starts automatically when the plugin is enabled; the coupling lens said "same approval as project .mcp.json") | Count dialogs on first session after `/plugin install` |
| S-14c | `${user_config.KEY}` for an optional field left blank substitutes an empty string into `env` (not the literal placeholder, not a load failure) | Dummy server that echoes its env |
| S-14d | SessionStart `additionalContext` of ~12 KB is accepted without truncation and re-injected on `resume` / `compact` | `claude --debug` with a hook printing a 12 KB payload |
| S-14e | `extraKnownMarketplaces` + `enabledPlugins` in a scaffolded `.claude/settings.json` installs both plugins for a teammate and still prompts the `userConfig` dialog | Second machine, fresh trust |
| S-14f | `claude plugin tag` (`<name>--v<version>`) works with two plugins in one repository tagged on the same commit; `plugin-dependencies` field shape | `claude plugin tag --help`; `docs:plugin-dependencies` |
| S-14g | The plugin cache's 60-second `npm ci` cap is met for the 72 MB tree on a slow network; behaviour on failure | Throttled network test |

## E. Spike execution map (integration pass 2026-09-04)

Every spike is run by exactly one ARC-00 story (or, for the two that need product code, deferred to the story that builds that code); ARC-00-S14 adds the Status / Evidence columns to §A and §B when the verdicts exist. Consumers are the stories whose text carries a branch on the verdict.

| Spike | Run by | Verdict consumed by (primary) |
|---|---|---|
| S-01, S-16, S-17 | ARC-00-S04 | ARC-06-S01/S05/S09/S13 (dialog count, toggles), ARC-02-S11 / ARC-07-S09 (S-16), ARC-04-S04 / ARC-06-S08 (S-17) |
| S-02, S-09 | ARC-00-S10 | ARC-04-S04, ARC-07-S09 (`list_changed` fallback text); ARC-06-S13 (Path B restart text) |
| S-03, S-05, S-20 | ARC-00-S06 | ARC-06-S11/S12 and ARC-09-S08 (S-03, Q-B), ARC-06-S01/S05 and ARC-08-S08 (S-05), ARC-04-S11 / ARC-06-S01 / ARC-08-S03 (S-20) |
| S-04, S-08 | ARC-00-S07 | ARC-07-S01 (masked input), ARC-06-S11 (`bootstrap.cmd`), ARC-09-S08; Q-B |
| S-06, S-15 | ARC-00-S08 | ARC-06-S01 (`MCP_TIMEOUT`), ARC-06-S08; ARC-01-S05 / ARC-06-S07 (`npm ci` footprint) |
| S-07 | ARC-00-S09 | ARC-03-S05/S10/S11, ARC-06-S06 |
| S-11 | ARC-00-S11 | ARC-01-S04 (floors in `engine.config.json`), ARC-06-S04 |
| S-12, S-18 | ARC-00-S05 | ARC-05-S07 (globs vs explicit list; `ask` in auto mode), ARC-05-S11 |
| S-14a–g, S-19 | ARC-00-S12 | ADR-0006/0008 → entry gate of ARC-06-S01 (D-06 hedge); S-19 → ARC-01-S11, ARC-02-S02, ARC-05-S04 |
| S-10 | **deferred to the owner's live sitting (build @ `7a91986`)** — the agent holds no instance credentials. The unit half is CI-enforced by `tests/tools/gate-split.test.ts` (35 tools derived from the registered catalogue, three flag states); the runnable procedure with the exact fixture, both preset runs and the T-01…T-18 order is `docs/spikes/S-10-readonly-preset-sufficiency/PROCEDURE.md` | ARC-04-S14 (CHANGELOG note), ARC-08 doctor note |
| S-13 | deferred → ARC-02-S03 | ARC-02-S02, ARC-05-S04 |
| Close-out (Status/Evidence columns, deferrals, gate sign-off) | ARC-00-S14 | ARC-01-S01 (entry), ARC-06-S01 (entry) |

## C. Risks (known, accepted, with mitigations)

| ID | Risk | Mitigation in the plan |
|----|------|------------------------|
| R-01 | Node.js cannot be vendored; a consultant without Node is stopped at the first live-mode step | Design-only needs no Node; the launcher prints the exact per-OS install command; README lists prerequisites first |
| R-02 | Committed `packages/snowarch/dist/` can go stale relative to `src/` | CI rebuilds and `git diff --exit-code`; release script refuses to tag on a diff; contract sha ties dist, manifest and docs to one commit |
| R-03 | Server behaviour changes (SCRIPTING no longer gates reads, precedence inversion, no cwd `.env`, unconfigured start) affect anyone using snow-mcp 1.0.0 outside the Architect | Product 2.0.0 supersedes (R-1); CHANGELOG migration note (ARC-04-S14, ARC-09-S02); the optional npm channel publishes as a new major under the new record `@farstic/snowarch` (ARC-09-S10) |
| R-04 | Plaintext-at-rest credentials (one 0600 file) may not satisfy a client security team | D-04; keychain backend and client-credentials grant on the roadmap; prod defaults to read-only; audit log |
| R-05 | First live session's MCP output may exceed Claude Code's 25,000-token cap on wide tables | `maxRecords` 100 pinned; `_meta["anthropic/maxResultSizeChars"]` considered per tool in ARC-04 |
| R-06 | Skill auto-routing regression when 18 descriptions are rewritten to ≤ 500 chars | S-13 plus a full VALIDATION-TESTS run before 1.0 |
| R-07 | Users who ran the old setup keep stale `servicenow-mcp` / `nowaikit` entries with plaintext secrets in `~/.claude.json` and its `.bak-*` files | Doctor detects and prints the exact `claude mcp remove` commands and the backup-file reminder; ARC-10 checklist |
| R-08 | The two-repo history is lost to future readers of the new repo | ARC-10 imports both histories with `git subtree`/`filter-repo` where licensing allows, or links the archived repositories from `docs/ARCHITECTURE.md` |
| R-09 | ServiceNowDocs upstream refresh breaks citations (it did in July: 2 dead) | Weekly `docs-bump.yml` re-verifies before proposing a pin; the doctor fails on dead citations |
| R-10 | Windows native path is designed from docs, never yet run | CI `windows-latest` job on every commit from ARC-06 onward; S-03/S-04/S-08 before |
| R-11 | Licence not settled → nothing can be published | D-02 is a hard gate in ARC-00 |
| R-12 | Scope cuts remove features somebody relies on (desktop app, other clients, prompts) | D-03; history preserved in the archived repositories |
| R-13 | `enabledMcpjsonServers` semantics change in a future Claude Code release | The doctor checks `claude mcp get servicenow` status text; a floor bump plus a documented approval click is the fallback |
| R-14 | The engine's "Status" and rule files depend on the doctor cache; a stale cache misreports the mode | Banner re-runs the offline subset when `.mcp.json` or the store changed or the cache is older than 24 h; `/snowarch status` always re-runs `--quick` |
| R-15 | Consultant laptops sit behind corporate proxies and TLS-intercepting gateways; Node's built-in `fetch` (undici) ignores `HTTPS_PROXY`/`NO_PROXY` unless a proxy agent is configured, and custom CAs need `NODE_EXTRA_CA_CERTS` | ARC-04 must make the server's HTTP client honour `HTTPS_PROXY` / `HTTP_PROXY` / `NO_PROXY` and `NODE_EXTRA_CA_CERTS` (or document that it already does, if the client library reads them); the ARC-07 reachability diagnosis and the ARC-08 doctor report proxy and CA state by name **CLOSED by ARC-04-S11 (2026-09-08):** every request goes through `src/servicenow/http.ts` (undici `EnvHttpProxyAgent`, exact version 6.28.1), `NODE_EXTRA_CA_CERTS` is documented per OS in the package README, and `classifyNetworkError` names DNS / TLS-trust / proxy failures apart with a remedy each. **S-20 is CONFIRMED on macOS** (§F: the launching shell's environment reaches the spawned stdio server without any `.mcp.json` entry); **Windows is pending Robert's sitting**, so ARC-06 keeps forwarding the four variables as `${VAR:-}` as a hedge — and ARC-04-S11's empty-string sanitisation is what makes that hedge safe rather than harmful, since `${VAR:-}` expands to an empty string that undici would otherwise read as a proxy URL. |
| R-16 | `@farstic/snow-mcp@1.0.0` already exists on npm (published 2026-08-21 from an untracked state) while `01` §12 sets the first unified release to 1.0.0 and lints the package version equal to the root version — the optional npm channel could never publish that version, and R-03 already calls the server changes "a new major" | Decide the first unified version before ARC-01 (2.0.0 is the consistent choice: a new major for the server per R-03 and above the engine's 2.8.0 line as a product), or decouple the npm package version from the root version. **Decided 2026-09-04 (R-1): 2.0.0, published only under `@farstic/snowarch`.** |
| R-17 | The planned project skill `status` shares its name with Claude Code's built-in `/status` command; `docs:skills` defines precedence only between skill levels and bundled skills, not against built-in commands | **Decided 2026-09-04 (R-2):** the skill is `/snowarch` with sub-commands `status` · `setup-instance` · `doctor` (ARC-02-S11); the skills lint carries a built-in-name collision check (ARC-02-S02); the plain-text "Status" trigger stays in `CLAUDE.md` |

## D. Items the evidence base flagged as unverified that the plan does **not** depend on

- The published `@farstic/snow-mcp@1.0.0` tarball's provenance (built from an untracked working state) — relevant only to roadmap item 2, which republishes from CI with `--provenance`.
- `hasTrustDialogAccepted` in `~/.claude.json` as a way to skip the trust dialog — deliberately not used (it would edit `~/.claude.json`).
- Whether Claude Code's Bash tool exposes `CLAUDE_PROJECT_DIR` to the Claude-first clone step — the recipe uses `.` (the cwd) and does not need it.


---

## F. Rows added during ARC-00 execution (2026-09-06)

| ID | Assumption / finding | Status | Evidence | Consumed by |
|---|---|---|---|---|
| S-01 (partial) | Pre-seeded `enabledMcpjsonServers` removes the MCP approval dialog on first `claude` | **PARTIALLY REFUTED — pre-trust.** `claude mcp get servicenow` reports `⏸ Pending approval` with `enabledMcpjsonServers` pre-seeded, identical to no toggle at all, on 2.1.214 and 2.1.258; `disabledMcpjsonServers` IS honoured pre-trust (`✘ Rejected`, absent from `claude mcp list`). Reproduced independently by the architect on 2.1.258. The dialog count itself (post-trust) is still pending the owner's interactive run. | `docs/spikes/S-01-preseeded-approval/README.md` | ARC-06-S01/S05/S09/S13: design-only mode is dialog-free beyond trust (rejection applies early); live mode must budget trust + one MCP approval unless the post-trust run shows the pre-seed is applied at that moment — do not promise fewer dialogs than the owner's run records. D-06 interruption (2) is therefore expected, not exceptional. |
| S-21 (new) | `claude mcp get` / `claude mcp list` are inert on `~/.claude.json` | **REFUTED.** Each probe rewrites exactly one top-level key, `migrationVersion` (2.1.214 writes `13`, 2.1.258 writes `14`); alternating binaries flips it on every switch; a repeat on the same binary writes nothing; `projects` is untouched. Measured by whole-file diff (developer session); same-binary "no write" confirmed by the architect. | `docs/spikes/S-01-preseeded-approval/README.md` §finding (a) | ARC-08 (doctor must not shell out to `claude mcp …` for a read-only check — read `.mcp.json` and the settings files directly; if it ever must, document the `migrationVersion` write and never call it from the SessionStart banner); ARC-06/ARC-09 (a pinned 2.1.214 beside a newer global `claude` will churn this key — record, do not fight). |
| S-15 | Root `npm ci --omit=dev --ignore-scripts` footprint and module resolution | **CONFIRMED** on 9 CI cells (3 OS × Node 20/22/24) + macOS: handshake against the real `dist/server.js` reports 394 tools everywhere; no install scripts in the production tree (`esbuild`/`fsevents` are dev-only). Numbers: pre-cut 72 MB `du` / 57 MB content / 176 packages; **post-D-03-cut 27 MB / 17 MB / 135** (architect-reproduced). VM rows pending (Ubuntu login, Windows VM). | `docs/spikes/S-15-npm-ci/README.md` | ARC-01-S04/S09 gate (use the summed-content metric; `ls node_modules` is not a stable count), ARC-04-S01 (the cut is worth ~45 MB, not ~20), `docs/INSTALL.md` size sentence |
| S-06 | `MCP_TIMEOUT` via settings `env`; 394-tool cold start | **Cold start MEASURED**: worst `initialize` 694 ms, worst `tools/list` 750 ms over 27 runs on 9 cells + macOS (controlled env, 394 tools); `MCP_TIMEOUT=120000` keeps ≈160× headroom — value stands. The "settings `env` governs the spawned server's timeout" half is interactive and pending the owner's sitting (with S-01/S-16/S-17). | `docs/spikes/S-06-mcp-timeout-cold-start/README.md` | ARC-06-S01 (`.claude/settings.json` env), ARC-08 (banner budget) |
| S-07 | ServiceNowDocs submodule recipe size/time | **CONFIRMED — recipe C (shallow + cone-sparse, direct) wins on every machine on both axes**: macOS local 37.6 s / ~300 MB, ubuntu runner 23.6 s, macos runner 30.3 s, windows runner 35.6 s; all at the pin `ba513f2`, 20 areas, clean tree. Architect re-run from a fresh clone: 35 s, HEAD at pin, 20 areas, clean. **Recipe B (`--filter=blob:none`) is slower than the naive recipe A on all four machines — drop it.** The `03` fallback (≤ 350 MB) is not needed. The "without `core.longpaths`" Windows control was not run (CI sets it unconditionally) — one matrix dimension to add. | `docs/spikes/S-07-docs-submodule/README.md` | ARC-03-S02 (documents recipe C; do not offer B), ARC-06 B02, `docs/INSTALL.md` ("about 300 MB on disk and 25–40 seconds") |
| S-22 (new) | The engine cites only documentation areas that exist in the corpus | **REFUTED — live defect in the engine today**: one engine file cites `markdown/now-assist/`, which is not a directory in the ServiceNowDocs corpus (the real area is `intelligent-experiences`, also cited). Found while deriving the 20 cited areas for S-07; confirmed by the architect. | `docs/spikes/S-07-docs-submodule/README.md` §finding (c) | ARC-02 retired-name/citation sweep (fix the citation), ARC-03 citation gate (this is exactly what it exists to catch — add it as a regression fixture) | **CLOSED 2026-09-08 (ARC-03-S04).** The dead citation is repaired at its source: `.claude/agents/now-assist-specialist.md:71` cited `markdown/now-assist/`, which is not a directory at `ba513f2`; Now Assist's platform documentation lives at `markdown/intelligent-experiences/` (383 files, with an `index.md`) and the agent now cites that. Two further dead citations found by the same gate — `markdown/it-asset-management/{itam-subscrip-summary,subscription-itam-licensing}.md`, neither of which exists anywhere at the pin — were repaired in the same commit to pages that do. `vendor/docs-areas.txt` fell from 20 areas to 19 and `node scripts/docs.mjs verify` reports `checked: 161 | dead: 0`, exit 0. The refutation stands as recorded: the engine *did* cite areas that do not exist, and it was a tool rather than a reading that found them.
| S-19 | `claude plugin validate` runs on a headless CI runner without login | **CONFIRMED** — three runners, Claude Code 2.1.263 from npm, not logged in, no TTY, `CI=true`: exit 0 on marketplace, plugin, `--strict` and a skills-shaped directory; exit 1 on a manifest missing `name` and on malformed JSON. Architect re-run on macOS: exit 0 on the marketplace and both fixture plugins, exit 1 on a bad manifest. | `docs/spikes/S-19-plugin-validate-headless-ci/README.md` | ARC-05 lint may call `claude plugin validate --strict` in CI; the `03` fallback is not needed |
| S-14f | Two plugins in one repository tag and validate cleanly; a plugin can declare a dependency on another | **CONFIRMED for tagging** (`<name>--v0.0.1` per plugin at one commit; `validate` cross-checks `plugin.json` against the marketplace entry). **REFUTED for dependencies**: neither `plugin-dependencies` nor `pluginDependencies` exists in the 2.1.258 schema — unknown fields are ignored at runtime and rejected by `--strict`. | `docs/spikes/S-14f-plugin-tag-two-plugins/README.md` | ADR-0006 (a two-plugin layout cannot express engine→server dependency) |
| S-14a (partial) | `userConfig` keeps every option out of world-readable files | **PARTIALLY REFUTED.** `sensitive: true` → macOS keychain item, no file on disk (good). **Non-sensitive options → `~/.claude/settings.json`, USER scope, mode 0644, even for a `--scope project` install** (architect-confirmed: file is 0644 and carries the flow's `pluginConfigs`/`extraKnownMarketplaces` keys). For this product the non-sensitive options would be the instance URL and the user name → they would leave the per-checkout boundary D-04 was chosen for. Dialog masking itself: INTERACTIVE-PENDING. | `docs/spikes/S-14a-user-config-masked-dialog/README.md` | D-06 re-open decision (a plugin channel must mark URL and user name `sensitive` too, or D-04's confidentiality argument weakens); ARC-07 (never write instance URL/user name to user-scope settings on any channel) |
| S-14b (partial) | A plugin-bundled MCP server is visible to `claude mcp get`/`list` | **REFUTED** — exit 1, "No MCP server named servicenow", although `claude plugin list --json` prints the whole `mcpServers` block. Dialog count: INTERACTIVE-PENDING. | `docs/spikes/S-14b-plugin-server-approval/README.md` | ARC-08 ruling (doctor never shells out to `claude mcp` for reads) is load-bearing on the plugin channel, not merely tidy |
| S-14c (partial) | An unset `userConfig` option and a blank one are distinguishable | **CLI: REFUTED** — `--config KEY=` is rejected ("value is empty. Omit the flag"), so blank and absent are one state; `${user_config.KEY}` expansion for an unset option: INTERACTIVE-PENDING. | `docs/spikes/S-14c-empty-user-config-substitution/README.md` | ADR-0006 |
| S-14e (partial) | `marketplace add` works from a private git URL | **CONFIRMED over SSH** (exit 0). Split not in the assumption: the marketplace registration is **user**-scoped, the enable is **project**-scoped. Second machine: pending the Ubuntu login. | `docs/spikes/S-14e-scaffolded-marketplace-install/README.md` | ADR-0006, ARC-06 (if the channel is ever adopted, registration lands in user scope) |
| S-14d, S-14g | SessionStart injection size cap; plugin cache cold-install time on a throttled network | **PENDING** — d is interactive (12,299-byte payload and hook built and verified standalone); g needs the throttled VM (day 4). Reference: the same tree installs in 1.0 s warm / 3.5 s cold on macOS and 2.4–12.4 s across nine CI cells against the 60 s cap; ~17 MB post-cut. | — | ADR-0006 |
| S-07 AC 2 | Any docs recipe needs `core.longpaths` on Windows | **REFUTED** — `longpaths: [true, false]` matrix on `windows-latest`: all three recipes pass with it `false`. File-count metric reconciled in the record (`git ls-files` counts the full sparse index, 48,997; the working tree holds 34,688 + the 49-byte `.git` pointer); settled total 302 MB. | `docs/spikes/S-07-docs-submodule/README.md` | ARC-03-S02, ARC-09 Windows CI |
| S-07 (superseded 2026-09-06, evening) | — corrections from the ARC-00-S09 adversarial pass, each reproduced by the architect — | (1) **Recipe C leaves the superproject submodule uninitialised** (`git submodule status` shows a leading `-`; the tree is populated and both porcelains are empty, so nothing earlier could show it). `git submodule init` fixes it. **Recipe C is five steps**: `clone --sparse` → `sparse-checkout set` → fetch/checkout `<pin>` → `absorbgitdirs` → `git submodule init`. (2) **The `core.longpaths` control was an artefact of the runner's 56-char workspace prefix**: longest corpus path 197 chars → 253 on the runner (under 260) but **274** under a realistic `C:\Users\<name>\work\ai-servicenow-architect\vendor\ServiceNowDocs\` (over). AC 2 is **unanswered for the case that matters** → Windows VM at a realistic path (owner input #2). (3) Size per machine: 302 macOS / 303 macos-latest / 305 ubuntu / **315 windows** — still under the 350 fallback; INSTALL.md sentence: "about 300 MB on disk, ~315 MB on Windows". (4) **Areas: 19 vs 20** — `markdown/release-notes` (329 files, 5.7 MB) is cited only by the engine's `README.md:490`, by no skill/agent; the S-07 measurements included it, ARC-03's generated set (skills+agents scan) does not. ARC-03-S02 decides; architect recommendation: **exclude** (the citation gate's scope is skills/agents; README's link is informational) and re-state the numbers as "≤ the measured figures". (5) Pin-behind-tip costs recipe A ~32 MB of `.git`, not only ~10 s. | `docs/spikes/S-07-docs-submodule/README.md` (corrected record) | ARC-03-S02 (five-step recipe; the 19/20 reconciliation; INSTALL.md wording), ARC-08 (a submodule check must read the **superproject's** `git submodule status` and require the initialised form — a populated tree is not enough), ARC-09 Windows CI (longpaths row is a VM job, not a hosted runner) |
| S-14f (superseded) | — corrections from the ARC-00-S12 adversarial pass — | (1) **A plugin dependency field DOES exist on 2.1.258: plain `dependencies`, array-valued** (`["<plugin>@<marketplace>"]`); an object value fails with the typed error "expected array, received object" (architect-reproduced). The earlier "no such field" verdict probed only the two spellings `03` §B guessed. `03` §B's row is to name `dependencies` (array) — the shape `claude plugin prune|autoremove` operates on. (2) "Two plugins tag cleanly at one commit" was asserted from `--dry-run` alone → **PARTIAL** until real tags are created in the throwaway fixture (approved by the architect). | `docs/spikes/S-14f-plugin-tag-two-plugins/README.md` | ADR-0006 (a two-plugin layout CAN express engine→server dependency), ARC-05 |
| S-14a (superseded) | — correction from the ARC-00-S12 adversarial pass — | The "sensitive value never reaches a plaintext file" observation is **macOS-only**: Claude Code's secure storage is the keychain **with a plaintext fallback** (`~/.claude/.credentials.json`, 0600) where no keychain exists — **parity with D-04's per-checkout 0600 store, not an improvement**. Windows/Linux backing store: outstanding (VM rows). This removes the one argument the S-14 records made *for* re-opening D-06. | `docs/spikes/S-14a-user-config-masked-dialog/README.md` | D-06 decision at the close of the time-box |
| S-19 (note) | — from the S-12 pass — | `claude plugin validate` prints no "Validating … manifest" line for a skills-shaped directory (bare pass), and `--strict` turns warnings (e.g. missing `author`) into exit 1. **ARC-05's lint keys on the exit code, never on output text, and the product manifests carry `author`.** | `docs/spikes/S-19-plugin-validate-headless-ci/README.md` | ARC-05-S… lint story, ARC-01 manifest scaffolds |
| S-14f (final) | Two plugins in one repository tag, push and validate cleanly | **CONFIRMED** — distinct annotated tags at one HEAD, pushed and verified on the remote (architect: `git ls-remote --tags` shows them). Guards observed: exists → exit 1 with a `--force` remedy; dirty tree → exit 1 naming the file; `--force` → 0; `--push` → 0. **The exists check fires before the dirty-tree check**, so a stale tag masks a dirty tree until the version moves. `dependencies` (array) exists. | `docs/spikes/S-14f-plugin-tag-two-plugins/README.md` | ADR-0006; **ARC-09 release script: check the working tree before the tag-exists test, or a stale tag hides a dirty tree** |
| S-14 (time-box status, end of day 1) | — | a/b/c/e PARTIAL (owner sitting + VM), d INTERACTIVE-PENDING (owner), f CONFIRMED, g PARTIAL (throttled case → VM; 31× headroom unthrottled), 19 CONFIRMED. Evidence-only balance recorded in `docs/spikes/README.md` (S12 closing summary). ADR-0006 stays Proposed; the box closes on day 5 or when the owner sitting and the VM rows land, whichever is first. | `docs/spikes/README.md` | D-06 gate before ARC-06-S01 |
| S-23 (new, from ARC-00-S05) | The §2.1 write gate can be made mechanical by generating the `permissions.ask` block from the tool-name suffix list | **REFUTED — a name-derived gate leaves ≥34 write tools ungated.** Over the real 394 tools (architect re-derived from `snow-mcp/dist/tools-manifest.json`): 125 match the §2.1 suffix list, 269 do not; `snow_*_read` covers 92 of the 269, the three globs `_read`/`_index`/`_query` cover 205; **64 tools fall outside both lists**, among them `snow_cat_request_approve`, `snow_cat_request_reject`, `snow_deploy_deployment_rollback`, `snow_kb_knowledge_article_retire`, `snow_itam_asset_retire`, `snow_tsk_task_complete`, `snow_scr_changeset_commit`, `snow_ntf_attachment_upload`, `snow_ntf_emergency_broadcast_send`, `snow_intg_event_fire`, `snow_core_instance_switch`, `snow_usr_user_group_unassign`, `snow_inc_work_note_annotate`, `snow_cfg_set_properties_bulk`. CLAUDE.md §2.1's prose already says a missing suffix does not exempt a call; a *generated* block from that list would. No `mutates` field exists in the server today. Also: auto mode is the flag `--permission-mode auto` (choices include `auto` and `dontAsk`), not a key sequence. | `docs/spikes/S-18-permissions-ask-auto-mode/README.md`, `docs/spikes/S-12-middle-wildcard-globs/README.md` | **ARC-04-S04/S13: `mutates` is authored per tool from behaviour, never derived from the name; a test asserts every tool carries an explicit value.** **ARC-05-S07: the `ask` block is generated from `contract.json`'s `mutates` only; a regression test asserts the 14 tools above are in it; the no-glob `allow` fallback is 269 explicit entries, the three-glob form is 205 + the explicit remainder.** S-18/S-12 observations themselves: owner sitting Part C. |
| S-20 | Shell environment set where `claude` starts reaches the project stdio server | **CONFIRMED on macOS** — `NO_PROXY` (len 18) and `NODE_EXTRA_CA_CERTS` (len 137) arrived at the spawned server at exact lengths without being in `.mcp.json` `env`; `.mcp.json` `env` adds to, not replaces, the inherited environment. `HTTPS_PROXY`/`HTTP_PROXY` follow by the same mechanism (recorded as inference — a fake proxy breaks Claude Code's own API traffic, so they cannot be set in the launching shell). Bonus: `CLAUDE_PROJECT_DIR` is set in the server's env and `argv[1]` is the resolved absolute path (`01` §5 claims, now observed). **Story defect fixed:** the procedure must use a local listening proxy and a valid self-signed PEM, never `proxy.invalid` / an empty file. | `docs/spikes/S-20-shell-env-inheritance/README.md` | ARC-06: forwarding the four variables as `${VAR:-}` is not needed on macOS — keep as Windows contingency; ARC-08 E-26 stops hedging for macOS |
| S-05 | SessionStart hook behaviour when `node` is absent; which fallback | **PARTIAL** (notice text → sitting Part D, ~10 min). **Fallback ruled: option 2** — committed `.claude/settings.json` ships hook-free; the bootstrap writes the hook into `.claude/settings.local.json` only when Node ≥ 20 is present. Option 1 (`disableAllHooks: true`) would also disable the user's own and plugin hooks for the project — the product reaching into settings the user authored. | `docs/spikes/S-05-hook-without-node/README.md` | ARC-06-S05/S12 (one file, one write — the same file that holds `enabledMcpjsonServers`) |
| S-14a (Linux store) | Where Claude Code's secure storage lands on Linux | **MEASURED on the Ubuntu VM: no keyring at all** (`secret-tool`, `gnome-keyring`, `~/.local/share/keyrings` absent); the login lives in `~/.claude/.credentials.json`, mode 0600, 509 B, key names only recorded. Same secure-storage layer a `sensitive: true` `userConfig` value would use → **on Linux the plugin channel has no storage advantage over D-04's 0600 store**. Direct plugin-`userConfig` observation on Linux still pending (S-14a VM row). | `docs/spikes/S-14a-user-config-masked-dialog/README.md` | D-06 close-out |
| S-15 (VM) | — | Ubuntu VM: 5,038 ms, `du` 76M, **content 57.3 MB / 171 packages — identical to all nine CI cells and macOS** while `du` differs (72M macOS) — same tree, different filesystem. | `docs/spikes/S-15-npm-ci/README.md` | ARC-01/ARC-09 gates name the content metric |
| S-24 (new technique) | Project stdio servers can only be exercised inside an interactive session | **REFUTED — `claude -p --mcp-config <file> --settings <rules> --strict-mcp-config` reaches a project stdio server with no trust dialog and no per-server approval.** Reusable for CI/e2e tests and for pre-answering some owner-gated observations (which ones: pending the developer's per-step assessment — whether `-p` honours `permissions.allow/ask` like an interactive session is the open half). | `docs/spikes/S-20-shell-env-inheritance/README.md` §Deviations | ARC-08 (doctor `--live` handshake), ARC-09 CI (end-to-end tests without a TTY), ARC-05 gate tests |
| Sitting observations (owner, 2026-09-07, 2.1.258, Part A live variant, relayed) | — | `/mcp`: `servicenow · connected · 5 tools`, exact set A names; panel shows Args `./spikes/stub-server/server.mjs` (the `${CLAUDE_PROJECT_DIR:-.}` default) yet the server env carries `CLAUDE_PROJECT_DIR` (S-20). **Session started in `auto mode`**; in auto mode neither `./snowarch doctor --json` nor `./snowarch version` prompted (auto-approved) — S-16 is unreadable in auto mode. In **default mode** `./snowarch version` prompted (verbatim text in the S-16 record); Claude executes the compound form `./snowarch version 2>&1; echo "EXIT: $?"` — whether `Bash(./snowarch doctor*)` covers the compound `doctor` form is the next observation. A1 dialog count: pending. | `docs/spikes/S-16-project-permissions-allow/README.md`, `S-01`, `S-18` | ARC-06 "what you will see" text is mode- and version-dependent; ARC-05 allow rules must be written for the compound form Claude actually runs |
| S-12 | Middle-wildcard MCP permission globs are honoured | **CONFIRMED on 2.1.258 (headless via S-24, both directions):** `allow: ["mcp__servicenow__snow_*_read"]` → `snow_core_capabilities_read` RAN, `snow_core_records_query` BLOCKED; control T2 (no rule) BLOCKED proves `-p` does not auto-approve. Interactive confirmation optional. | `docs/spikes/S-12-middle-wildcard-globs/README.md` | **ARC-05-S07: generate the three-glob `allow` (`snow_*_read`/`snow_*_index`/`snow_*_query`) + explicit non-mutating remainder; the 269-entry fallback is retired** |
| S-16 (compound form) | `Bash(./snowarch doctor*)` covers the command Claude actually executes | **REFUTED for the wrapper:** Claude executes `./snowarch doctor --json 2>&1; echo "EXIT: $?"`; the rule covers the first segment only, the `echo` segment is evaluated separately and prompts (owner sitting: dialog option 2 offered to remember `echo "EXIT: $?"` on its own; headless T6: Claude ran only the first part and reported the compound form blocked). `allow` on the MCP tool `snow_core_capabilities_read` → no prompt (owner, manual mode; headless T3). "First Bash call after trust" half: pending the owner's `design`/`control` sessions in manual mode (the live session started in auto mode, where nothing prompts). | `docs/spikes/S-16-project-permissions-allow/README.md` | **ARC-05-S07: the generated `allow` also carries the exact wrapper entry for `echo "EXIT: $?"` (or an equivalent tolerant shape) and is tested against the executed compound form; ARC-06 INSTALL text: "in default/manual mode expect one approval for the wrapper the first time"** |
| S-24 (semantics) | — | Controls T1–T5: with `--settings`, `allow` → runs; no rule → "This command requires approval", nothing executed; `ask` → "you haven't granted it yet", blocked. Three distinguishable states → the route reads real rule semantics and can pre-answer matching questions; it cannot count dialogs, show `/mcp`, or observe a *prompt* (it denies instead). | `docs/spikes/S-16…`, `S-12…`, `S-20…` records | ARC-05 gate tests, ARC-09 CI e2e, sitting scope |
| S-01 (dialog count, 2.1.258) | Pre-seeded `enabledMcpjsonServers` removes the per-server approval on first `claude` | **CONFIRMED on 2.1.258 (owner sitting 2026-09-07):** `live` = **1** dialog (workspace trust only; `/mcp` connected, 5 tools); `design` = **1** (trust only; `servicenow` **absent** from `/mcp` — not "disabled"); `control` (no toggle) = **2** — the second is "New MCP server found in this project: servicenow · … Use this MCP server / Use this and all future MCP servers in this project / Continue without using this MCP server" (default = Continue without). Pre-trust the pre-seed is ignored (earlier row); it is applied at trust time. 2.1.214 row pending. The trust dialog itself lists the committed `permissions.allow` entries ("⚠ This folder pre-approves 4 tool permissions … These will apply without asking"). | `docs/spikes/S-01-preseeded-approval/README.md` | **ARC-06: live path = trust dialog only; design-only = trust dialog only** (D-06 interruption (2) does not occur on 2.1.258); INSTALL.md quotes the trust dialog incl. the pre-approved-permissions warning and the fallback wording "if you see 'New MCP server found', choose 'Use this MCP server'" |
| S-06 (settings env half) | `env.MCP_TIMEOUT` in `.claude/settings.json` governs MCP startup | **CONFIRMED headless (2×2 on 2.1.258):** 2000 ms + 5000 ms delay → tool unavailable; 15000 → connects; 2000 + fast → connects; `exit(1)` control → unavailable. **Product rules from the same test:** a dead or timed-out MCP server exits **0** with **no stderr** (157-byte harness line only), even under `--debug`; and the assistant **fabricated a diagnosis** (named a real instance hostname from the operator's global CLAUDE.md, advised checking a PDI that does not exist in the fixture). A5 in the sitting is now only "what does `/mcp` show on failure" (2 min). | `docs/spikes/S-06-mcp-timeout-cold-start/README.md` | **ARC-08/ARC-09: the doctor and CI treat their own handshake result as the failure signal, never the exit code; the generated rule file forbids the engine from explaining MCP failures — it quotes the doctor.** |
| S-16 (wrapper non-determinism) | Bash allow rules can cover the product's own commands | **REFUTED as a reliable mechanism:** Claude wraps the same request differently per turn (`… 2>&1; echo "EXIT: $?"` vs `… > f 2> f; echo "exit=$?"; wc -c f f`); the `doctor` segment matched the rule both times, the wrapper segments prompted both times, and the remembered-rule offer covered only the `echo` segment. MCP tool allow rules match exactly (`snow_core_capabilities_read`: 0 prompts, owner + headless T3). | `docs/spikes/S-16-project-permissions-allow/README.md` | **ARC-04/ARC-05/ARC-08: the in-session status/doctor path is an MCP tool call (`snow_core_status_read` + a doctor-summary tool), not a Bash call; Bash allow entries remain only for the bootstrap launcher, documented as "one approval the first time"** |
| S-17 (2.1.258, owner) | An unconfigured stdio server is accepted as connected; a failing one is visible | **CONFIRMED.** Set A (5 tools) → `✔ connected · 5 tools`; negative control `STUB_EXIT_ON_START=1` → `servicenow · ✘ failed`, footer "※ Run claude --debug to see error logs"; detail view shows Status/Command/Args/Config location and Reconnect/Disable — **no error text**. | `docs/spikes/S-17-unconfigured-server/README.md` | ARC-06-S08, ARC-08: a green `/mcp` is meaningful; a red one carries no cause — the doctor supplies it |
| S-06 (2.1.258, owner, interactive half) | — | **CONFIRMED.** `env.MCP_TIMEOUT: "2000"` in the committed settings + `STUB_STARTUP_DELAY_MS=5000` → `servicenow · ✘ failed`, detail view **identical to the crash case** — Claude Code's UI does not distinguish a timeout from a crash. | `docs/spikes/S-06-mcp-timeout-cold-start/README.md` | ARC-08: the doctor distinguishes timeout (handshake exceeded budget) from crash (process exited) itself and prints which; INSTALL.md wording: "if `/mcp` shows ✘ failed, run `./snowarch doctor`" |
| Owner sitting — Part A status | — | **Part A complete on 2.1.258 (2026-09-07):** S-01 live 1 / design 1 / control 2; S-16 matching + wrapper finding; S-17 both halves; S-06 both halves. Remaining for the owner: Part B (plugin, ~30 min), Part C1/C2 (S-18 prompt in auto mode), Part D (S-05 notice, ~10 min), optional 2.1.214 repeat of Part A. | `docs/spikes/OWNER-SITTING.md` | ARC-00-S14 close-out |
| S-06 (rule 2 sharpened) | — | **Correction:** the MCP failure IS surfaced to the model's context as a typed notice — `servicenow (CONNECT_TIMEOUT): "MCP server servicenow connection timed out after 2000ms"`, `(CONNECTION_CLOSED): "Connection closed"`, `(ENOENT): "Executable not found in $PATH: node"`, healthy → none — but only there (not stderr, not exit code, not the `/mcp` panel). A prompt that asks the model to *explain* produced the real cause plus invented ones; a prompt that asks it to *quote* returned the notice exactly. | `docs/spikes/S-06-mcp-timeout-cold-start/README.md` | **ARC-05 rule file: "on any MCP failure quote the typed notice verbatim and the doctor's output; add nothing" — the doctor emits the same vocabulary (CONNECT_TIMEOUT / CONNECTION_CLOSED / ENOENT).** |
| S-05 (VM) | SessionStart hook with `node` absent: session continues with a non-blocking notice | **HALF REFUTED on Ubuntu 2.1.263:** the session continues, but there is **no notice at all** (marker file absent, `NO-HOOK-NOTICE`), while in the same session the MCP server announced `ENOENT`. A hook failure is invisible → nothing load-bearing may live in a SessionStart hook. Part D of the sitting is one TTY question. | `docs/spikes/S-05-hook-without-node/README.md` | ARC-06 option 2 fallback (hook only via `settings.local.json` when Node present) — now the stronger reason; ARC-08 banner is informational only |
| S-07 (Ubuntu, git 2.34.1) | Recipes work on the current Ubuntu LTS git; git floor 2.25 is sufficient | **REFUTED — two ways.** (1) `git submodule update --filter` does not exist on 2.34.1 → recipe B is unavailable (already dropped). (2) **Recipes A and C exit 0, report a clean tree at the right pin, and silently omit the five corpus root files** (`.gitignore`, `LICENSE`, `README.md`, `llms.txt`, `llms_template.txt`) — architect-verified on the VM: 0/5; the same recipe on git 2.39.5 (macOS) → 5/5; same OS with git 2.55 in CI → complete. Variable = git version. A vendored corpus missing `LICENSE` is an attribution defect that every recipe check passes. | `docs/spikes/S-07-docs-submodule/README.md` | **ADR-0008 (Proposed) supersedes ADR-0001's `floors.git`: bracket 2.34.1 broken / 2.39.5 good; seed carries `"2.39.5"` until the floor is narrowed by measurement; ARC-03/ARC-08: the corpus check asserts the five root files and the cone areas, not only the pin; ARC-01 seed and doctor updated** |
| S-14 (Part B, owner, in progress) | — | `marketplace add` → "declared in **user** settings"; `claude plugin install … --scope project` installs **without prompting** ("4 userConfig options not yet set (3 required) — run /plugin configure … or pass --config KEY=VALUE") → terminal masking is not observable at install; first session `/mcp` with required options unset → **no plugin server listed at all** (no plugin section). `/plugin configure` menu: four options, `*` on the three required. Masking observation pending. | `docs/spikes/S-14a…`, `S-14b…` | D-06 close-out; ARC-06 if the channel is ever adopted (install is silent; configuration is a separate in-session step) |
| S-07 (root cause, ruling) | — | **Root cause isolated:** on git 2.34.1 `git sparse-checkout set --cone <dirs>` stores the literal `--cone` as a pattern, so cone mode never enables and the root directory is never matched (architect-reproduced: `list` prints `--cone`; `init --cone` + `set` warns "disabling cone pattern matching"). **Ruling — ADR-0008 option A:** keep `floors.git` at **2.34.1** (lowest proven-correct) and add an idempotent repair step to recipe C (`git ls-files -v` → root paths marked `S` → `update-index --no-skip-worktree` + `checkout --`; no-op on ≥ 2.39). ADR-0008 supersedes only ADR-0001's `floors.git` row; recipe C is now five steps + repair. | `docs/spikes/S-07-docs-submodule/README.md`, `docs/decisions/ADR-0008-git-floor.md` | ARC-03-S02 (recipe text), ARC-01 seed (`floors.git: "2.34.1"`), ARC-08 completeness check stays |
| S-14e (second machine) | Scaffolded settings (`enabledPlugins` + `extraKnownMarketplaces`) install a plugin on a teammate's machine | **REFUTED (headless, Ubuntu 2.1.263):** the marketplace is read (listed) but nothing is installed — `plugin list []`, cache empty, **no notice**; `enabledPlugins` behaves as a record, not an instruction; the cache is what makes a plugin real; an explicit `plugin install` (session 3) works end to end. Open half: an interactive first run in a freshly trusted checkout (sitting B6). | `docs/spikes/S-14e-scaffolded-marketplace-install/README.md` | D-06: on the plugin channel the bootstrap must run `marketplace add` + `plugin install` itself — the interruption class the channel was meant to remove |
| S-14 (Part B, owner, 2.1.258) | — | **S-14a:** `/plugin configure` — the `sensitive` Password field is **partially masked: asterisks with the trailing 6 characters visible in clear**; terminal `plugin install` prompts for nothing. **S-14b:** after configure + `/reload-plugins` the server appears as `plugin:servicenow-server:servicenow · ✔ connected · 5 tools` **under the "Built-in MCPs" section**, no approval dialog; the permission dialog for a mutating tool shows the display name `plugin:<plugin>:<server> — <title…>` and **never the `mcp__…` identifier**. **S-14c:** unset optional `userConfig` → `SNOW_OPTIONAL: set but empty` (empty string). **New:** in the plugin-bundled server **`CLAUDE_PROJECT_DIR` = the user's HOME**, not the project directory (`cwd` was the project) — pending VM reproduction. | `docs/spikes/S-14a…`, `S-14b…`, `S-14c…` | D-06 close-out: (1) partial masking weakens the channel's credential argument further; (2) a plugin-bundled server cannot locate a per-checkout store via `CLAUDE_PROJECT_DIR` if the HOME observation holds; (3) permission rules must be authored against the identifier the model sees, not the dialog's display name |
| S-14b (prefix) | The plugin-channel tool identifier is `mcp__plugin_<plugin>_<server>__<tool>` | **CONFIRMED (owner, 2.1.258):** `mcp__plugin_servicenow-server_servicenow__snow_probe_env_read`, verbatim from the model's tool list; the permission dialog shows only `plugin:servicenow-server:servicenow — <title…>`. | `docs/spikes/S-14b-plugin-server-approval/README.md` | ADR-0006; ARC-05 (if the channel is ever adopted, every generated rule keys on this prefix; the display name is never a rule) |
| S-14g | The plugin cache's dependency install completes within its 60 s cap and fails loudly if not | **REFUTED on both halves (Ubuntu VM, ingress shaping with a throughput-probe control):** unthrottled 5.2 s (cold; the earlier "1.9 s / 31× headroom" was a warm cache); **4 Mbit → 36.5 s (1.6× headroom)**; **1 Mbit → 61.4 s: `npm ci` is cut at the cap, the tree is half-populated (37 M vs 74 M, same 155 top-level entries), and `claude plugin install` prints "✔ Successfully installed plugin" with exit 0.** Third instance this session of "work fails, surface says success" (with `-p` exit 0 on a dead MCP server and the silent SessionStart hook). | `docs/spikes/S-14g-plugin-cache-npm-ci-cap/README.md` | D-06 close-out (shipping the runtime tree through the plugin cache is unsafe on ordinary connections); **ARC-06: the bootstrap runs `npm ci` itself and asserts completeness (every lockfile package directory present) rather than trusting any wrapper's exit code; ARC-08 doctor: same completeness check** |
| S-14d (owner, 2.1.258) | A 12 KB `additionalContext` from a plugin SessionStart hook survives into the model's context | **Startup: CONFIRMED** — debug log shows the hook emitting `S14D-BEGIN bytes=12299 lines=205 … S14D-END bytes=12299 lines=205`, and the model quoted `S14D-END bytes=12299 lines=205` verbatim from its context. `/compact` and `--resume` halves pending. | `docs/spikes/S-14d-session-start-additional-context/README.md` | ADR-0006; ARC-06 (if a SessionStart banner is ever used for injection — it is not, per S-05) |
| S-14g (correction, same evening) | — | **"Half-populated" was the state at cap time, not the end state.** 400 kbit row: 61.3 s, `✔ Successfully installed`, exit 0, 17 M at cap time; the "155 top-level entries" count is identical for 74 M and 17 M trees (worthless as a completeness signal — only bytes distinguish). **`claude plugin install` returns at the cap while `npm ci` keeps running behind it**: the tree grew from 17 M to 73 M minutes later, passed through a state where `node_modules` was **absent entirely**, and at that moment 0 of 8 direct dependencies imported. A session started right after the install can find zero working dependencies; the eventual steady state is unmeasured (timeline experiment running). | `docs/spikes/S-14g-plugin-cache-npm-ci-cap/README.md` | **D-06:** the plugin cache is not a safe delivery vehicle for the runtime tree on ordinary links. **ARC-06/ARC-08 completeness assertion needs a "when":** on the monorepo path the bootstrap runs `npm ci` synchronously itself, so quiescence is by construction and the assertion runs after npm exits; the doctor's check must tolerate no in-flight npm (assert no `npm` process for the tree, or retry) — never trust a wrapper's return |
| S-14d (final) | — | **CONFIRMED (owner, 2.1.258):** 12,299-byte `additionalContext` intact at startup, after `/compact`, and after `--resume` (model quoted `S14D-END bytes=12299 lines=205` all three times). | `docs/spikes/S-14d-session-start-additional-context/README.md` | ADR-0006 |
| S-14g (final) | — | **Settled from `~/.npm/_logs`:** at 1 Mbit the underlying `npm ci` fails — `error process terminated / signal SIGTERM / code 1` — while `claude plugin install` had already printed `✔ Successfully installed plugin` and exited 0 at ~61 s; npm kept running ~110 s from start (≈50 s after the prompt returned), then rolled back; steady state 17 M, **0 of 8 direct dependencies importable** (`ERR_MODULE_NOT_FOUND`). The install's exit code that would have caught it is discarded one layer down. Measurement caveats recorded (the `pgrep` column missed `node …/npm-cli.js`; the npm log is the evidence). **REFUTED on both halves.** | `docs/spikes/S-14g-plugin-cache-npm-ci-cap/README.md` | D-06 close-out; **design number for any plugin-path check: ≥ 50 s overhang after the install returns** — wait for quiescence or retry; monorepo path unaffected (synchronous `npm ci`) |
| Plugin uninstall (owner, 2.1.258) | `claude plugin uninstall <id>` removes a project-scope install | **Observation:** refuses with "enabled at project scope (.claude/settings.json, shared with your team)" even after that file was deleted — the enablement lives in `~/.claude/plugins/installed_plugins.json`; `--scope project` is required. The message names a file that no longer exists (misleading surface; the registry is authoritative — same lesson as S-14e). | `docs/spikes/S-14e-scaffolded-marketplace-install/README.md` | ARC-06 cleanup text if the channel is ever adopted; ARC-08 "stale registration" detector reads the registry, not settings files |
| S-14e (interactive half, owner, 2.1.258) | A freshly trusted checkout scaffolded with `extraKnownMarketplaces` + `enabledPlugins` installs the plugins on first interactive run | **REFUTED.** CLI-generated SSH marketplace shape (`{"source": {"source": "git", "url": "git@github.com:…"}}` — a fourth shape) + both plugins enabled; marketplace removed beforehand; never-trusted path. After trust: `/plugin list` → "No plugins installed. Use /plugin install to install a plugin."; `/mcp` → no plugin server; **no notice of any kind**. Combined with the headless rows: scaffolded settings never install a plugin, on any path, silently. | `docs/spikes/S-14e-scaffolded-marketplace-install/README.md` | D-06 close-out: on the plugin channel a teammate must run `marketplace add` + `plugin install` themselves — the interruption class the channel was meant to remove; ARC-06 text (if ever adopted) must say so |
| S-18 (owner, 2.1.258, auto mode) | A `permissions.ask` rule on a mutating MCP tool prompts even in auto mode | **CONFIRMED.** `claude --permission-mode auto`, `ask: ["mcp__servicenow__snow_core_record_add"]` → calling the tool shows: "Tool use · servicenow — Snow Core Record Add Tool: (MCP) · About …: Stub: create a record (mutates). · **Permission rule mcp__servicenow__snow_core_record_add requires confirmation for this tool.** /permissions to update rules · Do you want to proceed? 1. Yes · 2. Yes, and don't ask again for servicenow — Snow Core Record Add commands in <path> · 3. No". The trust dialog lists only the `allow` entries, not the `ask` rule. Manual-mode repeat and the no-`ask` control pending. | `docs/spikes/S-18-permissions-ask-auto-mode/README.md` | **§2.1 is mechanical:** ARC-05's generated `ask` block enforces the write gate under auto mode; ARC-05's PreToolUse fallback story can be retired once the control lands |
| S-14e (final) | — | **REFUTED on both paths.** Interactive B6: the session read the scaffolded `extraKnownMarketplaces` key and *acted* on it (marketplace registered at user scope) while `enabledPlugins` beside it installed nothing, silently — so trust-gating is excluded; one key is an instruction, the other a record. Uninstall: deleting `.claude/settings.json` does not disable a plugin; the refusal names the deleted file; `--scope project` required. | `docs/spikes/S-14e-scaffolded-marketplace-install/README.md` | D-06 close-out; ARC-06 cleanup text |
| S-25 (new) | A local-path (`directory`) marketplace is a faithful stand-in for a git one | **REFUTED (VM):** with a `directory` source the cache entry is fully populated — including a complete `npm ci` — and then **not used**: the server launches from the source directory (`argv[1]` = `~/marketplace/plugins/…/server.mjs`), which has no `node_modules`. Harmless for the dependency-free fixture; a plugin with dependencies would fail at runtime while `plugin list` reports it installed. Anyone developing the plugin channel from a local checkout exercises a different code path than users. `CLAUDE_PROJECT_DIR` was correct in that context — so the owner's HOME reading (git source, cache execution) is neither reproduced nor refuted; reproduction assigned on the Mac with the git source. | `docs/spikes/S-14b…`, `S-14g…` records | D-06; any future plugin-channel spike must use a git-source marketplace |
| Pattern (4/4) | — | `claude -p` exits 0 on a dead MCP server (S-06) · a SessionStart hook that cannot spawn says nothing while the MCP server in the same session reports `ENOENT` (S-05) · `claude plugin install` prints "✔ Successfully installed" while its `npm ci` is SIGTERM'd, exit 1 (S-14g) · a scaffolded checkout silently has no plugins (S-14e). | `docs/spikes/README.md` | **ARC-08 design rationale: `snowarch doctor` detects every one of these itself; no product path trusts a Claude Code surface message or exit code as a success signal** |
| S-18 (final, owner, 2.1.258) | — | **CONFIRMED with control.** `ask` rule present: prompt in auto mode AND in manual mode, both naming the rule; `allow`-listed `capabilities_read` unprompted in both. **Control (no `ask` rule, auto mode): `snow_core_record_add` executed with no prompt — `stub: would create record`.** The `ask` block is what makes §2.1 mechanical; auto mode alone approves a mutating MCP tool. Incidental: the model's narration again named a real instance hostname from the operator's global CLAUDE.md although the stub has no instance (redacted in records). | `docs/spikes/S-18-permissions-ask-auto-mode/README.md` | ARC-05-S07 generated `ask` block is load-bearing; the PreToolUse fallback story is retired; ARC-06 INSTALL text: "auto mode never writes to your instance without this prompt" |
| S-05 (interactive, owner, 2.1.258, variant B) | The interactive TTY reports a SessionStart hook that cannot spawn | **CONFIRMED for the TTY:** startup screen shows "SessionStart:startup hook error / Failed with non-blocking status code: /bin/sh: /nonexistent/node: No such file or directory"; session starts, prompt usable, non-blocking. Headless (VM, variant A: `node` stripped) showed nothing. The trust dialog does not mention hooks. Variant B (`/nonexistent/node` interpreter) was used because this Mac's `claude` is an npm launcher (`#!/usr/bin/env node`), which variant A would have killed. | `docs/spikes/S-05-hook-without-node/README.md` | ARC-06 option 2 stands: a non-blocking, easily missed notice (and none headlessly) is not a channel the product may rely on |
| S-14a (Linux, final) | — | **REFUTED on Linux by direct measurement:** a `sensitive` plugin `userConfig` value lands in `~/.claude/.credentials.json`, mode 600, plaintext (file grew 509→609 B at install; no keyring on the machine); `~/.claude/settings.json` holding the instance URL and user name is mode **664** there. The channel's "secure storage" on Linux is exactly D-04's 0600 file, with a worse mode on the non-sensitive file. Windows remains open. | `docs/spikes/S-14a-user-config-masked-dialog/README.md` | D-06 close-out |
| `CLAUDE_PROJECT_DIR` (plugin channel) | A plugin-bundled server receives the project directory | **Observed once, unreproduced.** Owner (interactive, auto mode, git source, cache execution): `= HOME`. Developer on the same Mac, same source and execution context, headless: `= cwd` under project scope, user scope, git and non-git folders. Only remaining difference: interactive/auto session. Not a finding; one interactive owner probe would settle it. | `docs/spikes/S-14b-plugin-server-approval/README.md` | D-06: not currently a blocker for reaching the per-checkout store |
| S-18 (legibility note) | — | On the monorepo path the `ask` dialog names the rule identifier (`mcp__servicenow__…`) — a user can read the exact allow-rule off the screen; on the plugin path it shows only `plugin:<plugin>:<server>`. **The trust modal lists only `allow` entries and never the `ask` list** — the safety story is invisible at the moment of trust. | `docs/spikes/S-18-permissions-ask-auto-mode/README.md` | D-06 comparison (monorepo more legible); **ARC-06 INSTALL text states the ask list explicitly because the platform will not** |
| Owner sitting — CLOSED 2026-09-07 (2.1.258) | — | Parts A, B (B5 moot), C, D complete; cleanup verified (`plugin list []`, only the official marketplace, cache and `~/spike-runs/*` removed, `migrationVersion` 14 — one binary throughout). Verdicts closed for 2.1.258: S-01, S-16, S-17, S-06, S-12, S-18, S-05 (interactive), S-14a/b/c/d (macOS), S-14e (both paths). Left open: every Windows row (owner input #2), the optional 2.1.214 repeat of Part A, and the unreproduced `CLAUDE_PROJECT_DIR` interactive reading. | `docs/spikes/OWNER-SITTING.md` | ARC-00-S14 close-out |
| S-13 (addendum) | Claude Code discovers project skills from **every** `.claude/skills` between cwd and the filesystem root, not only cwd's own | **CONFIRMED** by experiment 2026-09-08. A scratch project created beneath a directory that already carries `.claude/skills` loads BOTH rosters and `project:` counts them together — the CLI prints the walk-up itself: `Loading skills from: … project=[<scratch>/.claude/skills, <ancestor>/.claude/skills]` → `project: 29` where 28 was expected. `env -u CLAUDE_PROJECT_DIR` changes nothing; it is the directory walk-up, not an env var. **This is a product fact, not a test artefact:** a user who clones the engine beneath a folder that already has `.claude/skills` gets both sets, with the budget in S-13 spent on both. | `docs/spikes/S-13-skill-description-cap/README.md`; `scripts/ci/skill-listing-check.mjs` (`pollutingAncestors`, `cleanScratchParent`); `tests/skill-listing.test.mjs` | ARC-02-S03 (measurement isolation), ARC-04 (doctor check, owned by the architect), ARC-08 |
| S-26 (new, from ARC-04-S08) | MCP clients actually send `_meta["anthropic/maxResultSizeChars"]` on `tools/call`, so a result cap read from it is ever exercised in the field | **UNVERIFIED — recorded as a candidate, not a finding.** ARC-04-S08's story specified the key and the server now honours it, but nothing in the MCP SDK types, the schema, or any observed request in this repo's own stdio probes shows a client sending it. The env fallback (`SNOW_MAX_RESULT_CHARS`, default 100000) is what will be in force if no client ever sends it, and the unit tests cover both branches — so the code is correct either way and only the *documentation* would be wrong. Numbered S-26 because S-25 is taken by the marketplace-source spike; the ARC-04-S08 brief called it "S-25 candidate". | `packages/snowarch/src/utils/result-size.ts`, `tests/utils/result-size.test.ts` | ARC-05 (do not state in the contract or USER-GUIDE that clients set this until one is observed doing it); a `claude -p` run with `--mcp-config` (the S-24 technique) logging raw `tools/call` params would settle it cheaply |
