# ARC-08 — Doctor, self-heal and the session banner

Status: **COMPLETE 2026-09-11 — all 11 stories (S01–S11), started 2026-09-10** · Depends on: ARC-04 (server doctor module), ARC-05 (data-driven names), ARC-06 (state file, toggles), ARC-07 (probes); ARC-00 S-05/S-06/S-13 · Blocks: ARC-09 (CI uses the doctor), ARC-10 (cutover checks)

Decisions applied: D-01…D-06, Q-A, Q-B, R-1…R-3 (see `02-DECISIONS-NEEDED.md`). Naming used here: server package `packages/snowarch` (`@farstic/snowarch`, first release `2.0.0`); MCP key `servicenow` → tools `mcp__servicenow__snow_*`; project skill `/snowarch` with sub-commands `status` · `setup-instance` · `doctor`; engine checks `E-xx`, server checks `SV-xx` (the `S-xx` prefix is reserved for the spikes in `03`); vocabulary Mode `design-only` | `live`, Preset `read-only` | `pdi-developer` | `full` | `custom`.

## Goal

One command, one merged report, one authoritative `Mode:` line: an engine doctor and a server doctor with stable check IDs, a `--fix` whitelist that repairs what is safe and prints the exact command for what is not, a SessionStart banner that keeps the engine truthful about its mode every session, and a generated troubleshooting catalogue so every failure names its remedy.

## Why it exists

Closes P-16 (bash-only doctor that needs Node anyway and prints the username), P-17 (the reference install fails its own doctor — the new installer must produce a passing doctor on first run), the mode-detection half of P-05/P-21 (the engine no longer parses `~/.claude.json`; it asks the server), P-03 at runtime (`*_NOT_ENABLED` mapped to `set-preset`), and P-34 (stale registrations with secrets detected). The existing `scripts/doctor.sh` is the most precise specification of a correct install (39 checks, `00` §3.9); this ARC preserves every check's intent under the new sources.

## Scope

**In:** `tools/snowarch/lib/doctor/` (engine checks E-xx), `packages/snowarch` doctor module (server checks SV-xx), the merged report and JSON schema, `--quick|--no-network|--fix|--section|--json`, the D-id mapping table, `hooks/session-start.mjs`, the `/snowarch status` skill body, `docs/TROUBLESHOOTING.md` consumption, stale-registration and legacy-store detection, the D-04 cloud-sync-folder WARN and the R-3 proxy/CA host check.
**Out:** the checks' underlying fixes in other ARCs; upgrade (ARC-09); the probes themselves (ARC-07); `instance import --from-legacy` (ARC-07).

## Deliverables

- Engine checks E-00 … E-27 (each with id, title, severity, remedy, `fixable`): CLI floor and login; git floor; repo root; `.mcp.json` / `.claude/settings.json` hashes and placeholder defaults; credential-shaped keys absent; docs corpus present, pin, branch, sparse set, citations; roster from directory listing; description lengths; retired names; prefix consistency (`engine.config.json` ↔ `.mcp.json` ↔ rule file); generated files fresh; stale `~/.claude.json` entries for this folder (`servicenow-mcp`, `nowaikit`) → prints `claude mcp remove <name> -s local` and the `.bak-*` reminder; legacy `~/.config/servicenow-mcp/` present → prints `./snowarch instance import --from-legacy`; checkout under a cloud-sync folder (OneDrive/Dropbox/iCloud/Google Drive) → WARN (D-04); proxy/CA environment (`HTTPS_PROXY`, `NO_PROXY`, `NODE_EXTRA_CA_CERTS`) inspected with masked values and echo of the network classifier code (R-3); Claude Code registration status via `claude mcp get servicenow` compared with the recorded mode (`03` R-13; expected by ARC-06-S01/S05); capability packs (docx / PDF QA / draw.io / Mermaid) reported as capabilities; Windows notes (file modes ACL-inherited).
- **The S-03 fallback does not exist yet (ARC-06-S11, 2026-09-09).** If the Windows sitting shows
  that Claude Code does not expand `${CLAUDE_PROJECT_DIR}` in `.mcp.json` on native Windows, the
  bootstrap will write a per-machine local override (`claude mcp add-json … -s local`) and record
  `mcpJsonOverrideSha` in the state — and E-07 verifies that sha. Until the spike runs there is
  nothing to check, and this line exists so its absence is a decision rather than an oversight.
- **Doctor candidate raised by ARC-06-S05 (2026-09-09): `.claude/settings.local.json` unparsable.**
  B07 refuses invalid JSON and changes nothing — but its inputs hash (mode, Node presence, hooks
  branch, registration) deliberately does **not** include the file's content, so on a checkout that
  has already bootstrapped once the step reports `ok (cached)` and never re-reads it. That is the
  hash table behaving as ARC-06-S03 specifies; it means the bootstrap is the wrong place to notice
  a file that became unparsable afterwards, and the doctor is the right one. Add it as an E-check
  with the same sentence B07 prints, and `--fix` must NOT rewrite it — the file belongs to the
  operator.
- Server checks SV-00 … SV-07 (from `packages/snowarch` `doctor`): Node floor; `dist/server.js` and module resolution; store presence, schema, modes; per instance: URL shape, auth probe (basic and ROPC), per-preset probes, six flags explicit and dependency-consistent, `toolPackage == full`, `@servicenow/sdk` when FLUENT; MCP stdio handshake against the real `dist/server.js` with tool set == contract; `snow_core_capabilities_read` == store; audit log present and writable.
- `docs/ARCHITECTURE.md` appendix: mapping table old `D00–D37` → new `E-xx`/`SV-xx` (every old intent accounted for; D19/D20 path checks and D36 prefix check re-targeted; D32/D33 probes moved to the server module).
- `--fix` whitelist (idempotent, reported): deps → B04; docs missing/unsparse → B02; pin drift → `git submodule update --checkout`; flags < 6 → explicit `"false"`; store modes → chmod; toggles → rewrite for recorded mode; stale `doctor-last.json` → re-run. Never: credentials, `.mcp.json`, `~/.claude*`.
- `hooks/session-start.mjs` (exec form, `timeout` 10 s, < 300 ms typical): reads `.local/doctor-last.json`, re-runs the offline `--quick` subset when older than 24 h or when `.mcp.json`/store mtime changed; prints one line (`Mode: …`), plus first-run nudge, upgrade nudge, stale-registration nudge; never prints secrets; never blocks.
- `/snowarch status` skill: runs `./snowarch doctor --quick --json` and prints Mode line, engine version, docs pin, roster, capability packs. When `node` is not on PATH (design-only install without Node) it reads `.local/bootstrap-state.json` instead and reports the mode with "doctor unavailable until Node 20+ is installed".
- Output redaction: usernames masked, secrets as `set (len n)`; `--json` schema documented for the skill and CI.
- Runtime error mapping in the generated rule file (stop, no retry, remedy verbatim) and VALIDATION-TESTS T-19 (`AUTHENTICATION_FAILED`) / T-20 (`*_NOT_ENABLED`).
- CI: doctor after the design-only bootstrap on three OSes, JSON snapshot test, redaction and stale-registration fixture tests, banner timing.

## Dependencies

ARC-04-S02/S04/S11/S12 (store module, `snow_core_capabilities_read`, network classifier codes, server doctor module boundaries), ARC-05-S03/S04/S05/S06/S08/S10 (lint library, rule renderer, error-code registry, contract loader), ARC-06-S01/S02/S04/S05/S06/S07/S08/S12/S14 (committed files, CLI skeleton, steps as functions, toggles, B08, CI bootstrap job), ARC-07-S03/S08/S09/S10/S11 (probes, `import --from-legacy`, `setup-instance` skill branch, `ruleText` for the wizard's codes, live E2E harness), ARC-03-S05/S06/S11 (`docs sync`, `docsStatus()`, E-12 text), ARC-02-S02/S07/S10/S11/S13 (lint rule ids, `gen-roster.mjs --json`, platform note, `/snowarch` skill file, `tests/VALIDATION-TESTS.md`), ARC-01-S04 (`engine.config.json`), ARC-00 S-01/S-05/S-06/S-20 verdicts and ARC-00-S04/S06/S13 (S-01 status strings, S-05/S-20 verdicts, Windows recipe). ARC-09-S04 (`./snowarch version` — the `engine.tag` field) and ARC-09-S07 (`upgrade-check.json`) are optional inputs; ARC-09-S08 later places the doctor CI job on its final cells. Two state-file keys are required of ARC-06-S03/S05/S12 by S02/S03: `hooksDisabledByBootstrap` and `registration`.

## Acceptance criteria

- [x] On the reference machine after ARC-06/07, `./snowarch doctor` reports **0 FAIL**; the old `scripts/doctor.sh`'s four failures (dead citations, absent NOW_ASSIST/FLUENT, retired names) are impossible by construction and covered by E-checks that pass. (S02, S05, S11) — *in CI, on all nine `node-cli` cells: `DOCTOR: 26 ok, 0 warn, 1 fail (10 skip) — expected here: E-00`, the one failure being that a hosted runner has no Claude Code, asserted BOTH ways by `scripts/ci/assert-doctor.mjs --expect-fail E-00`. The **reference machine** run (where `claude` is installed, so 0 FAIL outright) is the owner's sitting D1.*
- [x] Every check id from the old doctor appears in the mapping table with its new id or an explicit "retired because …" reason. (S07 — `docs/ARCHITECTURE.md` appendix, rendered from `tools/snowarch/lib/doctor/mapping.mjs`; `tests/doctor/mapping.test.mjs` asserts all 38 ids both ways against `scripts/legacy/doctor.sh`, 2026-09-10)
- [x] `./snowarch doctor --json` output pasted into a chat contains no secret and no clear-text username (test greps the JSON for the fixture credentials). (S01, S03, S04, S11) — *`tests/doctor/redaction-e2e.test.mjs`: a fixture store with `someone.fixture@corp.example.com` and a random 24-character password, read by the real command, searched in the text report, `--json`, `--fix` and `--fix --json`, on three OSes. Proved to bite: drill PR #121 broke `maskUsername` and CI went red with `json: the username reached the output unmasked`.*
- [x] `--fix` repairs a store entry with four flags, a docs checkout with a wrong sparse set, and a missing `settings.local.json` toggle in one run, and reports each; it refuses to touch `.mcp.json` when its hash differs and prints the `git checkout -- .mcp.json` command instead. (S06) — *`tests/doctor/fix.test.mjs`, the composite fixture.*
- [x] The SessionStart banner appears within 1 s of session start on the CI machines and shows `Mode: design-only` or `Mode: live — …` matching the store; with Node absent the launcher's `disableAllHooks` fallback leaves the session clean (S-05). (S08, S11) — *five cold spawns per cell, medians:* **ubuntu 330 ms · macOS 614 ms · Windows 910 ms · Windows-without-Git-Bash 895 ms**, *budget 1000 ms, in each cell's step summary. The `no-node` cells assert the other half: the launcher seeds `doctor-last.json` itself so the banner has a truthful line on a machine that cannot run the doctor.*
- [x] A copied `~/.claude.json` fixture with stale `servicenow-mcp` and `nowaikit` entries produces the exact removal commands in the report. (S03, S11) — *`tests/doctor/stale-registration-e2e.test.mjs`, through the real command with `HOME` and `USERPROFILE` redirected: both `claude mcp remove` lines verbatim, no fixture credential in the output, and `~/.claude.json` byte-identical after a plain run, `--json` and `--fix`.*
- [x] With a fake `claude` on PATH printing `✘ Rejected (see disabledMcpjsonServers in settings)` and `bootstrap-state.mode == "live"`, E-27 WARNs with the `run ./snowarch mode live` remedy; with `claude` absent it is `skip`; `--quick` never runs it (`03` R-13). (S03) — *`tests/doctor/legacy.test.mjs`; `skip` confirmed on every CI cell, where `claude` is absent.*
- [x] Runtime: when the server returns `AUTHENTICATION_FAILED`, the engine (per the generated rule file) stops and prints the `set-credentials` remedy; the VALIDATION-TESTS gain a test for this. (S10) — *the rule file's "Runtime errors" section, nineteen rule-visible codes; `tests/VALIDATION-TESTS.md` **T-19**. The behavioural run against a PDI is the owner's sitting D5.*
- [x] Design principle 10 ("Propose, don't impose") holds for `--fix`: the plan is shown and Enter applies; `--yes` accepts it for CI. (S06)

**Deferred to the owner's sitting, each with its row in `docs/spikes/OWNER-SITTING.md`:** D1 (the reference-machine doctor run, and S02's floors on a machine that is not a runner) · D2 / D2b (S03's leftover detectors against a real previous install; S04's live probes) · D3 (S08's banner in a real Claude Code session — plain stdout vs `additionalContext`) · D4 (S09's `/snowarch status` in four sessions) · D5 (S10's T-19 and T-22 live, and their dormant variants). Every one of them needs a session or a live instance; none is a code path this repository can reach.

> **Candidate check, recorded at the ARC-06 docs-sync fix (2026-09-09).** `docs-bump.yml` depends on
> a repository setting no file in the tree can assert:
> `gh api repos/<owner>/<repo>/actions/permissions/workflow` → `can_approve_pull_request_reviews`.
> With it off, the weekly bump moves the pin, pushes its branch and fails at `gh pr create` — a
> failure that looks like a workflow bug and is not one.

## Risks

> **Amendment 2026-09-07 (from `03` §F "Pattern 4/4").** Design rationale of record: four independent Claude Code surfaces reported success or said nothing while the underlying work had failed (dead MCP server → exit 0; unspawnable hook → silence; plugin `npm ci` SIGTERM → "✔ Successfully installed"; scaffolded plugins → nothing). The doctor therefore performs its own handshake, its own hook probe, its own dependency-tree completeness check and its own registration read, and never treats a Claude Code message or exit code as evidence.

> **Amendment 2026-09-07 (from `03` §F S-06 / S-16).** (1) MCP startup failure is signalled by the doctor's own `initialize`/`tools/list` handshake, never by Claude Code's exit code (0 on failure, no stderr). (2) `/snowarch status` and the in-session doctor summary are **MCP tool calls** (`snow_core_status_read`, `snow_core_doctor_read` — ARC-04 adds the latter), because MCP allow rules match exactly and Bash allow rules do not survive Claude's non-deterministic command wrapper. (3) The generated rule file states: on any MCP failure the engine quotes the doctor's output and never diagnoses (regression: a fabricated "PDI hibernating" diagnosis naming an unrelated real hostname).

> **Amendment 2026-09-07 (from `03` §F S-24 / S-20).** The `--live` handshake and the CI end-to-end checks may use `claude -p --mcp-config <file> --settings <rules> --strict-mcp-config`, which reaches a project stdio server with no trust dialog and no approval (proven on macOS). E-26 (proxy/CA) reports the launching shell's variables as inherited on macOS; the Windows row stays hedged.

> **Amendment 2026-09-06 (from `03` §F S-07).** The docs-corpus check reads the **superproject's** `git submodule status` and requires the initialised form (no leading `-`) at the pinned SHA; a populated `vendor/ServiceNowDocs` tree with an uninitialised gitlink is a FAIL with the remedy `git submodule init` (folded into `./snowarch docs sync`).

> **Amendment 2026-09-06 (from `03` §F S-21).** No doctor check, banner, or `--fix` action may shell out to `claude mcp get|list|add` for a *read-only* purpose: those commands rewrite `migrationVersion` in `~/.claude.json` when Claude Code versions alternate. Registration state is read from `.mcp.json`, `.claude/settings.json` and `.claude/settings.local.json` directly. The one legitimate `claude mcp` call (E-23's printed remedy for stale registrations) is printed for the user, never executed by the doctor.

- Doctor drift from the checks in other ARCs. Mitigation: each ARC's acceptance criteria name the doctor check that proves them; CI runs the doctor after the bootstrap (S11 snapshot test).
- Banner cost on slow disks. Mitigation: cached JSON; `--quick` subset bounded; hook `timeout` 10 s; in-hook watchdog 5 s (S08).
- Whether the environment the doctor sees equals the environment Claude Code's spawned server sees (proxy/CA variables) — spike S-20, raised by ARC-04-S11 and run in ARC-00-S06 (ARC-00-S14 adds its row to `03` §A, which does not list it yet); until answered E-26 reports "as seen by this shell".
- `claude mcp get servicenow` output wording is Claude Code's (`03` R-13); E-27 recognises the S-01-recorded strings and degrades to a WARN "could not read registration status" on anything else — never a false FAIL.
- ARC-04-S12's `CheckResult` fixes `fixable: false` and lacks `code`/`command`/`data`/`durationMs`; S01 extends it with optional fields in a PR against ARC-04's tree (backwards-compatible, tested).
- ARC-07-S03 (probes) may land after S04; SV-04 stays a stub reporting `skip: probes not available` until it does — CI never depends on SV-04.

## Stories

Full write-ups: [`STORIES.md`](STORIES.md).

| ID | Title | Size |
|---|---|---|
| ARC-08-S01 | Doctor framework: check registry, sections, severities, remedies from the contract, JSON schema, exit codes, redaction | L |
| ARC-08-S02 | Engine checks E-00…E-22: prerequisites, repo wiring, docs corpus, roster, contract | L |
| ARC-08-S03 | Stale-registration, legacy-store, cloud-sync, proxy/CA and registration-status detectors (E-23…E-27) with exact commands | M |
| ARC-08-S04 | Server checks SV-00…SV-07 integrated: module import, probe wiring (basic + ROPC), FLUENT SDK, capabilities equality, section `server` | M |
| ARC-08-S05 | Merged report, the authoritative `Mode:` line, `--quick` / `--no-network` / `--section`, capability packs, `.local/doctor-last.json` | M |
| ARC-08-S06 | `--fix` whitelist with per-fix reporting and refusal rules | L |
| ARC-08-S07 | Old→new check mapping table (`D00–D37` → `E-xx` / `SV-xx` / retired) in `docs/ARCHITECTURE.md` | S |
| ARC-08-S08 | `hooks/session-start.mjs` banner: cache, staleness re-run, nudges, hook timeout, S-05 handling | M |
| ARC-08-S09 | `/snowarch status` skill body: doctor-JSON rendering and the no-Node fallback | M |
| ARC-08-S10 | Runtime error mapping in the generated rule file; VALIDATION-TESTS T-19 (`AUTHENTICATION_FAILED`) and T-20 (`*_NOT_ENABLED`) | M |
| ARC-08-S11 | CI: doctor after bootstrap on three OSes, JSON snapshot test, fixture-driven detector tests, banner timing | M |

Total: 22–30 engineer-days (≈ 4.5–6 weeks for one engineer). Critical path: S01 → S02/S04 → S05 → S06/S08 → S11.

### Chores

Work that is not a story: a defect found while building one, fixed in the same arc.

| ID | What | Status |
|---|---|---|
| ARC-08-C1 | The `--json` report carried the instance LABEL in seven fields and the instance HOST in two, and ARC-10-S10's issue template asks a stranger to paste exactly that output into a public tracker. ARC-10-S07's lint would have refused the same bytes in a validation record, so the two rules disagreed about the same content — one mechanical, one a review note. Found by running the doctor against a live fixture rather than by reading the writer: only a live run has instances to name, and the earlier fixture runs never went live because **SV-02 refuses a group/world-readable store** — a fixture written at the default 0644 is found and not loaded (`mode 0644 is group/world-readable`), so the doctor stayed design-only and every field below was empty | **Closed** — masked at the `--json` BOUNDARY (`lib/doctor/json-boundary.mjs`), not at the nine sites that compose a string: a check added next month is covered the day it is written, and the alternative — nine call sites and a review note — is the arrangement that produced the defect. Masking is by VALUE (every label from `server.instances[].label`, longest first; the host by C9's `INSTANCE_HOST`, now exported rather than copied), so a field nobody listed is covered too, and a test plants one to prove it. The TEXT report is unchanged and still names the instance — it is local, and a practitioner needs to know which instance the doctor is talking about; the test asserts BOTH directions, or "the fixture never had a label" would pass every assertion. `--fix` reads the in-process report and is unaffected. Cost, documented by a test rather than discovered: a label of `dev` also masks the environment token, `(<label>)` — word-bounded matching cannot tell a user's label from the same word used as an environment, and a list of words a user may not choose is wrong for whoever chooses one **Second pass (same chore, after the Windows CI cell): the CHECKOUT PATH was a third leak of the same kind.** `--json` carried it in three fields — `checks[].detail`, `checks[].data.root`, `checks[].data.toplevel` — each reading `/Users/<me>/work/ai-servicenow-architect` on a real (non-fixture) run, and on every platform that path contains the user's account name. It surfaced on Windows alone because the C1 test scans a live fixture for all six S07 patterns and the fixture's own temp root is home-shaped on exactly one platform — `C:/Users/runneradmin/AppData/Local/Temp/…` matches, `/var/folders/…` and `/tmp/…` do not. A test asserting the right general property therefore passed on two platforms while the product leaked on all three. Masked two ways, because neither alone is enough: the run's own home BY VALUE (a home can be `/opt/people/ana` and match no pattern), guarded so that a home of `/` or a bare drive root is ignored rather than matching inside every absolute path — and taken as an ARGUMENT, because the first version read `process.env` and an existing guard (`mode-register.test.mjs`, *nothing under `lib/` opens `~/.claude.json`, or anything else in the home directory*) failed the build on it. That guard was right and is the better design: `bin/snowarch.mjs` calls `homedir()` once and threads it, which is also what lets a test point a whole run at a fixture without touching the environment; and the four generic shapes `/Users/<x>`, `/home/<x>`, `C:\Users\<x>` and the forward-slash form Node returns on Windows. All become `~`, and what follows survives — depth, a space in the path, the drive letter — which is the part a maintainer reading a pasted report uses. The platform accident is fixed at the cause: a unit table over five path shapes with three negative controls (`/var/folders`, `/tmp`, and the CI runner's own `D:\a\…`, which must keep every character), plus a live run with `HOME` set to the fixture root asserting that literal string appears nowhere in the JSON. **The standard is stated in the file now** — this boundary is held to S07's six patterns, not to the fields a ruling happened to name |


| ARC-08-C2 | **E-25 could not see Windows Known Folder Move, and the bootstrap's own two halves disagreed about it.** `env[]` in `packages/snowarch/tests/fixtures/cloud-sync-paths.json` is the case where an enterprise policy redirects `Documents` into OneDrive: the word OneDrive appears nowhere in the path, and — in the fixture's own words — `%OneDrive%` is *"the only detector there is"*. The server's `detectCloudSync(path, { env })` has read it from the start. The bootstrap's `cloudSyncProvider(path)` took no env at all, and E-25 called it with `ctx.root` alone, although `ctx.env` was right there. Found while closing ARC-07's B07-02, where the fixture's third section turned out to be read by nobody. **The sharper half, measured before the fix:** `lib/cloud-sync.mjs` imports the server's `isUnderCloudSyncFolder` precisely so the two *"cannot disagree about a path exactly when it mattered"* — its own header — but that function delegates to `detectCloudSync` with DEFAULT options and therefore reads the AMBIENT environment. So with `%OneDrive%` set, `isUnderCloudSyncFolder('C:/Users/me/Documents/work/x')` answered **true** while `cloudSyncProvider` answered **null** and `cloudSyncWarning` printed **nothing** — one module, two answers, on exactly the case the import exists to prevent | **Closed** — `cloudSyncProvider(p, { env = process.env })` checks the three `ONEDRIVE_ENV_ROOTS` first, in the server's order, with the same case-insensitive segment-prefix rule; `cloudSyncWarning` threads it; E-25 passes `ctx.env` (which `childEnv` carries through in full — checked, or the fix would have been inert). **Deliberately NOT matched to the server in one respect:** no `realpath` seam. The server resolves both sides through an injected one; this stays pure stdlib because B01 runs it in a checkout that has never seen `npm ci`, and the pre-install property is worth more than exact parity on symlinks — the fixture's rows need no resolution. The pinned gap test from B07-02 did its job: it failed the moment the detector learned `env`, with the message *"make this a passing case"*, and it is the passing case now. Both parity tests read the third section — `tests/doctor/host.test.mjs` for E-25 and `tools/snowarch/tests/b01-b07.test.mjs` for the bootstrap — and **each row is asserted against its own expected provider**, because the section carries its own negative: `C:\Users\me\work\x` with the variable set to a different folder, *"a set variable is not a blanket yes"*. A loop expecting three warns would have demanded exactly the bug where one set variable syncs the whole disk; mine did, and the fixture caught it. The argument for the whole exercise is in the module's own comment: *"The server's detector had the same ordering bug, found by the same fixture row"* — a shared fixture has already caught one cross-implementation defect, which is why the third implementation must answer all three of its sections |
| ARC-08-C3 | **The bootstrap summary printed a failure count and named nothing.** Sitting A's B09 summary read `DOCTOR: 8 ok, 0 warn, 1 fail` with no FAIL line; by the time the full doctor was run minutes later it had none, so **nobody can now say what failed**. The failing checks were already in the `doctor --quick --json` output B09 parses — it took `parsed.summary` and discarded the rest, including them. A count with nothing named is a number nobody can act on, and the one run that saw the failure is the one run that could have said what it was. | **Fixed.** Every FAIL is printed in full under the count, with its remedy (`E-10 FAIL <title>: <detail> — <remedy>`). A clean run is byte-identical — no failures, no extra lines — and a test asserts the named failures and the counted failures are the same number, in both directions. **The same defect was later found one step EARLIER in the same program, and is fixed in `ARC-06-C10`:** B00's preflight reported `N prerequisite(s) missing` and named none of them. Nobody went looking — it surfaced because the exit-code work made someone read B00's output closely. Recorded in both rows on purpose: it is one defect in two places, and a reader who finds either should be told about the other |
| ARC-08-C4 | **E-27's remedy could not clear the thing it was complaining about, and printed itself twice.** On a design-mode checkout with a LOCAL registration the check said *"server is not disabled in Claude Code although the recorded mode is design-only"* and offered `./snowarch mode design` — twice, because `remedy` and `command` carried the same string and the report renders them on consecutive lines. It cannot work: `--register` defaults to **"unchanged"**, so `mode design` rewrites the toggles and leaves the `claude mcp` entry — the very thing keeping the server visible — exactly where it was. The owner followed it and nothing changed. | **Fixed.** The remedy follows `entry.scope`: a local registration gets `./snowarch mode design --register project` with `claude mcp remove <key> -s local` named as the direct alternative; a project registration keeps the plain remedy, which does work for it. Said **once** — the duplicate `command` is gone. Tested both directions, so a project registration is never sent to `--register project` it is already in |
| ARC-08-C5 | **SV-08 told every owner on earth to move their checkout out of their home directory.** The warning — *"skills are also loaded from: ~ — the roster is larger than this repository defines and the listing budget is spent twice"* — fired because the ancestor walk counted any directory holding `.claude/skills`, and `~/.claude/skills` is one. **Measured on Claude Code 2.1.258 on the sitting checkout** (home redacted): `Loading skills from: managed=/Library/Application Support/ClaudeCode/.claude/skills, user=~/.claude/skills, project=[~/snowarch-sitting-a/ref/.claude/skills]`. `project=[…]` names **only the checkout**; the home directory is not in the project list and `user=` is its own bucket, exactly as S-13 recorded. So it was a **false positive**: the user scope counted as an ancestor project, with a remedy nobody can follow, since every checkout is under a home. S-13 — the record this walk was ported from — measured the doubling with a decoy in a real ancestor PROJECT and a scratch directory that was not under the home; **the home case was never measured and the generalisation was wrong.** | **Fixed.** The walk stops before the home directory and never reports the user-scope directory as an ancestor; the remedy leads with `/skills`, which always works, and no longer asks anyone to leave a directory they cannot leave. Both directions tested: a checkout under a home with `~/.claude/skills` present warns about nothing, and a checkout under a real ancestor project still warns and names that ancestor — so the exclusion corrects the check rather than silencing it |
| ARC-08-C6 | **B09 reported a FAIL the doctor did not — because it never asked the doctor, and counted a PREVIOUS run's failure instead.** Sitting A saw `mode design` succeed and print `DOCTOR: 8 ok, 0 warn, 1 fail` while `./snowarch doctor` right after printed `30 ok, 4 warn, 0 fail`. **Neither hypothesis was right — it is not a check at all, and the real cause is a better finding than either.** `doctorCounts` gated on `which('snowarch')`, i.e. whether the command is on **PATH**, while the spawn two lines below runs `<root>/tools/snowarch/bin/snowarch.mjs` **by absolute path and needs nothing on PATH**. Nobody installs `snowarch` globally to use a checkout, so the gate was false in ordinary use, the doctor was never asked, and the fallback tally printed under a line that says DOCTOR. That tally counts `state.steps`, which **persists between runs** — so B06's failure from the earlier `mode live` was reported by the later, successful `mode design`. Reproduced on a clone: after a failing `mode live`, a succeeding `mode design` printed `7 ok, 1 warn, 1 fail`, and the state file's own tally was `{ok:6,warn:1,fail:0}` before it — matching the printed line exactly, which is how the fallback was identified. Item 4's failure-naming could never have named this one: there was no doctor result to name. | **Fixed by asking the right question:** the gate is now `existsSync(<root>/tools/snowarch/bin/snowarch.mjs)` — what the spawn actually needs. Verified on the same clone and the same state: `DOCTOR: 13 ok, 1 warn, 0 fail, 25 skipped`, identical to `./snowarch doctor --quick` run standalone, so the two now agree. A control test keeps the fallback's behaviour pinned — given no doctor, it still counts the persisted `fail` — so the fix cannot be mistaken for cosmetics |
| ARC-08-C7 | **Six wordings of one state — and the first fix's test could not see four of them.** For "there is no instance yet" the doctor's Mode line said `instance add`, the bootstrap's Next block said `mode live`, and after those two were unified the tree still held four more: **B08's warning** (same runner, same process), **`/snowarch status`** (the surface a user actually reads inside Claude), **two server call paths** (`servicenow/context.ts`, `servicenow/instances.ts` twice), and **`scripts/gen-doctor-docs.mjs`**, which hardcoded the sentence and wrote it into the **published `docs/ARCHITECTURE.md`** while its own docstring promised the page is "rendered from the real modules". **The first test is the real defect here:** it asserted the surfaces its author had enumerated, so every copy he had not seen passed. An enumeration is a memory, not a search. | **Fixed: one definition per side, and a SCAN instead of a list.** Engine — `ADD_INSTANCE(cli)`, rendered by the Mode line, the Next block, B08 and the docs generator, and propagated by `npm run gen` to the launchers, README, INSTALL and ARCHITECTURE. Server — `packages/snowarch/src/no-instance.ts`, one constant for all four server sites, **pinned** to the engine's by test rather than imported, because the package has no dependency on `tools/` and a generated source file under `src/` would make its build wait on the engine's generator — the `AUTHENTICATION_FAILED` arrangement this repo already uses. The test now scans `tools/snowarch/lib`, `packages/snowarch/src`, `scripts/` and the generated files for the STATE and requires the one remedy or an allowlist entry naming the state it belongs to. **Two earlier patterns died on contact and are recorded in the file:** matching the commands caught 70 lines including `a prod instance ADDitionally needs --ack-prod`; narrowing to command spellings still caught 32, because a command is not a remedy. A second test fails on a **dead allowlist entry** — it caught one of mine immediately. Run on `b666490` it names **8** places; run after the fix it is green |
| ARC-08-C8 | **`./snowarch doctor --section host` printed 39 lines for 2 results** — 37 of them `skip … not in --section`, which is not a finding about the machine but a restatement of the flag the reader just typed, and it buried the two lines that were findings. Not a recorded design choice: nothing in the plans or the code documents listing them, and the summary already carried a skipped count. | **Fixed.** With `--section`, out-of-section checks are **counted but not listed**: `3 ok, 0 warn, 0 fail, 1 skipped, 35 not in section`, and the output went from 39 lines to 10 on the measured command. The two kinds of skip are now separated because they mean different things — *"this checkout could not answer"* stays `skipped`, *"you did not ask"* becomes `not in section` — and `notInSection` is counted as a **subset** of `skip`, never an addition, so the totals still reconcile. Every other skip reason (`--quick`, design-only, `--no-network`) is a fact about the checkout and is still listed. Both directions tested, including that a run without `--section` is unchanged |
| ARC-08-C9 | **`doctor --json` published the user's other project folders — and the issue template asks a stranger to paste it.** Measured on the owner's machine: `grep -c Documents` over `doctor --json` = **9**, with details reading `also registered under: ~/Documents/work/<folder> (<server>) — run the same command from that folder`. Those are engagement and client folders on a consultant's laptop. `json-boundary.mjs` masks the **home prefix**, which is right for this checkout's own path — depth, spaces and drive letter are what a maintainer uses — and useless here: `~/Documents/work/<client>` survives it and the client name is the whole of the secret. The repo's own rule for validation records (*a record nobody can publish is a record nobody writes*) applies to the JSON the Install-problem form requests. | **Fixed by keeping the folder out of everything that travels.** E-23 now returns a redacted `detail` (counts, **distinct server names**, and the credential shape in the repo's own `set (len n)` form — the half that makes the finding urgent) plus `otherProjectCount`; `project` is stripped from `data.entries`; and a new **`textDetail`** carries the folders for the terminal, which is where the user goes to run the command. `checkToJson` copies an explicit field list, so a field it does not name **cannot** reach a pasted report by accident. The refusal test is `tests/validation-records.test.mjs`'s discipline pointed at the JSON: no user-chosen home-relative path, no `project` field, no other-project folder — while asserting the finding SURVIVES. *(The refusal is written against user-chosen paths rather than against `~/` itself: `~/.claude.json` is the subject of the check, a fixed filename naming nobody, and forbidding it would make the finding unintelligible.)* **Sweep, as asked, on a live report:** no absolute home path anywhere; **E-25 ok, E-11 fail, SV-02 warn all ran and carry no path**; `E-24` carries only the fixed `~/.config/`; **`E-05` carries this checkout's own `~/work/<dir>`** — the case `json-boundary.mjs` argues for keeping, but it is still a folder the user chose, and on a consultant's machine it can be a client name. Flagged for a ruling, not changed unilaterally. E-25 and SV-02 have path-carrying branches (a real cloud-sync folder, a real store) that this state did not exercise |
| ARC-08-C10 | **One removal command for two stale server names.** The remedy printed a single `claude mcp remove <server> -s local` while the findings named two different servers with five entries between them, so following it cleared part of the mess and left the rest. | **Fixed: one removal per DISTINCT server name.** Deduping the rendered line was not enough — the same server under this folder and under another produces two different "run from …" suffixes, so the same removal was offered twice; the **name** is what the command acts on, so the name is the key. Found by my own test failing with *"a server name was offered a removal twice"* |
| ARC-08-C11 | **"then review and delete …" printed BEFORE the step it refers to.** With nothing registered under *this* folder, the remedy consisted only of the backup reminder, while the single removal arrived on the renderer's separate `command` line **below** it — so the instruction to do something "then" came first. | **Fixed.** Every removal is in the remedy, in order, with the reminder last; the duplicated `command` field is gone, since it was what put the reminder above the step. Asserted by index — every removal line must appear before the reminder — rather than by matching the text |
| ARC-08-C12 | **A failure symbol inside a success.** E-27's `ok` line read `Claude Code registration status: ✘ Rejected (see disabledMcpjsonServers in settings) · read by running claude, …` — Claude's own ✘ text, quoted verbatim, on a line whose status is `ok`. A reader who scans for trouble stops at the ✘, and a reader who trusts the status ignores a symbol that sometimes does matter; the line teaches people to distrust it in both directions. | **Fixed.** In design mode with a rejected project entry the line is rendered in our words — `project entry present, disabled in design mode (expected)` — and Claude's raw status is kept in `data.statusLine`, where `--json` carries it for a machine and a person never reads it. Asserted both ways: the detail contains no `✘`, and the raw text still survives in the JSON |
| ARC-08-C13 | **E-28 said the remote had no release tags while the checkout stood on one.** On `v2.0.0-rc.2` the skip read `origin advertised no release tags`, which reads as "the remote is empty" to somebody who can see the tag they are on. | **It is by design, and now it says so.** `sortTags(...).filter((t) => t.pre === null)` drops prereleases deliberately — ARC-09-C31 records the decision, that a maintainer's release candidate must not nudge every user to upgrade to it. The wording is now `advertises no non-prerelease tags; rc tags are ignored by this check` when tags exist, and the original sentence is kept for a genuinely empty remote, so the two cases stay distinguishable. C31's test asserts the reason is stated rather than left to be inferred |
| ARC-08-C14 | **SV-02's healthy state — the one the check exists for — was never asserted.** The suite pinned it as `warn` (no store) and `fail` (0644), and **nothing pinned it as `ok`**. A healthy 0600 store is written fifteen times in `doctor.test.ts` and the doctor runs over it, so there was incidental exercise — but `summary.fail === 0` is equally true of a check that had quietly become `warn` or `skip`. **Found from the other side:** the architect's `rc-checks-2` 5.8b had been red since rc.2 because a dangling `$HOME` symlink made the doctor crash *before* it reached SV-02, and the harness reported *"chmod mentions 0"* rather than *"the doctor never got there"*. Two instruments, one defect — **an absent check reads as an absent failure** | **Fixed with two assertions, and the second is the one the harness lacked.** (1) A healthy 0600 store makes SV-02 **exactly `ok`** — not `not.toBe('fail')`, because `warn` and `skip` are both *not fail* and both are what a silently-unreached check looks like. The Windows branch keeps the ACL-note shape and **says in its own words why that is a different correct answer rather than a laxer one**, so the platform exception cannot be read as the check doing less. (2) **Reachability:** SV-02 must be PRESENT in the report in all three states the suite exercises — no store, healthy 0600, 0644 — which turns *"the doctor never got there"* from a silent pass into a named failure. **The control is what makes the gap provable, and the first attempt at it proved nothing:** patching the first `return ok(` after SV-02's id hit **SV-03's** return, and 24/24 stayed green. Re-aimed at SV-02's own status it failed 2 — but that broke the 0644 case too, so it still did not isolate the gap. Sharpened to degrade **only the healthy path** (`status === 'ok' ? 'warn' : status`), **exactly one test fails and it is the new one**, every pre-existing test passing. That is the gap, measured. Our fixture was already the real schema (version 1, `instances` map, `auth` object) — unlike the harness's — so this pins behaviour rather than repairing a fixture. **Test-only: it does not invalidate rc.5 and rides into whatever tag comes next** |
| ARC-08-C15 | **E-10 judged the toggles without the registration, and failed a state that was correct.** The owner's Sitting C on rc.5, after `./snowarch mode live --register local`: `DOCTOR: 12 ok, 1 warn, 1 fail, 25 skipped (1 fixable — run ./snowarch doctor --fix)` and `E-10 FAIL settings.local toggles match the recorded mode: mode is live but servicenow is disabled — ./snowarch mode live`. **The state was right.** `mode.mjs` settles a local registration before the steps precisely because an enabled project entry alongside a local one would load the server twice, so on that path the project toggle stays DISABLED however live the checkout is — and the WRITER knows it, gating on `mode === 'live' && registration === 'project'`. The CHECK gated on `mode === 'live'` alone. One rule stated twice, and the two statements disagreed on the one path that exercises the difference. **And it is ARC-08-C4's shape in full:** the FAIL's remedy is `./snowarch mode live`, the command just run, while B09 advertises `--fix` — and both write through the registration-aware writer, both reproduce the state already on disk, and the FAIL returns. A user is handed a failure, a remedy that no-ops, and a fix that reports success while nothing changes. `quick: true`, so it fires on every quick run, not only the full doctor | **Fixed by deleting the second statement of the rule, not by patching it.** `projectEntryEnabled({ mode, registration })` is exported from `settings-local.mjs` beside the writer that always had it right; `computeSettings` now reads it and so does `toggleProblems`, which gained the `registration` argument E-10 always had available in `state`. **The ok line says why** when *live* and *disabled* appear together — `live · disabled (local registration carries the server)` — because that pair reads as a contradiction to somebody who has just been told the mode is live. **A case that had no sentence now has one:** live + local + an ENABLED project entry IS the double load the ordering exists to prevent, and it is reported as *"the local entry and the project entry would both load"* rather than passing silently, which is what the old rule did. **`--fix` was never destructive here, and that was checked rather than assumed:** F6 passes `registration: ctx.registration ?? 'project'` and the doctor fills it from state, so the writer would have left the toggles exactly as they were — the danger was never a bad write, it was a failure nobody could clear. **Controls, and the split is deliberate:** reverting the registration argument fails exactly the three tests that assert the new behaviour — the local path, the double-load sentence, and the agreement property — while the test asserting the UNCHANGED project and design paths stays green, which is how the fix is shown not to have bought one path by breaking another. **The agreement property is the part that was missing:** a test that runs the check and the writer over every (mode, registration) pair and requires the check to find nothing wrong with the writer's own output. Two copies of a rule cannot be kept in step by review, and that test fails the moment either side is re-stated |
| ARC-08-C16 | **The Mode line called a live, connected checkout design-only — in a report that said the opposite three times.** The owner's Sitting C on rc.5, one `./snowarch doctor` after `mode live --register local`: `E-11 ok .local/ state: mode live`, `E-27 ok Claude Code registration status: ✔ Connected · scope local (local)`, `SV-05 ok stdio handshake: 397 tools advertised, matching the contract` — and then the last line, `Mode: design-only — server disabled in .claude/settings.local.json although instance "pdi" is configured; run ./snowarch mode live`. `/mcp` in a real session showed `servicenow · ✔ connected · 397 tools` through the local entry. **This is the line the SessionStart banner and `/snowarch status` quote**, so the one sentence a user sees most often was the only one that was wrong. `deriveMode` computed `serverEnabled = toggles.enabled !== false` — `.claude/settings.local.json` governs `.mcp.json` and nothing else, and a `local` or `user` registration carries the server in `~/.claude.json`, where the project toggle is deliberately OFF because otherwise both entries load. ARC-08-C15's defect, one line lower, in the third reader of the same rule | **Fixed by giving the derivation the fact it was missing, not by letting a recorded mode win.** `projectEntryCarries(registration)` is split out of `projectEntryEnabled` in `settings-local.mjs` — `projectEntryEnabled` is now defined over it — and `deriveMode` asks *is the project entry the carrier?* before the toggle file gets a vote: `serverEnabled = projectEntryCarries(registration) ? toggles.enabled !== false : true`. **The "recorded mode wins over the inference" rule was checked and does NOT apply:** it governs the **docs** mode (`docsStatus`, sparse/full/skip), a different subsystem; this module is *"derived, never remembered"* by design, and reading `state.mode` here would reinstate the stale-state defect it exists to avoid. **Nor does the fix break this module's promise never to read `~/.claude.json`:** the registration comes from `bootstrap-state.json`, our own record of what we registered, which is the same file the rest of the block already reads. **Found by running the command, not by reading it:** the first version passed `state?.registration` at a call site where `state` is declared eighty lines later in a different function — `node --check` was clean and `./snowarch doctor` died with `ReferenceError: state is not defined`. **Controls:** reverting the registration argument fails exactly the three tests asserting new behaviour — the owner's state, the empty-store case, and the agreement property — while the test asserting the UNCHANGED project and design verdicts stays green. **The agreement property now covers three readers**, and its first version was WRONG in an instructive way: it asserted no (toggle, registration) pair produces a problem, and failed, because a `local` registration with the project entry ENABLED is the double load and E-10 is supposed to fault it. Restated to the property that matters — for each registration, the toggle state the WRITER produces for live is the state the Mode line calls live and E-10 calls correct |
| ARC-08-C17 | **RELEASE BLOCKER — the hook told a working checkout it was design-only, and the rule file then forbade every tool call.** Sitting C, T-22, rc.5: fresh `claude` in the checkout, store present, local registration, server connected with 397 tools. Prompt: *"Create a Script Include named X_TEST_Probe on the pdi instance now."* The engine answered *"Mode is design-only — the session hook reports no ServiceNow instance is configured. Per `.claude/rules/00-mode-and-mcp-gate.md`, in design-only mode I never call an MCP tool, so I can't reach a "pdi instance" right now regardless of the request"* — and offered to set up the PDI that was already set up. **The model was obeying the rule correctly; the rule was reading a line that was false.** Net effect: a user with a configured, connected instance could not get one tool called, which is the whole product. **Not ARC-08-C16's defect** — that fixed the toggle's vote; this is the other input. `loaded` is the SERVER's answer and a quick run never spawns it, so `instances` arrived empty and `deriveMode` read *empty* as *none work* when it meant *nobody asked*. Absence reported as a verdict, in the one line a session reads first. In T-19 the model overrode the same false line after calling `capabilities_read`; here it obeyed. Both are this row, and this one is total | **Fixed by telling the derivation that the question was not asked.** `deriveMode` gained `probed`, defaulting to **true** so no existing caller changes behaviour, and the quick run passes `probed: false` with the store's own entries — `readStoreSummaries`, SV-03's cheap half: no probe, no network, no spawned server, three non-secret fields per entry named one at a time. On that path a configured instance and a reachable server is `live`, with variant `liveUnprobed` so nothing claims a probe that did not happen: `configured` is what the store knows, `loaded` is what the server answered, and only the second is evidence that it works. **Both directions held:** an empty store on an unprobed run is still not live, and a PROBED run keeps every verdict it had — an entry the server refused is still `noInstanceLoaded`. **`SNOW_STORE` first**, because that is the precedence the server itself uses and a doctor reading a different store would be answering a different question. **Controls, and the split is the point:** the four unit tests drive `deriveMode` directly and prove the READER; reverting the store read leaves all four green, which is exactly the shape ARC-06-C15 was caught in. A fifth test runs the real `runDoctor` on a fixture checkout with a real 0600 store and reads the Mode line off the report — reverting the wiring fails **that test and only that test**. It also asserts the line carries no URL, username or credential, because this line is printed by a hook into every session |
| ARC-08-C18 | **`/snowarch status` was a renderer written in prose, re-executed by a model once per session.** `.claude/skills/snowarch/SKILL.md` § status ran `doctor --quick --json` and then told the model how to render it: seven lines each with the JSON key to read in brackets, a rule for omitting a line whose key is null, a branch for failing checks, a branch for exit 3, and a fallback naming one of three causes — 45 lines of rendering instructions with no test, no two runs guaranteed alike, and a terminal user with no way to see the same output at all. Three copies of the sample block existed (`docs/snippets/status-template.md`, the skill, VALIDATION-TESTS T-07), byte-pinned **to each other** — so a line could be wrong in all three and every test would pass. Four of its values were: the contract sha showed 7 characters where the rest of the product prints 12, the `Instances:` sample carried a `default` marker that is not a key of a schema-v1 instance, the `Doctor:` stamp had no timezone, and `Instances:` itself has been unreachable on the mandated `--quick` run since ARC-09-C8 took the server section out of it. On rc.5 the model, executing these instructions by hand, wrote the doctor JSON to `/tmp` twice to read it back | **Fixed — `./snowarch status` is a command, and the skill's step is "run it and print its output verbatim".** `renderPanel(report) -> string` is pure and structurally so: no `node:fs`, no `process.env`, no clock, asserted by walking its import graph — **and the walker is tested on a module known to be impure first**, because a purity check that cannot see is indistinguishable from a module that is pure. That walk caught `report-text.mjs` (its `useColour` reads `process.env`) on the way in, so the pure line renderers moved DOWN into `panel.mjs` and `report-text.mjs` imports them, rather than the test being narrowed to fit. `capabilitiesLine` and ARC-09-C46's `nonOkLines`/`failureLines` now have one home and four readers — the full report, B09's install summary, the upgrade block and the panel. The command renders IN-PROCESS (a spawn would spend a Node start-up inside the banner budget C8 fought for) and `--json` emits the doctor report **unchanged**, not a second schema for one set of facts. `resolveCheckout` is the doctor's own four-step preamble, extracted and shared, with `who` putting the caller's name on the line — `STATUS:`, because a reader sent looking for a doctor run that never happened is a reader sent wrong. **"Verbatim" is now a promise that can be kept:** `ranAt` comes from the report and prints in UTC, and the determinism test renders one fixed report under a **different TZ, LANG and cwd** and asserts one unmasked string — deliberately not "render it twice", which a pure function passes by construction while missing the ambient read that will actually happen. Both shas print 12. FAILs are listed with their remedies and warnings are not, which is what the skill already said in words; on a quick run 25 of 39 checks are `skip`, so "every non-ok check" would have been 27 lines. `BANNER.fromState` took the cause as an argument and **lost its default** — it ended *"until Node 20+ is installed"* unconditionally, which is the one cause this command can never report, since a machine without Node cannot run it to find out; that is also the only fallback the skill keeps. The template block is no longer a fourth copy: it IS `renderPanel()`'s output for the live fixture, asserted against the renderer, so the three doc copies are pinned to the product instead of to each other. **The frontmatter grant was the quiet one** — a skill told to run a command its `allowed-tools` does not list falls back on every invocation, silently, looking exactly like a broken install; `status` in both spellings is in the list and the exact-list test went from eight entries to ten. **Controls:** local-time rendering fails the determinism test, the bytes test AND the purity walk (three instruments, three angles); `nonOkLines` in place of `failureLines` fails only the warns test; restoring the JSON instruction to the skill fails only the skill test. **Raised, not decided here:** `Instances:` is unreachable on a quick run, and `tests/fixtures/doctor/status-live.json` predates C8 — it carries a server block a quick run can no longer produce, so `status-template.test.mjs`'s "a report the doctor really produced" is historical rather than current |
| ARC-08-C19 | **Two of the panel's seven lines were specified by the skill and unproducible by the command the skill mandates.** `Instances:` read `report.server`, which is null on every `--quick` run since ARC-09-C8 took the server section out of the subset; `Docs:` read `engine.docs`, which only E-12 fills and E-12 left the subset in the same change. So the line carrying the **release family a grounding decision turns on** was absent from every run a session makes. **The instances were ALREADY BEING READ AND DROPPED:** `runDoctor`'s `fromStore = probed ? [] : storeSummaries(root, config)` runs on every unprobed run, is handed to `deriveMode`, and goes nowhere else — ARC-07-C5's shape a third time, after ARC-09-C46 | **Fixed — every line the skill specifies is producible from what a quick run can read.** `instances` is a new TOP-LEVEL report key, `{ source: 'store' \| 'server', entries }`, null when neither ran; `server` keeps meaning "the server answered", because folding a file read under that key would report it as a handshake. `SCHEMA_KEYS` is the one definition and both key-set tests compare against it rather than a literal, so they followed the addition without an edit — the ARC-09-C47 shape, built right. `configuredDocs()` is the corpus a quick run may pay for: `pin` and `family` from `engine.config.json` (already parsed), `mode` from `.local/bootstrap-state.json` (already read), `present` from one `existsSync`, **and one bounded, offline `git rev-parse` in the submodule** — owner-approved, because a `Docs:` line naming a pin the corpus has drifted off would be asserting what it did not measure, and the line now says `corpus is on <sha>, NOT the pin — ./snowarch docs sync` when they disagree. Agreement is silent: a line announcing it on every healthy run is a line readers learn to skip. What the call does NOT buy is the corpus BRANCH (`branchOf` is another of C8's spawns), so the closing sentence names it — `Capability packs, citation counts and the corpus branch are not probed on a quick run`, built from the keys that are actually null rather than a remembered list — and a store-sourced list adds `Instances are the store's own records; nothing was probed.` **Three defects found while building it, each by a test that was not looking for it.** (1) `engineBlock` took `pin` from E-13 and `family` from E-14 and never from the docs object, so the fallback's fields were computed and dropped by the mapping — the row's own defect one layer down. (2) `familyMatches` was **never mapped into `engine.docs` at all**: `undefined` for every consumer since the block was written, so the sentence could not have named the branch — a key read by a renderer and written by nobody, caught by the wiring test and not by the renderer tests, which is the reader-vs-writer split again. (3) The first version copied the SERVER's entries into the new key wholesale, and those carry a `username`; a masked account name reached the **doctor cache**, which an issue template asks a stranger to paste. `fix.test.mjs`'s redaction test caught it within the hour. The block is narrowed to what the panel renders — label, environment, preset — because a report that travels must not carry a field nothing reads, and the property is now pinned directly instead of left to the redactor. **The two committed fixtures are knowingly stale and were NOT edited:** `status-live.json` carries a `server` block from a `--quick` run, which no current quick run can produce, and both predate `instances`. Forging a key into a file whose test says "a report the doctor really produced" would make that sentence false, so the age is excluded by name and a new test validates a report the CURRENT product produces, key for key — the stronger form of what the fixtures stood in for, and one that cannot go stale. **Controls:** `instances: null` fails the three instance tests; no docs fallback fails the docs test; `familyMatches` unmapped fails the branch test — and notably only the wiring test, not the panel test, which hands the renderer its data. **Budget:** the one git call costs ~30–50 ms locally (macOS/APFS, five runs each: 83 ms median without, 133 ms with). The Windows CI cell is the measurement that decides it, quoted in the PR; if it puts C8's budget at risk the call is dropped and this row says so |
| ARC-08-C20 | **The status fixtures were a guarantee about a product that no longer existed, and nothing could tell.** `status-template.test.mjs` says of them *"written by `--quick --json` on fixture checkouts, never by hand"* — one sentence, and the only thing standing behind two files committed once from a checkout nobody else has. It had stopped being true of the PRODUCT rather than of the files: `status-live.json` carries a `server` block from a `--quick` run, and ARC-09-C8 took the server section out of the quick subset, so **no quick run this product makes can produce that report**. Every test reading it was measuring a fortnight ago, and ARC-08-C19 had to excuse the `instances` key by name because the fixture predated it | **Fixed — the capture is a command, and a test re-runs it.** `scripts/make-status-fixtures.mjs` builds a checkout, writes a fixture store (one instance: `pdi` · `pdi` · `custom`, explicit flags, an RFC 6761 `.test` host that resolves nowhere, no account name), runs the real `statusCommand --json` and writes both files; `tests/doctor/status-fixture-capture.test.mjs` re-runs it and fails on any drift. **The whole class — "the fixture and the product have drifted and no test can tell" — is closed by making the fixture a reproducible output rather than a remembered one.** C19's two excuses are deleted, not reworded: no `ADDED_SINCE` filter, no age exclusion, `validateReport` asserted whole. **The first version typed its own file list from memory and captured `4 fail`** — `CLAUDE.md`, `.mcp.json` and `.claude/settings.json` missing and the state file without a schema version — which would have baked four failing checks into the block the skill quotes; it uses the tests' own `greenTree`/`linkInstall` now, because a second copy of a list makes exactly the mistake a second copy always makes. **The suite then caught three more, each a guard earning its keep:** ARC-09-C6's sync-write test (`5 buffered write(s)` — a script that exits on every path was using `process.stdout.write`, so a `--json` capture could reach its caller cut in half); `fixture-cleanup`'s TMPDIR count (`greenTree` writes an `.owner` record BESIDE the directory and a bare `rmSync(root)` left every one of them behind); and the module ran its main body **on import**, so the drift test imported it, captured, `process.exit()`ed, and reported `pass 1` — four assertions gone and the very test that would have caught the drift was the first casualty. **Only the clock and the capturing process are pinned** — `ranAt`, every `durationMs`, and `prereqs.shell`, which `guessShell` reads by walking the PARENT process (`zsh` from a terminal, `unknown` under `node --test`: a fact about who typed the command). The machine PATH is not normalised by a second normaliser — the capture passes its own root as `home`, so the shipped `maskForJson` rewrites it to `~` at the boundary that masks a real user's. Version, contract sha and docs pin are captured verbatim: they move with a release and the sample block is meant to move with them. **The block moved in six of its seven lines** (mode-line tail, sha, pin, instances, doctor tally and time, plus the new store sentence) and the three copies followed from the one file; `tests/release-workflow.test.mjs` planted its unmasked address at `server.instances[0].username`, which no longer exists, so the plant moved to a check's `detail` — a place a leak could still arrive — with the assertion unchanged. **CI found two more, and the second is a PRODUCT defect rather than a fixture one.** **(a)** The capture pinned the clock and the shell and nothing else, so the drift test held only on the machine that wrote the file: macOS node-20 and node-22 cells failed on `2.55.0` vs `2.39.5` (git) and `22.23.2` vs `24.16.0` (node, in three places) while the node-24 cells passed for the same reason. `HOST_FACTS` is a named table now — os, shell, node, git, the `ok` booleans, the clock — with the rule it encodes written beside it (*if the value would differ between two correct captures on two correct machines, it is the machine's*), and `assertNoHostValues()` **refuses to write a fixture that still carries this machine's node or git version**, naming the table to add the field to — so the next ambient field is caught on the machine that would otherwise commit it rather than on somebody else's CI run. **(b)** The committed fixture carried `toplevel: '/private~'`. `homeValues` returned the home as GIVEN and the report carried the path as RESOLVED: on macOS `os.tmpdir()` says `/var/folders/…` while the realpath of anything inside it is `/private/var/folders/…`, so the masker replaced the unresolved prefix INSIDE the resolved path and left `/private` in front of the mask. **A redactor that only worked where it was written** — the class `realpathOrSelf` already exists for in `doctor/index.mjs`. It masks both spellings now, **longest first, because the order is the bug**: mask the short one first and the long one can never match. `realpath` is a parameter so the closed path is driven rather than waited for, and a home that is not on disk is still a string worth masking. **(c) CI on three OSes found three more, and the method was the real finding.** I had been pinning field-by-field as each cell failed; the table is judged where the fixture is COMPARED, which is nine cells on three OSes. Audited all **89 leaf paths** of the report against the rule and pinned what reads the host, the checkout, the filesystem or the clock. **(c1)** The `test` job checks out WITHOUT `vendor/ServiceNowDocs` and the `bootstrap` job WITH it — two correct jobs on one commit, two byte streams — so `docs.present`/`head`/`headMatchesPin` are the checkout's and are pinned (`pin` and `family` stay verbatim: `engine.config.json`'s answer is the product's). **My first fix here was a precondition naming the missing submodule before comparing bytes, and it was the wrong half** — it would have turned five green cells into five explained failures; removed. **(c2)** Three Windows cells: `toplevel: '~/AppData/Local/Temp/snowarch-doctor-lffMzB'` vs `'~'`. On Windows the temp directory is under HOME, so E-05's path is home-relative and per-run; pinned, **reversing my own argument** that leaving it visible guarded a masker regression — that holds only where TMPDIR sits outside HOME, two platforms out of three, and the regression is held by `json-boundary`'s C1 and the TMPDIR test's explicit assertions. **Underneath it was a THIRD masker defect, and that one is the product's:** `git rev-parse --show-toplevel` answers with FORWARD slashes on Windows while the home arrives with backslashes, so the by-value mask missed and only the generic pattern fired — the username masked, the rest of the path left behind. `homeValues` yields the forward-slash spelling too, **one direction only**, because a POSIX path has no backslashes to convert and the reverse would add a string nothing can produce. **(c3)** `.local/`'s file mode is `700` on POSIX and `acl-inherited` on Windows — the filesystem's answer; pinned, with E-11's detail rebuilt the way E-02's is. The TMPDIR test runs **three** layouts now, the third under HOME, so the Windows shape is exercised on every platform. **(c4) And pinning those fields made this test's own masker assertions unfailable** — the reviewer measured it: with the masker reverted, exactly ONE test failed where three had before, because `toplevel` and `data.root` are replaced before the bytes are compared, so the `/private~` and `var/folders` checks ran on a value that could no longer carry the bug they name. *A check that cannot fail, presented as the thing holding the line* — the defect class turned inward, inside the test written to catch it, and a stale comment two lines above still claiming `toplevel` is unpinned in the same file that pins it. `capture(mode, { pin: false })` returns the report as the doctor produced it; **byte-identity and the committed-file comparison take the pinned report, the four masker assertions take the raw one**, across all three layouts. Restored control: a reverted masker fails `json-boundary` C1 **and** this test — `layout #1: the unresolved-prefix bug is back`. **(c5) And one of the four raw assertions was wrong in principle, not merely on Windows.** `/"~[\\/][^"]*(?:AppData|Temp|snowarch-doctor)/` treated a home-relative path as a masking failure; on a raw Windows capture under the default TMPDIR `toplevel` is legitimately `~/AppData/Local/Temp/snowarch-doctor-<random>` — the home WAS replaced by `~`, which is the mask working. **The masker's contract is "the home never appears in any spelling", not "no path under the home is ever shown".** It passed here only because macOS's layout-#3 path happens to contain none of those three words: **a Windows string used as a bug signature, which is the mirror of the mistake it was written to catch.** Replaced by its positive form — the raw `toplevel` starts with `~` and a separator (or is `~`), and the absolute home appears in NO spelling: as given, as resolved, and each with the separator git prints, resolved INSIDE the capture's callback because `capture` removes the checkout in its `finally`. Checked against both local layouts and against the Windows value from the CI log verbatim: all four hold, and the old assertion fires on it — which is the measurement that it was failing a working mask. **(d) A reviewer's worktree first showed (c1) as a legibility problem:** the drift test reported a six-line byte diff where the reason was a missing submodule — every line true, none of them the reason. That reading was right and the fix was not: the answer is the pin, so the bytes do not depend on the prerequisite at all. *A test that cannot tell "the prerequisite is missing" from "the thing under test is broken" is a real defect — but when two correct checkouts must produce one byte stream, naming the difference is not enough; it has to stop being a difference.* **`toplevel` is deliberately NOT pinned:** the fix makes it `~` wherever the capture runs, and a pin would keep the fixture green if the masker broke again — the value being `~` IS the assertion. The control for it is a test that captures twice under two different `TMPDIR`s, one behind a symlink, and asserts identical bytes; reverting the resolved spelling fails that test, the drift test and the masker's own unit test, three angles on one byte. **Found and NOT fixed here:** `modeLineDetailed` prints `WRITE=off CMDB_WRITE=off SCRIPTING=off ATF=off NOW_ASSIST=off FLUENT=off` for an instance whose store says five of the six are ON — it reads `effectiveFlags` from the SERVER's instance list, which is empty on every quick run since C8. The retired fixture shows the same six `off`, so this predates the row; it is the line quoted verbatim by the banner, `./snowarch mode` and the panel, and it is asserting flags it did not read |

The seven fields, measured on the live fixture before the fix:

| Path | Shape |
|---|---|
| `$.modeLine` | `instance=acme-prod (prod)` |
| `$.modeLineDetailed` | the same line, with the check counts |
| `$.server.instances[0].label` | the label itself |
| `$.checks[32].detail` | `acme-prod: …` — a check naming the instance it judged |
| `$.checks[32].data.fix.label` | the `--fix` payload's target |
| `$.checks[32].data.fixes[0].label` | the same, in the multi-fix form |
| `$.checks[35].detail` | a second check, same shape as the first |

After: the JSON passes all six of ARC-10-S07's redaction patterns, contains neither the label nor the host, and the mode line reads `Mode: live — instance=<label> (prod) preset=read-only`.

### Acceptance

The acceptance pass against `docs/plans/06-ACCEPTANCE-PLAN.md` §2. One row per backlog item: what it
claimed, what the tree said, and what changed.

| Item | Outcome | Evidence |
|---|---|---|
| B08-01 — S01 AC 6: `node --test tests/doctor/` passes on three OSes before `npm ci` | **rework + record** | Measured on a worktree with no `node_modules`: **231 cases, 222 pass, 9 fail**. The nine are `fix.test.mjs`, `json-boundary.test.mjs` and `redaction-e2e.test.mjs`, every one needing a loaded instance → the server → `@modelcontextprotocol/sdk`, which is not there (`ERR_MODULE_NOT_FOUND` from `dist/server.js`). Separately the literal command does not run on Node 24 **with or without** an install: `Cannot find module '<cwd>/tests/doctor'` — a bare directory is not a test target on that line, which is why `tests/run.mjs` computes a file list. Now: `tests/doctor/stdlib-only.test.mjs` asserts the property that is true (43 files, **0 bare specifiers**, both directions, one stated self-exemption), a step in the three `bootstrap (node-cli)` cells runs the 17-file stdlib subset **before `npm ci`** and refuses to run if `node_modules` exists (**196/196** locally on the no-install worktree), and AC 6 is amended to say all of this |
| B08-02 — S04 AC 7: a store edited after the server started makes SV-06 fail naming `maxRecords` | **rework** | `packages/snowarch/tests/doctor/sv06-store-drift.test.ts`, two cases. In process and importing from `dist/`, because SV-05/SV-06 look for `server.js` and `contract.json` beside their own module — imported from `src/` they find no contract and **skip**, which reads exactly like a pass and did on the first attempt. The cached handshake is "the server already running" (`resetHandshakeCache` is the fixture seam), so no clock is involved. Both directions: the drift fails naming `maxRecords` with `server 100 vs store 50` and a restart remedy; the same edit **after a restart** is green again. The negative control had to be run in SOURCE — removing the comparison from `dist/` proved nothing because `npm test` runs a `pretest` build that restored it |
| B08-03 — S04 AC 5: with the SDK present SV-03 is ok and prints its version | **rework** | It printed the location, not the version. The probe already resolves `@servicenow/sdk/package.json` to find the SDK at all, so the version was one guarded read away: SV-03 prints **both** now — the version is what a support conversation asks for, the location answers which of two installs is in use. Never throws: a versionless or unreadable `package.json` still reports the SDK as present, asserted as its own case. AC 5 amended to record that both are printed |

**Also resolved in this pass, for the plan's two open flags.** *Sitting B is not empty* —
`OWNER-SITTING.md:150` defines it and it carries `## B0.`–`## B5.` (lines 771–868), the ARC-00
`S-14a`–`S-14d` questions; B1–B4 are ANSWERED, **B5 is outstanding**. *The `§ D1`–`D5` references are
placed correctly* and are not the Windows sitting: `## Sitting D — Windows` is line 502, while the
six per-story doctor sittings sit at lines 89, 193, 227, 373, 409 and 430. The naming collision is
the real finding and is recorded in the plan.
