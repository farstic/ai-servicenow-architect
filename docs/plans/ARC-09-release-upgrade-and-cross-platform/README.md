# ARC-09 — Release, versioning, upgrade and cross-platform proof

> **Amendment 2026-09-07 (from ARC-01-S02's R-4 scan).** **Any `gitleaks` step in CI must exclude
> `vendor/ServiceNowDocs`, or it will fail permanently from the moment the corpus lands.** Measured on
> the engine's checkout of the corpus: **592 rule hits**, of which **`curl-auth-user` ×526** — the REST
> API reference pages are full of `curl -u user:password` examples, plus `generic-api-key` ×44,
> `curl-auth-header` ×8, `private-key` ×8, `jwt` ×4 and two single hits. **None is a real credential**;
> they are documentation samples published by ServiceNow. For contrast the same scan over the engine's
> own history (59 commits) and over the ARC-01-S02 import artefact (57 commits) each returned **no leaks
> found**, so the corpus is the entire source of noise. Add `vendor/ServiceNowDocs` to `.gitleaksignore`
> or to the workflow's path filter, and say in the workflow why — a future maintainer who deletes the
> exclusion will get 592 failures and no explanation.


Status: **In progress — S01–S10 merged (10 of 11), started 2026-09-11** · **acceptance closed 2026-09-13 (ARC-09 acceptance PR)** — B09-07 answered by experiment (the release DOES run under pwsh on Windows; the cell had been proving the opposite), B09-05 recorded not-applicable, B09-06 closed as found · Depends on: ARC-01 (version of record, CI skeleton), ARC-02 (`CLAUDE.md` marker line; `/snowarch status` renders the doctor JSON whose `engine` header S04 feeds), ARC-03 (docs pin, `docs sync` recipe, `docs verify`, `docs-real.yml`), ARC-04 (store module, `build-dist.mjs`, `contract.json`, server doctor module, package identity), ARC-05 (contract gate, error registry), ARC-06 (first `version`, bootstrap state/resume, launchers, `bootstrap` CI job, handshake module), ARC-07 (`--password-stdin`, non-interactive `instance add` with `--no-probes`), ARC-08 (doctor registry/JSON, `doctor` CI job, banner nudge slot); ARC-00 S-03/S-04/S-07/S-08 verdicts and story S13 · Blocks: ARC-10 (the 2.0.0 tag)

## Goal

Ship one versioned artefact — a git tag whose message records the contract sha and the docs pin — produced by a release script that rebuilds `dist/`, runs every gate and generates the changelog; give users `./snowarch upgrade` that re-runs only the bootstrap steps whose inputs changed; and prove the design-only and live paths on macOS, Linux and native Windows on every commit.

## Why it exists

Closes P-12 (five counters, zero tags), P-19 (server version drift), P-29 (CI never ran tests; release never fired), P-37 (pre-commit gates inert per clone — replaced by CI), P-40 (Windows native path never proven), and the "monthly bump ritual never executed" half of P-11 (automated in ARC-03; verified here on the release path).

## Scope

**In:** `scripts/release.mjs`; tag format and message; `docs/CHANGELOG.md` generation (conventional commits); `CLAUDE.md` version line and README badge written by the script; `snowarch version|upgrade`; input-hash invalidation rules; store schema migration framework (explicit, versioned, backed up); `.gitattributes`; CI matrix `ubuntu-latest` / `macos-latest` / `windows-latest` × Node 20/22/24 with jobs: lint, server tests, contract, dist rebuild-diff, design-only bootstrap, doctor, citations; Windows job without Git Bash; release workflow on tag; optional secondary npm publish job (`--provenance`, disabled by default — roadmap `01` §17).
**Out:** plugin/marketplace packaging (roadmap), Docker (removed by D-03).

## Deliverables

- `scripts/release.mjs <x.y.z>`: bump root and package versions; rebuild `dist/`; run `npm test`, contract, engine lint, citations; write `CLAUDE.md` version line, README badge, `docs/CHANGELOG.md`; commit; tag `v<x.y.z>` with message `contract: <sha>\ndocs-pin: <sha>\nclaude-floor: <ver>`; refuse on any failing gate or dirty tree.
- `.github/workflows/release.yml`: on `v*` tags, re-run all gates on three OSes and attach the doctor JSON and the install-page size/time table as release assets.
- `./snowarch version` (version, tag, contract sha, docs pin, floors); `./snowarch upgrade [--to vX.Y.Z]` (`git fetch --tags`, checkout or `pull --ff-only`, a second `bootstrap` run, doctor); the SessionStart banner's "behind origin" nudge (reads a cached fetch, never fetches itself).
- Input-hash table (documented in `docs/ARCHITECTURE.md`): `package-lock.json` → B04; `dist/contract.json` sha → B05/B08; `vendor/docs-areas.txt` + gitlink → B02; store schema version → B06 migration; `.mcp.json` / `.claude/settings.json` hashes → B01/B07.
- Store migration framework in `packages/snowarch` (`version: 1` → n; backup file `instances.json.bak-<ts>` with 0600; never touches credentials' values).
- CI matrix with the Windows job proving `bootstrap.cmd`, `snowarch.cmd`, masked input (`--password-stdin` path in CI), the MCP handshake, and the SessionStart hook in exec form.
- `.gitattributes` (LF for `*.mjs *.md *.json *.sh`; CRLF for `*.ps1 *.cmd`); `docs/CONTRIBUTING.md` release section.

## Dependencies

ARC-01, ARC-02, ARC-03, ARC-04, ARC-05, ARC-06, ARC-07, ARC-08 (per-story detail in `STORIES.md`). ARC-00 S-03/S-04/S-07/S-08 verdicts (Q-B) and the story ARC-00-S13 PATH-stripping recipe (`spikes/windows-recipe.md`, **pending Sitting D — the file does not exist**: a recipe nobody has measured is a recipe nobody should follow, so it is written when the sitting produces it — not spike S-13, which is the skill listing deferred to ARC-02) for the Windows job design; ARC-00 S-19 for `plugin-validate`.

**Q-B: pending.** S-03, S-04 and S-08 are NOT RUN — blocked on the Windows VM (owner input #2) — so native Windows is **proven in CI, not by a person**, and the release note says exactly that until those three run (OWNER-SITTING Sitting D).

## Acceptance criteria

- [x] `node scripts/release.mjs 2.0.0` on a green tree produces one commit and one tag; `git show v2.0.0` displays the contract sha and docs pin; `./snowarch version` prints the same values; `tests/version-consistency.test.mjs` passes. (S01, S04) **(S01/S04; proven end-to-end on the S07 harness in S11's walkthrough — `release 9.2.0 → 9.3.0`, the plan printed, gates `dist → lint → test → contract → docs`. The real `v2.0.0` cut is the architect's after the M4 merge — OWNER-SITTING is not involved; it is a maintainer action, and its numbers go in the roadmap record PR.)**
- [x] The same command on a tree with a stale `dist/` or a failing lint exits non-zero without committing or tagging. (S01) **(S11's walkthrough hit four refusals in a row, each naming its own remedy and each leaving the tree untouched: missing corpus → `working tree not clean`; stale `dist/` → *run `node scripts/build-dist.mjs` and commit it in a normal PR*; pin behind the contract → *run `node packages/contract/pin.mjs`*; stale `vendor/docs-areas.txt` → *run `node scripts/gen-docs-areas.mjs --write`*. `release: gate failed: lint (exit 2) — nothing was written`.)**
- [x] A user on `v2.0.0` runs `./snowarch upgrade --to v2.1.0` (fixture release changing only `vendor/docs-areas.txt`; the S07 harness uses `v9.0.0 → v9.1.0`): only B02 re-runs, credentials untouched (`sha256 .local/instances.json` unchanged), doctor 0 FAIL. (S05, S07) **(S07, 2026-09-11, harness AC 1)**
- [x] A fixture release changing the store schema migrates the store, leaves a 0600 backup, and the server loads it. (S06, S07) **(S07, 2026-09-11, harness AC 2)**
- [x] CI is green on all nine matrix cells; the Windows cell runs with Git Bash removed from PATH and passes the design-only bootstrap, the doctor and the MCP handshake. (S08) **(S08: 52 required contexts at the time, 54 after S09. `bootstrap (no-gitbash, windows-latest)` keeps its name and its context; `windows-native` adds three cells driven entirely through `.cmd` with Git Bash stripped from `PATH`, covering the wizard's `--password-stdin` door, the server handshake, the hook, `version`, a store migration plan and an upgrade check.)**
- [x] `git ls-files --eol` shows the expected line endings; a Windows checkout of `bootstrap.ps1` runs without a line-ending error. (S09) **(From the `eol (windows-latest)` cell on a `core.autocrlf=true` clone: the three launchers `i/lf w/crlf attr/text eol=crlf`, `bootstrap.sh`/`snowarch`/`.mcp.json` `i/lf w/lf`, `assert-crlf: 3 file(s) are CRLF throughout`, `assert-lf: 4 file(s) contain no CR`, and all three `--help` runs exiting 0 under `cmd.exe`.)**
- [x] `docs/CHANGELOG.md` for 2.0.0 contains the "supersedes engine v2.8.0 and snow-mcp 1.0.0" note and the migration notes from ARC-04 (R-03). (S02) **(In `## Unreleased` → `### Notes`, with the five-point migration for snow-mcp 1.0.0 users; `scripts/release.mjs` moves the block into the release section it belongs to.)**

**ARC-09 COMPLETE — 2026-09-11.** Eleven stories (S01–S11) and twenty-one chores (C1–C21 — C11 onwards landed after the arc closed, most of them found by a release rehearsal), every chore a
defect found while building a story and fixed in the same arc. Three things are deliberately NOT
ticked here because a person has to do them, and each names where it waits:

- **The real `v2.0.0` cut** — a maintainer action on `main` after the acceptance sittings. **The
  rehearsal Release is done (2026-09-12):** run 10 of the `v2.0.0-rc.0` rehearsal was green end to
  end on three OSes, seven assets, both negatives refused, everything deleted afterwards — the record
  is in `docs/CONTRIBUTING.md` § *The rehearsal — a standing step before every release*. Runs 1–9
  each found one defect (C12, C12c, C16, C17, C17b, C18, C19, C20, C21), all fixed before the M4 merge.
- **The first `publish-npm` dispatch.** `docs/spikes/OWNER-SITTING.md` § Sitting E, steps E1–E5.
  The secret is deferred to the acceptance phase by the owner's decision of 2026-09-11, and the
  first dispatch is `dry_run: true` against `v2.0.0`.
- **A real upgrade on a user's machine.** The harness proves the mechanism on three OSes in CI
  (`upgrade-e2e`); what it cannot prove is a checkout someone has been living in. Sitting A's
  install rows are where that lands.

## Risks

> **Amendment 2026-09-06 (from `03` §F S-14f).** `scripts/release.mjs` checks the working tree is clean **before** testing whether the tag exists — the CLI's own plugin-tag command does it the other way round and a stale tag then masks a dirty tree until the version moves. Every product manifest carries `author` (S-19: `--strict` fails on that warning).

- Windows CI runners differ from consultant laptops (policies, AV). Mitigation: S-08 on a real locked-down VM; the install page states the tested configurations.
- Conventional-commit discipline slips. Mitigation: commitlint in CI; the release script falls back to a manually edited changelog section.

## Stories

Full write-ups (persona, context, scope, design notes, acceptance criteria, tasks, test strategy, dependencies, size, risks, definition of done) are in [`STORIES.md`](STORIES.md). The original title 4 (`version` + `upgrade` + nudge) is split into S04 and S07; title 9's package name is corrected to `@farstic/snowarch` per D-01. Sizing summary: 15–25 engineer-days (3–5 weeks for one engineer).

| ID | Title | Size |
|---|---|---|
| ARC-09-S01 | `scripts/release.mjs`: preflight, gates, version writes, release commit, annotated tag with contract sha and docs pin | L |
| ARC-09-S02 | `docs/CHANGELOG.md` generation from conventional commits; commit-message lint in CI; the 2.0.0 "supersedes" and migration notes | M |
| ARC-09-S03 | `.github/workflows/release.yml`: on `v*` tags re-run every gate on three OSes, create the GitHub Release with doctor JSON and install-metrics assets | M |
| ARC-09-S04 | `./snowarch version` extended (tag, commit, tag-message comparison; `--json` superset of ARC-06-S02's shape; feeds the doctor's `engine` header) | S |
| ARC-09-S05 | Input-hash invalidation table: `lib/inputs.mjs`, `state.staleSteps()`, `tests/input-hash.test.mjs`, `docs/ARCHITECTURE.md` section | M |
| ARC-09-S06 | Store schema migration framework in `packages/snowarch` (explicit, versioned, 0600 backup, credential values never touched) | M |
| ARC-09-S07 | `./snowarch upgrade [--to vX.Y.Z] [--check]`, the SessionStart "behind origin" nudge, and the upgrade fixture harness | L |
| ARC-09-S08 | CI matrix completion: every job on the intended cells; the Windows job without Git Bash (launchers, `--password-stdin`, MCP handshake, hook in exec form) | L |
| ARC-09-S09 | Line-ending proof: `tests/eol.test.mjs` over `git ls-files --eol`; launchers run from a CRLF-default Windows checkout | S |
| ARC-09-S10 | Optional `publish-npm.yml`: `npm publish --provenance` of `@farstic/snowarch` from a release tag, manual dispatch, dry-run by default (roadmap `01` §17 item 2) | S |
| ARC-09-S11 | `docs/CONTRIBUTING.md` release / upgrade / CI-matrix sections; `docs/INSTALL.md` "Upgrading" section; `docs/ARCHITECTURE.md` versioning section | S |
| ARC-09-S12 | Semver prerelease ordering: one comparator (`tools/snowarch/lib/semver.mjs`) for the release preflight, the upgrade's tag sort and E-28 | M | Done (2026-09-23) — found cutting rc.10: `release: 2.0.0-rc.10 is not greater than the latest tag v2.0.0-rc.9`. The prerelease compared as a string, so `"rc.10" < "rc.9"`. **Two copies, written separately and wrong identically** — `compareVersions` in the release preflight and `sortTags` in the engine. The refusal was the loud half; the quiet half was that `./snowarch upgrade --pre` takes `sortTags[0]` as its target, so a checkout on rc.9 would have been told it was already newest and never offered rc.10, and E-28 reads the same order so the doctor would have agreed — two instruments, one wrong answer, nothing to disagree with. Now §11 implemented once: numeric fields numerically, numeric below alphanumeric, more fields beating fewer, a release above its prereleases; an unparseable version throws rather than ranking, because a comparator returning 0 tells `Array.sort` they are the same version. Tests: the specification's own eight-version example asserted as every pair (transitivity, not just neighbours), rc.10/rc.11/rc.100, `release.mjs` cutting rc.10 while rc.9 is latest, and an upgrade test that a checkout on rc.9 resolves rc.10 and `--check` reports it |

### Chores

Work that is not a story: a defect found while building one, fixed in the same arc.

| ID | What | Status |
|---|---|---|
| ARC-09-C6 | The exit sweep covered two named entry points while THIRTY `.mjs` files under `scripts/`, `scripts/ci/` and `tools/snowarch/hooks/` write to stdout and call `process.exit`. The rule was worded "an entry point that prints", which invites the reading that a CI script is a different kind of thing — the pipe does not know what sort of program is on the other end. The near-miss that settles it: `scripts/ci/release-notes.mjs` writes a whole changelog section (several KB, growing) and has three exits, safe today only because the big write is its last statement | **Closed** — the sweep is a SCAN of four directories rather than a list, so a new printing script is covered the day it appears; all 30 files write through `writeSync` (checked per file: zero had a live handle, so `process.exitCode` was available everywhere and was used where control flow allowed it — `assert-clean`, `footprint`, `gen-docs-areas`). The scan's own definition of "prints" had to learn `writeSync`, or fixing the problem would have emptied the sweep's membership and it would have reported a pass over two files. A negative control proves the rule can fail; a second test proves the conversion changed the mechanism and not the output. `tools/snowarch/hooks/` enters as a GUARD — the banner hook has never called `process.exit`. **The rule forbids any REFERENCE to `process.stdout` / `process.stderr` in such a file, not only a write through one:** the first touch makes libuv set fd 1 non-blocking, and `writeSync` on a full non-blocking pipe throws `EAGAIN` — so a script that reads `isTTY` for colour and then writes a large report synchronously crashes in exactly the place the rule protects. None of the thirty does it; the sweep keeps it so, and a planted `isTTY` + `writeSync` + `exit` file is asserted to be an offence |
| ARC-09-C7 | Carried from ARC-08-S11: a `.cmd` invoked from `shell: cmd` without `call` transfers control and never returns, so ARC-06-S14's Windows steps had their `if not "%ERRORLEVEL%"=="0" exit /b 1` lines sitting after a line that never comes back — dead code in the cells that exist to catch a failed install. ARC-09-S04 met the same rule from the other side (an `exit /b 3` inside an echoed string) | **Closed** — every `cmd` step calls its `.cmd`; the two PowerShell steps deliberately do NOT (there `call` is not a command, and a regex that added it everywhere put it in both). A test pairs the two rules by shell, and a drill on the Windows cells reproduces the bug rather than describing it: without `call` the marker after it never prints and the caller exits with the callee's 7; with it, the planted failure turns the step red |
| ARC-09-C8 | `bootstrap (windows-latest, node 20)` spent 1014 ms of product time on the banner's re-run path against a 1000 ms budget, and C5's phase table said where: 969 of 987 ms was the quick doctor. `quick: true` had been a label rather than a cost | **Closed** — `quick` is a cost contract now (no spawn beyond one bounded git call, no tree walk, no network, under 50 ms on the slowest Windows cell, and no reach for a SHARED context), written into CONTRIBUTING and enforced statically with a negative control. The shared-context clause is the one that matters: `docsFor`, `serverReport` and `lintContextFor` each build something once and cache it on the ctx, so the FIRST check to touch one pays for all of them — moving E-12 out put 143 ms onto E-13, and moving E-19 out put 66 ms onto E-20, both measured, the total unchanged both times. So the GROUPS moved: E-09 and E-19 (own tree walks), E-12…E-15 (share `docsStatus`, three git spawns), E-20/E-22 (share `buildLintContext`), and the whole SV- section (one in-process run of the server's own doctor). **776 ms → 89 ms** of check time locally, slowest remaining check 20 ms, fourteen checks. The three doctor snapshots do NOT move: they record a FULL design-only run, which is unchanged. What it costs a user: `--quick` no longer reports the server section. What it buys: the banner, which is paid before anyone's first word |
| ARC-09-C9 | `writeReportCache` called the STATE FILE's `assertStorable`, whose `URLISH` clause refuses any `scheme://`. A doctor report legitimately quotes documentation — E-00's remedy names the Claude Code install page — so on any machine without Claude Code on PATH the whole cache write threw and NO cache was written at all: every session paid the banner's re-run path and `/snowarch status` read nothing, silently. Older than C8; C8 only exposed it, because the fixtures used to run `--quick`, which omits E-00 | **Closed** — the cache has its own guard (`assertCacheStorable`): a secret-shaped KEY refused as before, a VALUE refused when the redactor would rewrite it OR when it carries an instance address; documentation URLs pass and are stored verbatim. The instance clause is not redundant and the test says why: `register()` is called only from `instance-file.mjs`, only for `password`/`clientSecret`, so `redact('https://dev12345.service-now.com')` returns it UNCHANGED — measured, and a redactor-only guard would have stored it. Two layers, stated: the runner redacts every result before the writer sees it (ARC-08-S11's chokepoint), the writer is the last line and does not depend on the runner having done its job. `cacheError` is no longer silent — one stderr line `doctor: cache not written — <reason>` in BOTH modes (stdout carries the report and nothing else), and the reason in the `--json` object where a script can read it. Reproduced with an empty HOME and no `claude` on PATH: **23/26 → 26/26** in `tests/doctor/fix.test.mjs`, same environment, fix reverted and restored for the control |
| ARC-09-C10 | `CREDENTIAL_EXT` — the one description of a credential-shaped file, shared by `tests/never-commit.test.mjs` and the doctor's E-09 — listed nine extensions and NOT the `.env` family. So `ENVISH`, which exists to match `SERVICENOW_BASIC_PASSWORD=…`, could only ever see `.sh` scripts, and the one kind of file named after the thing being hunted was the one kind skipped. A committed `.env` with a real value is THE classic credential leak. Found by asking whether `packages/snowarch/.env.example` — which SHIPS in the npm tarball (ARC-09-S10) — was covered by the scan. It was not | **Closed** — the filter now takes `.env` and `.env.<anything>` (`.env.example`, `.env.local`, `.env.production`), and not `.envrc`, which is a shell script and a different question. Both controls, in a FIXTURE tree and never the real one: a tracked `.env.example` carrying an assembled real-looking value makes E-09 **fail** naming `.env.example:1` and never printing the value, and the `.env.example` this repository actually ships **passes**, because every credential variable in it is empty — which is why widening the filter is safe to ship rather than a red build on our own tree. `never-commit` gains the file through the same shared regex and now asserts it. No doctor snapshot moves (the snapshot fixtures name no `.env` — checked, not assumed) and the contract sha does not move (the doctor registry is not in the contract): `CONTRACT GATE: server ok · dist ok · generated ok · engine ok`. **What it changes for a user:** a checkout that tracks a `.env` with a real password now gets an E-09 FAIL naming the file — which is the point |
| ARC-09-C11 | `tests/upgrade/harness.mjs`'s `buildWorld` LINKS `node_modules` into every fixture, so any fixture command that installs or builds writes into the REAL checkout. ARC-09-S11's release walkthrough ran the install gate inside a fixture and `npm ci` deleted and recreated the directory THROUGH the symlink: the developer's real `node_modules` went from **215 entries to 0**, typescript included. No tracked file was touched (`package-lock.json` unmodified, `git status` showed only the story's own edits) and `npm ci --ignore-scripts` restored it to 215 with `type-check` clean; the walkthrough was then re-run with a copy-on-write clone and the real tree was identical before and after. Nothing warned, and the damage was outside the tree the command was pointed at | **Closed** — two mechanisms, because one of them assumes people read headers. `buildWorld({ modules: 'link' \| 'copy' })`, default `'link'` (the tests' fast path; 400 MB per fixture would make the harness unusable), and a fixture that will run `release.mjs`, `npm ci`/`npm install` or `build-dist.mjs` asks for `'copy'`. AND `scripts/release.mjs`'s install gate REFUSES a tree whose `node_modules` is a symlink or junction — `lstat`, never `stat`, because `stat` follows the link to a real directory, which is the whole problem — with a message naming the harness option and `--no-install`. The refusal happens BEFORE the gate runs and the test asserts nothing ran, since a guard that fires after `npm ci` has already deleted the directory is a message rather than a guard. Five controls in `tests/release-modules-guard.test.mjs`, including the real one: a world built with `'copy'`, and the checkout's own entry count asserted identical before and after. Chores stay with the story that found them, which is why this row is in a closed arc |
| ARC-09-C12 | The **v2.0.0-rc.0 rehearsal** got through the preflight, all six gates and the writes, and then stopped: `release: version-consistency failed after the writes — rolled back, nothing was committed` (exit 1; tree clean, no tag — the rollback worked). Two defects, one of them a design gap. **(a)** `tests/version-consistency.test.mjs`'s drift mutation asserted the LITERAL `root 2.0.0-dev`, so the post-write gate failed the moment the root said anything else — the real 2.0.0 cut would have failed identically, and `release-dryrun` could never have caught it because `--dry-run` stops before the writes. Two more literals of the same class: `contract.test.ts` (`CONTRACT.version`) and `handshake.test.ts` (`serverInfo.version`). **(b)** `dist/contract.json` EMBEDS the version, so a release that wrote the version and stopped left the artefact stale: the release commit would have failed its own `dist ok` gate on its own pull request, and the tag's `contract:` trailer named a contract whose `version` still said `-dev` | **Closed** — (a) the three literals are read from `package.json` now, and `tests/version-literals.test.mjs` sweeps every non-fixture test for the version of record with a negative control (a planted `expect(x).toBe('<version>')` is caught; the same string in a comment is not) and a stale-exemption check, which caught the exemption I had written for the sweep's own file out of habit. (b) the writes now run **version → `build-dist` → `pin --yes` → `gen-all` → changelog**, in that order and inside the rollback scope, because the changelog and the tag both quote the contract sha and it does not exist until the rebuild; the post-write checks are version-consistency **and the contract gate**; `STAGED` carries `packages/snowarch/dist` and the pin so the release commit contains them; and `rollback` now restores tracked files and DELETES files the rebuild created — `git checkout --` cannot restore a path git has never seen and fails outright when handed one, taking the whole rollback with it (found by C12b's own rollback test). Four tests on a fixture whose contract embeds the version, like the real one: the rebuilt artefact is what the source builds, the pin equals its sha, the tag's trailer equals it, the commit contains both, and a post-write failure leaves all three byte-identical **Rehearsal run 2 (from develop @ the C12 merge) got further and found two more, both real-tree-only — C12c.** Step 1 succeeded end to end: `release 2.0.0-dev → 2.0.0-rc.0`, every gate, the writes, the post-write checks, commit `chore(release): v2.0.0-rc.0` over 10 files. Step 2 `--tag-only` then REFUSED with `release: working tree not clean:` — three files dirty after the release commit. **(1) `STAGED` was a HAND LIST.** The writes run `gen-all`, and `gen-governance` rewrote `.claude/rules/00-mode-and-mcp-gate.md`, `docs/TROUBLESHOOTING.md` and `governance/mcp-protocols.md` — their generated headers carry the contract sha, which had just moved — but the list did not know those files, so the commit went out without them and the release left the mess `--tag-only` refused on. `STAGED` is DERIVED now: the writers' own outputs plus every `targets` entry of every generator in `scripts/lib/generators.mjs`, 25 paths, so a generator added tomorrow is staged the day it exists. And the release now FAILS if the commit leaves the tree dirty, naming the files — the check that proves the derivation on every release instead of on the next rehearsal. **(2) THE HAND-WRITTEN CHANGELOG DID NOT SURVIVE.** S02's AC 2 promises the block under `## Unreleased` appears byte-identical in the released section. `extractNotes` stopped at the first `###` heading after `### Notes`, so every hand-written `### Added`/`### Fixed` group a pull request had added was dropped: measured on the real file, the block is **1,224 lines and the old reader carried 25 of them** — every per-story note from ARC-06…ARC-09, gone, from a function whose one job is to move them verbatim. The reader is the whole section now, and nothing inside it is a boundary: not a `####`, not a `---`, not a second `### Notes`, and — found by this story's own negative control — **not a `## ` line inside a fenced code block**, which the regex boundary had been treating as the end of the section. The tests run against the REAL block, because a three-line stand-in has no second heading and cannot fail the way the repository did. with no tag. `dist/` does NOT move at `2.0.0-dev` — the rebuild is a no-op at the current version, which is what `CONTRACT GATE: … dist ok` says. **And the rollback test went red on the four Windows cells, for a reason that was the fixture's and not the rollback's:** the fixture repository carried no `.gitattributes`, so it inherited the runner's `core.autocrlf=true` and `git checkout --` restored `{\r\n "schema"…}` where the fixture had written LF. ARC-09-S07's lesson, a third time — so it is now ONE helper, `tests/helpers/gitattributes.mjs`, which COPIES the tracked file rather than retyping it (the docs fixture had hand-typed a one-line subset that omitted `packages/snowarch/dist/** text eol=lf`, which is the rule that matters here), used by all three fixture builders with a test that the helper's output equals the tracked file and that each builder calls it. Reproduced on macOS by setting `core.autocrlf=true`: without the file the restore differs and carries CRLF, with it the bytes are identical. The byte-equality assertions were left exactly as they were — they are what caught it |
| ARC-09-C13 | ARC-09-S11's AC 3 — walk the release checklist on the S07 harness — could not be done, and the reason was the fixture: `COPIED` was a CURATED subset, and two of this repository's own checks are about the tree AS A WHOLE. `gen-all --check` stops on a generator whose target or input is absent ("could not run", not "skipped"), and lint rule L05 asserts that every path a tracked file CITES exists. Chasing it one directory at a time went **79 dead citations → 46 → the next one**; the subset was the defect, not the list | **Closed** — the fixture is every tracked file, derived from `git ls-files`, minus ONE exclusion held in a named list with its reason (`vendor/ServiceNowDocs`, the corpus submodule) and an assertion that there is exactly one, so the next "just exclude X" is a conversation. Building it got FASTER — **19.3 s → ~14 s** — because copying tracked files beats recursive directory copies dragging untracked junk along. `rewriteVersion` calls the RELEASE'S OWN WRITERS now (`npm version --workspaces` plus `writeMarker`/`writeHead`) rather than editing two of three manifests by hand, so a fixture release is written by the code that writes a real one and `version-consistency` passes inside the fixture for the same reason it passes on develop. **The walkthrough is a test**: it produces a released tree with `applyWrites`, commits it, tags it through `--tag-only`, runs `verify-tag`, and then runs the seven TREE-SCANNING tests against it — the guard for the class runs 4 and 5 found. It does NOT run the full suite end to end, and the cleanest statement of why is `engine.config.json validates against its schema`: the schema requires `docs.upstream` to match `^https://…\.git$` and the fixture points at a local bare repository because it must work with no network — both right, neither reconcilable. Measured full-suite cost if anyone proposes it again: 1154 tests, ~2 minutes, **24 failures of which 23 are properties of a real checkout**. Price of the guard as built: ~14 s to build a world, ~42 s for the walkthrough, in `upgrade-e2e` on three OSes — a job that already pays for a harness world — and not in `npm test`, per ARC-09-S07's exclusion |
| ARC-09-C14 | **Rehearsal run 3** stopped at step 2 with `release: HEAD is behind origin/rehearsal/v2.0.0-rc.0 — git pull --ff-only first`, on a branch that had never been pushed. `merge-base --is-ancestor origin/<branch> HEAD` fails in TWO different situations — the branch is behind, or the ref does not exist — and the preflight read both as "behind", offering a remedy (`git pull --ff-only`) that cannot work on a ref that is not there. In the supported flow `--tag-only` runs on `main`, which always has an upstream, so this could not bite a real release; it bit the rehearsal, which is a local branch by design | **Closed** — three states, not two. When `origin/<branch>` does not exist the message says so and names BOTH ways out, because which is right depends on what the maintainer is doing: `git push -u origin <branch>`, or `--offline` if the remote is not part of this release. One test per state, and the behind case is genuinely behind — a second clone commits and pushes so the fixture is behind rather than merely told that it is. Writing those tests reproduced ARC-09-C13's lesson from the other side: the first version put the bare origin INSIDE the fixture's working tree, where it is an untracked directory, so the preflight refused for "working tree not clean" before reaching the remote check at all — where a fixture puts its scaffolding is part of what it tests. **And it failed on all twelve `test` and `release-dryrun` cells while passing locally**: `git init --bare` points HEAD at `init.defaultBranch` — `master` on a default installation — so a bare repository whose only branch is `main` has a HEAD naming a branch that does not exist, and cloning it leaves the clone on an unborn `master`. The push then fails with `src refspec main does not match any`. This machine's git resolved it; the runners' did not. Reproduced here by setting `init.defaultBranch master` in a throwaway HOME — `warning: remote HEAD refers to nonexistent ref`, clone on `master`, the runner's exact error — and fixed with `git init --bare -b main`. ARC-08-S05's lesson, a third time: a fixture must not depend on the machine it runs on |
| ARC-09-C15 | `docs/CHANGELOG.md` still opened with the IMPORTED engine's title block: "All notable changes to the Claude ServiceNow Architecture Engine", a repository link to `farstic/claude-servicenow-live`, a `**Last updated:** 3 June 2026` line nothing updates, and a minor-version cadence paragraph describing a scheme this project does not use. ARC-09-S02's layout says the file starts with its title and the one rule — and this file is what a Release body is cut from, so those lines are the first thing a reader of a Release meets | **Closed** — the top is the title and one sentence (everything but the `### Notes` block under `## Unreleased` is generated; edit the Notes, nothing else). The imported header is MOVED, byte for byte, into its own `## The imported engine's header` section immediately above the frozen region — not deleted, because the provenance is the point, and not INSIDE the frozen region, which was my first attempt: AC 7 pins that region to a git object (`bcd4dcd`) and refused it, which is the guard working exactly as intended. Two tests: the first lines are the title and the rule, and the imported header sits below the live sections and above the frozen one with its lines intact. The title-block assertion is scoped to the title block rather than the whole live half — ARC-04's migration note legitimately says "supersedes engine v2.8.0 (farstic/claude-servicenow-live)", which is the sentence R-03 requires; the defect was a header claiming to BE that project's changelog, not a sentence describing what it superseded |
| ARC-09-C22 | **Rehearsal run 10** published `v2.0.0-rc.0` with `prerelease=false`, so a rehearsal tag sat in the Releases list marked **Latest** — which is the one thing a reader of a Releases page trusts. `gh release create` defaults to a full release and the workflow never said otherwise | **Closed** — `isPrerelease` in `scripts/lib/release/tag.mjs` (semver §9: a hyphen and a dot-separated identifier after the patch number, with the `v` prefix optional because every caller has one), and the workflow ASKS THE MODULE rather than writing a second regex in YAML — ARC-09-C18's lesson applied before it cost anything. Tests cover both directions and the near-misses: `v2.0.0-` and `v2.0.0rc0` are NOT prereleases, because a looser rule would mark a real release as a prerelease, which is the worse mistake — it hides a shipped version from everyone reading the page |
| ARC-09-C17 | **Rehearsal run 4** (release.yml run 34645878636, tag `v2.0.0-rc.0`): the C16 fix worked — the re-fetch and the tag check were green on all three OSes, `npm ci` and `npm run lint` passed — and then `npm test` FAILED on the released tree, 1133 pass / **5 fail**, on every `verify` job. The class: tests that encode *this is a development tree* and therefore go red on the release commit, i.e. on the very pull request (`main ← release/vX.Y.Z`) that has to merge. Three of the five were mine, added by C12a/C12c | **Closed** — each now asks a question that is true of BOTH trees, never a skip. (1) The C12c round trip branches: on a released tree the Unreleased block IS the empty skeleton by design, so it asserts the skeleton bytes and that the newest release section carries the Notes and a hand-written group; on a development tree, the round trip as before. (2) `docs/CHANGELOG.md`'s HISTORY now starts at the newest `## <version>` heading, not at the frozen `## Before 2.0.0` — a release moves the hand-written block into a section ABOVE the frozen one, and a sentence that was history yesterday does not become a live reference because a release happened. One `historyStartsAt`/`isHistory` helper, shared by both sweeps that scan the file. (3) The version sweep flagged ITS OWN failure message, which named the rehearsal version in prose; the message is version-free and the sweep skips its own file by name. (4) The fixture allow-list records the LITERAL each fixture carries rather than being keyed to "the current version", which called every fixture stale the moment a release moved the manifests. (5) `version-tag`'s AC 3 asserted `info.tag === null` — a property a release CHANGES — and now branches, asserting the released shape (`exact`, distance 0, the message's contract/docs-pin/floors) on a tagged tree, which is the shape it should be most interested in. A negative control per fix, including two that exercise branches this untagged, unreleased checkout would otherwise never run |
| ARC-09-C17b | **Rehearsal run 5** (release.yml run 34650046564, tag `v2.0.0-rc.0` on ce584eb): the tag step, `npm ci` and lint green; `npm test` on the released tree 1140 pass / **2 fail**, down from five, and both were C17's OWN — the same class, introduced by the fix for it. (1) `version-literals` flagged `tests/validation-tests-shape.test.mjs`'s *where the changelog stops being live* comment: C17's new history-boundary fixture was built with `## 2.0.0-rc.0`, the REHEARSAL's version, which is the current version on a rehearsal tree — the sweep was right. (2) `version-tag`'s AC 3: `the layout is not six lines: 7 !== 6`. A TAGGED checkout prints SEVEN — the seventh is S04's comparison of the tag's message with the tree, which exists only when a tag is exact — and C17's fix branched on tagged/untagged but left the six-line assertion ABOVE the branch, so the branch was unreachable in the one case it was written for | **Closed** — (1) a fixture version is now one no release will ever carry (`9.9.9`, or the harness's `v9.x`), with the rule in the sweep's header and a negative control in the shape that slipped through: a `##` heading inside a fixture array, not an `expect(...)`. (2) the count assertion moved inside the branches — 6 untagged, 7 tagged with the seventh matched against `tag says:   contract …… · docs-pin …` — and the tagged fixture control asserts it too, since this checkout would otherwise never run that path. **And the same 'six' was written in four other places**: `docs/CONTRIBUTING.md`'s command comment, `version-info.mjs`'s own header, and TWO `ci.yml` steps whose node one-liners assert `l.length!==6` — all four now say six, seven when tagged. Those steps run on an untagged checkout today, so none had failed; they would have on the first tagged run |
| ARC-09-C18 | `tools/snowarch/tests/input-hash.test.mjs` AC 3 went red once in a local full-suite run and passed on a re-run and in isolation (27/27), which made it look like contention. It is not: **rehearsal run 6 stopped the release before the writes** — `release: gate failed: test (exit 1) — nothing was written` — on this test, and the gate's log named the cause. The assertion compares step lines after normalising durations, and the normaliser was a SECOND regex written in the test, `/\(\d+\.\d+ s\)/`, which knows only the sub-second form. `humanDuration` prints `(0.4 s)` under 950 ms and `(1 s)` at or above it, so a B00 that took a full second — a loaded machine, or the release gate running the whole suite — produced `+ '[B00/09] preflight … warn (1 s)'` against `- '… warn (time)'`. Deterministic given a slow step; invisible in isolation, where nothing is slow | **Closed** — the duration grammar lives next to the renderer that produces it (`DURATION_IN_LINE` / `withoutDuration` in `lib/steps/format.mjs`) and the test IMPORTS it. The control feeds every shape `humanDuration` can produce — generated from the formatter across thirteen millisecond values rather than listed by hand, since a hand list would be a third description of the same grammar — and asserts each becomes `(time)`; it also asserts that `(cached)` and a skip's `(reason)` are NOT normalised, because those are parenthesised STATUS and erasing them would hide the difference between a step that ran and one that did not, which is what this file exists to assert. Verified against run 6's exact line: the old normaliser leaves `(1 s)` untouched, the new one yields `(time)`. Third test-side clock of the arc after C3 and C4, so CONTRIBUTING's test section now carries the rule: a test never re-implements a renderer's format, it imports it |
| ARC-09-C19 | **Rehearsal run 7** (release.yml run 34653741578, tag `v2.0.0-rc.0` on 05c6ad9) got further than any run before it: tag step, `npm ci`, lint, **`npm test` 1144 pass / 0 fail on the released tree** (C17/C17b/C18 proven on a real tag), the dist check, the design-only install and the citation check all green. Then `The doctor, on the install just made` FAILED — `doctor --json --no-network --no-cache` exits 1 under `bash -e`, and the step had no judgement. The report was CORRECT: `{"ok":31,"warn":1,"fail":1,"skip":6}`, the one FAIL being `E-00 Claude Code not found on PATH` on a hosted runner. The bootstrap cells have judged exactly this with `assert-doctor.mjs --expect-fail E-00` since ARC-08; `release.yml` never got it | **Closed** — the step CAPTURES (set +e, keep the code, only 2-and-above fatal: usage, or the doctor could not run at all) and a separate step JUDGES with the same script and the same flag the bootstrap cells use — one implementation of "which failures does this environment explain", not a second opinion in YAML. A test asserts the order: written, then judged, then uploaded, because a judgement after the upload is decoration. `assert-assets.mjs` had the mirror-image defect — it refused ANY fail, which would have blocked every release for ever on machines nobody ships from — and now refuses every fail except E-00, with both directions tested (E-00 alone published; E-00 + E-12 refused, naming E-12 and saying E-00 was expected; E-12 alone refused). The Release notes carry the sentence a reader needs beside the numbers. **Nothing fakes a Claude Code on the runner** and there is no `--quick` shortcut: the asset is the full doctor, published where anyone can read it, and a convenient lie in it would be worse than an explained FAIL |
| ARC-09-C20 | **Rehearsal run 8** (release.yml run 34655763909, from develop @ 50dafff): C19's capture worked — `doctor exit: 1`, the file written — and the next step, the judgement, REFUSED: `assert-doctor: this is not a green design-only install / a server check ran before npm ci: SV-00, SV-01, SV-02, SV-05, SV-07, SV-08`. The report was green: `{"ok":31,"warn":1,"fail":1,"skip":6}`, E-00 the only FAIL, SV-02 warn (no store), server `state: ready, mode: unconfigured`. `assert-doctor.mjs` rule 3 encodes the BOOTSTRAP cells' shape — the doctor runs before `npm ci` there, so every SV check must be skip — and `release.yml` installs first for its lint and test gates, so its SV checks legitimately ran. The script had one caller for two arcs and encoded that as the only world | **Closed** — the expectation is explicit: `--deps absent\|installed`, **default `absent`**, so the cells that were right stay right without changing a line. Under `installed` the SV checks may run and each must be ok/warn/skip — a FAIL there is a real defect, not an environment — and `report.server.state` must be `ready`, because an installed tree whose server never answered is not green, it is silent. Six tests, each shape passing its own world and FAILING the other: the bootstrap fixture passes `absent` and fails `installed` (no server answered), run 8's shape passes `installed` and fails `absent` with the message that was right for that caller, a failing SV check is refused under `installed`, and an unknown `--deps` value is a usage error rather than a silent default. `workflows.test.mjs` asserts the flag on the release step AND that no bootstrap cell passes one — a cell that started saying `installed` would stop noticing something installing too early |
| ARC-09-C21 | **Rehearsal run 9** (release.yml run 34658049913, from develop @ 6e2c9db): all three `verify` jobs GREEN — tag, `npm ci`, lint, `npm test` on the released tree, dist, install, citations, the doctor captured and judged with `--deps installed`, metrics, artifacts — and `publish` ran for the FIRST TIME: download, metrics table, release notes, `assert-assets: 6 asset(s) clean`. Then `gh release create` refused: **`HTTP 422: Validation Failed — body is too long (maximum is 125000 characters)`**. No Release was created. The body was the changelog's whole `## 2.0.0-rc.0` section — ARC-09-C12c correctly moved 1,224 lines of hand-written Notes into it — plus a metrics table and the C19 sentence, both appended by the workflow with `cat` and `printf` AFTER the script ran, so nothing knew the total | **Closed** — ONE WRITER: `release-notes.mjs` composes the whole body, metrics and sentence included, under a named `RELEASE_BODY_MAX = 125_000` (the API's number, as a constant, because a reader who meets a truncation needs to know whose rule made it). **Which end survives is the decision**: the cut is made from the TOP of the prose, so the generated groups and the trailer — what the release IS — are kept whole, and the Notes are replaced by a link to `docs/CHANGELOG.md` at the tag, which has every word. The obvious implementation truncates the tail and loses exactly the part that says what changed; a test asserts the groups and the trailer survive, so that mistake cannot be made again quietly. Truncation lands on a paragraph boundary with fence markers balanced — cutting from the top makes 'the body OPENS inside a code block' the likely failure, so the test counts markers rather than looking at the end. A small section is unchanged. And a test asserts nothing appends to `release-notes.md` after the script, because two more writers is how this happened. Measured on the real file: the section alone is 108,225 characters — run 9's overflow came from the generated groups on top of it |
| ARC-09-C28 | **`upgrade-e2e (windows-latest)` hit `timeout-minutes: 30`** on fe75226 (run 34675462899, 05:23:51Z→05:54:03Z, **30.2 min, cancelled**) — the C13 walkthrough was GREEN, so C27b held and the job simply ran out of time: fourteen fixture worlds at 130–270 s each where Ubuntu takes 10–20 s | **Closed by one flag, after the instrument named the term.** `world[win32]` before — `tree-copy=10937ms corpus-upstream=1639ms modules-link=0ms **rewrite-version=290137ms** gen-all=808ms release-A-base=353739ms rewrite-version-A=1964ms rewrite-version-B=1976ms clone-user=4738ms TOTAL=375475ms` — the first `npm version --workspaces` was **107–290 s, 70–77% of every world**. The reading that mattered was not *the first call is slow*: the ONE world that gets a real `node_modules` copy did the same call in **2.6 s** while the worlds that junction to the checkout's took 139–290 s. `npm version --workspaces` REIFIES `node_modules` after the bump, and through a junction that resolves the actual tree through the link. `--no-workspaces-update` skips the reify and nothing else: the probe hashes `package-lock.json` either side of the bump and reports it rewritten for all three fixture releases with the flag exactly as without, and the AC 3 walkthrough — the fixture's own lint gate, `version-consistency` and six more tree-scanning tests run INSIDE the released tree — passes. `world[win32]` after — `tree-copy=15447ms corpus-upstream=3336ms **rewrite-version=1592ms** gen-all=876ms release-A-base=61109ms clone-user=2428ms TOTAL=74422ms`. **Job 06:10:14Z→06:20:05Z = 9 min 51 s, success** (was 30.2 min, cancelled), `timeout-minutes` untouched at 30. The product keeps the plain call: `applyWrites` reifies a `node_modules` it OWNS and refuses a symlinked one by design, so a real release never pays this — the fixture differs about a directory it does not own, not about what gets written. `--ignore-scripts` deliberately not added: no manifest here has a version/preversion/postversion script. One hypothesis tested and discarded rather than shipped as a reason: the reify does NOT write back through the junction into the real checkout's `node_modules` (byte-identical before and after on macOS). The lock check is kept as an ASSERTION — the bump moving the lock is why npm is called at all — and its absence throws rather than passing vacuously; the one `world[…]` line per world is kept as a standing diagnostic. **Left undone on purpose:** the per-file template (one tree copy, one corpus, one base release) is now a COST argument, not a timeout one — `release-A-base` 24–61 s and `tree-copy` 3.3–15.4 s are the next terms — and gets its own chore rather than riding this PR |
| ARC-09-C29 | **Ten otherwise-hermetic fixture tests depended on github.com being up.** An upgrade-suite run failed at `FAIL B00: cannot reach github.com (DNS)` during a network drop, in tests whose corpus upstream is a LOCAL bare repository | **Closed — and the cause was not a hardcoded host.** B00 ran `git ls-remote https://github.com/<repo>.git` with `repo` from `engine.config.json`: the PRODUCT repository, used as a stand-in for "is github.com up" (ARC-06-S04's scope says exactly that, and `01` §4.1's floor is "HTTPS to github.com"). **Nothing in a bootstrap contacts that remote** — the consumers are B02 (`docs.upstream`) and B04 (the npm registry, live mode only), and `upgrade` uses the checkout's own `origin`; `git grep '"repo"'` finds the field read in the two launchers and nowhere else. So the check asked about a host the run does not use and stayed SILENT about the one it does: a corpus on an internal mirror passed B00 and failed at B02, which is the failure a preflight exists to move forward. B00 now probes the configured corpus remote with the same git operation, `corpusProbe()` deciding between three outcomes — probe it, *corpus upstream is local — no probe*, or *corpus upstream is `<scheme>` — not probed (this check speaks HTTPS)*, because reporting an `ssh://` remote as "local" would be the same untrue-statement bug one scheme further along. The default `docs.upstream` is on github.com, so **nothing changes in the shipped configuration**. Three implementations, one set of sentences: `net-sentences.mjs` gained the two skip lines and `dnsFailure` is filled with a SLOT (`%s` for printf, `{0}` for PowerShell's `-f`) instead of a literal host. Two of my own bugs on the way, both caught by existing guards in one run: the generator's new import bypassed the `--root` pattern the sentences use (it would have generated the real repository's text into a fixture tree), and both launchers matched `file://` against the scheme arm and called a local upstream an unspeakable remote. Launcher budgets 185 → 198 and 265 → 278, with the reason recorded. **Two residuals, ruled 2026-09-12 and recorded here so neither reads as a design.** (a) B04's npm-registry dependency: the new rule implies it should be probed in live mode, and adding it is a NEW failure mode in a shipped configuration — **a story for the 2.0.x patch stream, decided with the acceptance results in hand**, not a chore now. (b) `probe-net.mjs` still carries `DEFAULT_TARGET = 'https://github.com/'` as `probeNetwork`'s default for a caller that passes none. Production always passes the plan's target now, so that constant is **the old floor in dormant form, a leftover rather than a decision**; `target` becomes required (or the default goes) in the next pull request that touches the file |
| ARC-09-C25 | **Every relative link in `README.md` was 404 from the repository root.** `README.md` is composed from `docs/INSTALL.md`'s body, so `[proxy](TROUBLESHOOTING.md#proxy_unreachable)` — correct in a page that lives in `docs/` — resolves to a file that is not there once the body is at the root. Five links on the project's front page, and `tests/docs-links.test.mjs` could not see it: it checked the three SOURCE pages, which is right for every other property and blind to this one, because the composed copy is the only place the defect exists | **Closed** — `renderReadme` retargets relative link targets to `docs/<target>`, normalised so `../x` resolves OUT of `docs/` to the root rather than becoming `docs/../x`. Absolute URLs, `mailto:`, root-relative targets and a bare `#anchor` are left alone, each asserted. The source keeps writing links that work where it lives — the alternative was to write every link wrong in the source so it came out right in the copy. `README.md` joins the link-checked set as a fourth document resolved from the root, with a planted-link control (`[x](TROUBLESHOOTING.md#y)` in a synthetic install body must compose to `docs/TROUBLESHOOTING.md#y`) — without it, "the README has no dead links" stays true the day the generator stops rewriting them, because the four it rewrites would simply break together. S01's migration-pointer test now asserts BOTH forms and that they differ, since the pair is the property |
| ARC-09-C26 | **`commitlint` printed `0 commits ok` for a range it never judged.** With no `--base`/`--head` the range comes from the branch's UPSTREAM, so running it after a push compares the branch with itself. The same branch reported `7 commits ok` before a push and `0 commits ok` after, an hour apart, and both were quoted as a gate in a report | **Closed** — an empty range is a REFUSAL (exit 2) naming the range and the way out, unless `--allow-empty` is given; and every success line now names the two refs it judged, so a count is never read without knowing what it counted. Both directions tested, plus that the refusal prints no pass line. CI is unaffected: a pull request supplies `GITHUB_BASE_REF` and a pull request with no commits does not exist. Two existing tests pinned the exact success string and caught the format change, which is the behaviour that makes pinning it worth it. `docs/CONTRIBUTING.md` § Commits carries the correct local invocation and why the bare one lies |
| ARC-09-C30 | **`release-dryrun (ubuntu-latest)` failed one cell of PR #157 on a test that had nothing to do with that PR**: `input-hash.test.mjs` AC 2, *a password edited in place … makes NOTHING stale*, returned `['B08']`. Same file ARC-09-C18 named as having gone red once and passed on a re-run — written off then as a flake with no cause | **Closed, with the cause measured rather than inferred.** `storeStamp` is `floor(mtimeMs / 1000):size`, coarsened on purpose so a same-length credential edit does not restamp the store. The test restores the mtime through `stamp.mtime` — a Date with MILLISECOND precision — while `mtimeMs` carries a sub-millisecond fraction, and **`Date` rounds**. One captured crossing names the step: `before 1789207675999.5 → Date() 1789207676000 → after 1789207676000`, moving the floor from …675 to …676. **22 crossings in 40,000 cycles (~1 in 1,800), all 22 just BELOW a whole second (fraction 999.x), none above** — so the fixture is nudged 5 ms down when it lands in that band, and the guard is one-sided BECAUSE THE RUN SAYS SO. My first proposal guarded `% 1000 < 2`, the side that never crosses, and would have fixed nothing; the architect asked for a symmetric guard or the side distribution, and the distribution is what corrected it. Proven both ways on a forced band: 2,000 of 2,000 cycles cross without the guard, 0 with it. The PRODUCT is untouched — the coarsening is deliberate and right; what was unsound is a fixture that depended on rounding luck. No re-run: a re-run without a fix is what the standing rule forbids, and every later pull request rolled the same die |
| ARC-09-C27 | **`upgrade-e2e (windows-latest)` was the last red cell on #147** (run 34669534161): `not ok 3 — C13: ARC-09-S11 AC 3` with `git status --porcelain` reporting ` M vendor/ServiceNowDocs` after the fixture release, on a tree clean on ubuntu and macOS | **Closed** — a path length, and the record that says otherwise is about a different path. The corpus's longest page is 197 characters and ARC-00 S-07 is quoted in `sync.mjs` as having REFUTED the need for `core.longpaths` on Windows; its own words are `the core.longpaths control did NOT reach MAX_PATH, so AC 2 is unanswered for a real install path` — the question was left OPEN, about a real checkout, where `D:\a\<repo>\<repo>\vendor\ServiceNowDocs\markdown\alpha\` keeps it inside 260. The fixture runs under a TEMP directory: ~103 characters before the corpus path begins, so the same file lands at ~285 and the margin the record measured is spent before the checkout starts. The product carries `-c core.longpaths=true` on every corpus git call (`withLongPaths`) AND writes `core.longpaths` into the corpus config once it exists; **this first fix did only the first half, and only on one call** — `submodule update --init` — which is why the cell stayed red on 0b8e616 with the new diagnostic printing `longpaths: "<unset>"` beside `porcelain: "M markdown/alpha/llll….md"` and head equal to the gitlink. Completed in C27b: one git entry point in `tests/upgrade/harness.mjs` carrying the flag on EVERY call, `persistLongPaths()` writing it into each fixture repository and reading it back, and a both-directions scan asserting no git call in `tests/upgrade/` bypasses the wrapper. The SYMPTOM is why this cost two round trips and is gone too: a parent prints ` M <submodule>` for content modified, HEAD moved off the gitlink, or a file that never checked out — the same two characters for all three. `submoduleState()` asks the submodule itself (porcelain, HEAD vs gitlink, `core.longpaths`, platform), the corpus is asserted complete AND at its gitlink as two assertions that fail differently, and the state is logged on EVERY run including green ones — a diagnostic that prints only on failure leaves nothing to compare the red run against. Two defects of my own found by existing tests on the way: C14's rename left `{ rel }` resolving to the new module-level normaliser, so every precondition-asserts finding printed an arrow function instead of a path; and that sweep called `git config --get` a mutation, demanding an assertion that a READ took effect — the row's hazard is a value that does not take, so the read flags are excluded by name with a two-way control, after a first attempt whose lookahead sat behind a backtrackable `\s*` and matched anyway |
| ARC-09-C24 | **PR #150 went red on `release-dryrun (windows-latest)`** (run 34665084977): inside the release's test gate, `tests/hook/session-start.test.mjs` AC 1 failed with `the fast path took 1597 ms` against a 1000 ms CI-only budget. The same class as C3, C4 and C18 — a wall-clock assertion inside a parallel matrix cell measures the cell — and it would trip any pull request that happened to land on a loaded Windows runner | **Closed** — six wall-clock assertions removed from the unit suite, five from a grep and one found while doing it (`tests/contract/engine-lint.test.mjs`'s best-of-three 45 s hang detector). Each was replaced by the FACT it was standing in for, never by a looser number: the banner reports which branch answered (`path: 'cache'` / `'timeout'`) and still prints with git off PATH, so the hook's two budgets and its watchdog's `< 2000` had nothing left to say; `docsStatus` leaves `citations`, `fileCount` and `sizeBytes` `null` for work it did not do, which is what the 300 ms budget was proxying for; `syncCorpus` prints one `[docs] <phase> … N s` line per unit of work, so **zero phase lines** is the no-op the 60 s ceiling was guarding, with a control asserting a first run prints some; the doctor registry's `--quick` 3 s budget goes to C8's cost contract, whose shared-context half is enforced statically in that same file and whose milliseconds `scripts/ci/check-timings.mjs` measures on five cells. Where the claim is only *it terminates*, the bound is now the SPAWN's — `timeout` + `killSignal`, then assert the child was not killed — which also took engine-lint's case from best-of-three to one run. `docs/CONTRIBUTING.md` gains the rule naming all six |
| ARC-09-C23 | The **M4 merge to `main`** (PR #148, run 34661815057) was green on 53 of 54 contexts and stopped on `commitlint`: `FAIL 964a6f4 "docs(roadmap): M3 merged to main 2026-09-11 @ 81bff6c (PR #123)" — scope "roadmap" is not one of the allowed scopes` and `FAIL df877dd "ARC-09-S01: one command cuts a release, and refuses to cut a bad one" — expected type(scope)?: subject`. Both predate the convention — S02 added the lint AFTER S01 merged, and the M3 record commit was written before the scope list existed. A story's pull request lints its own range and never meets them; a MILESTONE pull request to `main` lints `origin/main..HEAD`, the whole arc, including history the convention never governed. ARC-09-S02's own rule is that history is never rewritten to suit the parser, and the changelog generator already tolerates these with `(unconventional)` — the LINT contradicted it | **Closed** — the lint governs commits that DESCEND from `485fc49` (S02's merge, the commit that introduced it); anything older prints as `(pre-convention, recorded as written)` on stdout and does not fail. The boundary is inclusive and a test says so, because "is the floor itself governed" is otherwise a coin flip for the next reader. Verified on the real range: the two blocking commits and two more of the same era are recorded, **44 commits governed and ok**. One bug of my own, caught by two EXISTING tests within a minute: `merge-base --is-ancestor` answers 0 for yes and 1 for no, and anything else is git failing to answer — 128 when the floor is not in this clone, which is every fixture repository and any shallow checkout. Reading 128 as "not governed" made the lint silently check nothing there, which is the quiet failure the file exists to prevent; only exit 1 means no, and an unknown floor lints everything |
| ARC-09-C16 | **Rehearsal run 3 reached `release.yml` and lost all three `verify` jobs at their FIRST step** (run 34643610242): `tag v2.0.0-rc.0 is not annotated — create it with scripts/release.mjs`, on a tag that WAS annotated — the remote listed both `refs/tags/v2.0.0-rc.0` and its `^{}` peel. `actions/checkout@v4` on a tag ref writes `refs/tags/<name>` pointing straight at the COMMIT even with `fetch-depth: 0`: it peels the tag, so an annotated tag arrives in the runner's clone indistinguishable from a lightweight one and `cat-file -t` says `commit`. `publish` skipped, no Release was created. Nothing before the rehearsal could have seen it — `release-dryrun` never has a tag, and `tests/release-workflow.test.mjs`'s tag is local | **Closed** — `release.yml` re-fetches the tag object right after checkout in BOTH jobs that read it (`git fetch --force origin "+refs/tags/<ref>:refs/tags/<ref>"`), with the reason in the step name, and a test asserts the step exists in every tag-reading job and precedes the thing that reads it. `verify-tag.mjs` also stops blaming the tag: when the local ref is a commit AND `git ls-remote --tags origin <name>^{}` shows a peel, it says the remote has the annotated object and this checkout peeled it, and names the re-fetch. The refusal STANDS either way — a clone cannot read a message it does not have — only the remedy changes. Three tests: the peeled-locally/annotated-remotely case built from a real bare remote (with the precondition asserted, `cat-file -t` = `commit`), the negative control of a tag that is lightweight on the remote too keeping the original message, and the workflow-order assertion. The remote probe is quiet and runs only on the failing path: an `ls-remote` with no `origin` prints two lines about access rights, which is noise in a report about a tag and broke an existing test that asserts the exact refusal |
| ARC-09-C5 | Nobody knows where the banner's re-run path spends 690–920 ms on the Windows cells, and the cap must not move until somebody does | **Measuring, no fix** — two tables per cell as step summaries, on the four Windows cells and one ubuntu cell for contrast: the quick doctor's per-check `durationMs` (top five plus the sum of the rest), and the hook's own phases (cache read + decision · state/config · doctor spawn · render) timed inside the hook behind `SNOWARCH_BANNER_PHASES=1`, whose absence is the tested default and which prints to stderr because stdout is the banner's. A first signal from the committed fixture report: `SV-00 Node version` is 276 ms of 449 (61%) — a spawn, on the one check whose cost is a process. The trip is a **Windows** property, not a no-gitbash one: on run 34604434070 windows node 20 read 922 ms of product cost against no-gitbash's 860 |
| ARC-09-C3 | `tests/version-tag.test.mjs` AC 5 proved "no network code" with `elapsed < 2000`. A wall clock cannot tell a socket from CONTENTION: 2494 ms inside the concurrent suite, 232–268 ms for the same file alone | **Closed** — the proof counts connections instead. A real listener on loopback, named as the proxy in the CHILD's environment only, and the assertion is that nothing arrived. A negative control runs first — one deliberate connection, which the counter must see — because a listener that cannot count is a listener that proves anything. No timing left to tune |
| ARC-09-C4 | `scripts/ci/banner-timing.mjs` judged the raw median of five cold hook spawns against a 1000 ms budget. The same cell (`bootstrap (no-gitbash, windows-latest)`) measured 728, 802, 944 and **1027 ms** across four consecutive runs whose product code was byte-identical (`git diff 09243a2..b9bdab3 -- tools/ scripts/ packages/` is empty), and the fourth failed the budget the first three passed | **Closed, with the assumption corrected by the measurement.** The harness measures an empty-Node floor, interleaved with the real runs, and spends the budget on the difference. I wrote it believing most of the 1027 ms was Node starting up; the first CI run says otherwise — `median 903 ms; node floor 43 ms → banner 860 ms`. The subtraction is still right (a budget should not be spent on another process's startup) but buys ~40 ms here: this cell's banner is genuinely near its cap. It measures the RE-RUN path only (the cache is deleted before every spawn), never the fast path a second session gets. **Open follow-up for ARC-08's owner:** ~140 ms of margin against an observed ±150 spread means this can trip again; the number to move is then this cell's cap, with the measurement recorded — not the method |
| ARC-09-C2 | `tests/doctor/helpers/tree.mjs` symlinked `vendor/ServiceNowDocs` into every fixture, and `existsSync` says yes to the empty submodule MOUNT POINT a checkout without content carries — so eight `--fix` tests in `tests/doctor/fix.test.mjs` reached F2, whose job is to repair a missing corpus, and it repaired the LIVE one: 305 MB cloned from the real upstream by a unit test. Invisible where the corpus is already complete; on a CI cell with no submodule it built the corpus while the rest of the suite ran, which is what made `the two docs entry points are one implementation` read 17 areas and then 19. A fixture now gets a real corpus or its own empty directory, and its config cannot reach a network upstream | **Closed** — the writer named and fixed at the cause; a new test asserts no live mount point in a fixture, no network upstream in a fixture config, and the live corpus unchanged by building one. Verified on a corpus-less clone: the whole suite leaves 0 entries where one test used to leave 19 areas and 305 MB |
| ARC-09-C33 | **`input-hash.test.mjs:251` asserted a probability as a law.** `test (windows-latest, node 24)` on PR #170, once, and not on the previous head: `assert.notEqual(statSync(store).mtimeMs, stamp.mtimeMs, 'a restored mtime is never byte-equal')` failed with `actual 1789240976993 = expected 1789240976993`. The comment above it explained that `utimesSync` ROUNDS a `Date` to the millisecond so the value returns "near but not equal" — the rounding is real, "never equal" is not. It holds only while the original mtime carries a fraction, and when the fixture's write lands on a whole millisecond the round trip is EXACT. NTFS ticks are 100 ns, so roughly one run in ten thousand; APFS and ext4 rarer, which is why it had never been seen. **The same family as C30's boundary crossing, on the other side of it** — C30 guarded the 999.x side, this is the .000 side | **Closed** — line 251 was never the property under test; the lines after it are (nothing goes stale, the stamp is `floor(mtimeMs/1000):size`, the version read is the version only). The `notEqual` is replaced by what the stamp actually depends on — `Math.floor(restored/1000) === Math.floor(stamp.mtimeMs/1000)`, the restored mtime is in the same SECOND — which holds on every crossing, including the one that failed. **The C30 guard is deliberately NOT widened to cover this:** the two are different questions, and one guard answering both would answer neither precisely. The explanation stays as a comment, because the rounding it describes is true and worth knowing; only the claim that it is never exact is gone |

| ARC-09-C32 | **E-28 asked the remote on every single networked run while no release existed** — ARC-09-S07 specifies that it "refreshes the cache at most once per 24 h", and the no-release path returned BEFORE `writeUpgradeCheck`, so nothing was cached, `needsRefresh` was true for ever, and each run spent an `ls-remote` with a 15 s budget. Measured: three runs, three calls, `cache written? NO`. Found by the architect's live E-28 runs on the rc.1 remote, which reported the same skip three times where a cached second run was expected — the prediction was wrong and the measurement was right | **Closed** — and it is THREE changes, not one, because two other branches assumed "a cache implies a release exists". **(a)** the empty outcome is written before the skip; **(b)** the cached hit returns a skip instead of `ok "up to date (no tag)"`, which claimed currency with something that does not exist — the `?? 'no tag'` fallback was answering it; **(c)** `--no-network` branched on `latestTag` being TRUTHY, so once (a) landed a run that DID check and found nothing would have reported "nothing has been checked yet" — **turning a true statement into a false one**, and the only one of the three that is a regression created by the fix rather than a defect it repairs. Wording is part of the change and asserted verbatim: PRESENT tense for what the check sees now, PAST for what the cache remembers, which is the distinction a cache introduces — `origin advertises no release tags` live, `origin advertised no release tags · last checked <stamp>` cached, `--no-network; the last check (<stamp>) found no release tags`. **Fix-up after the live run: (c)'s sentences could not print, and my test drove the body past the framework to assert them.** `planRun` skips every `network: true` check BY THE FLAG before any body runs, so the doctor said `E-28 skip release currency: --no-network` and nothing more — and those sentences had been unreachable since ARC-09-S07 wrote them (`9c4592d`), so C32 extended dead code rather than creating it. Calling `E28.run(...)` directly with `noNetwork: true` is the passes-for-the-wrong-reason class, in my own test, in the pass that keeps finding it elsewhere. Resolved by making them REACHABLE rather than deleting them, because a user offline is exactly who most wants to know what the last check found: a check may now declare `offline: true` — an optional flag, unlike the four mandatory ones, and only meaningful on a network check — and the runner runs such a check under `--no-network`. `--quick` is untouched: its own branch already skips every network check, so the `|| quick` removed from the second condition changed nothing there, and `--quick` remains a cost contract about spawning. The flag cannot enforce the promise it makes, so the test counts subprocess calls and asserts zero, and a network check WITHOUT the flag is still skipped — both directions. The case runs through `runChecks`, not the body. Live, on the fix-up head: run 1 `origin advertises no release tags`; run 2 `origin advertised no release tags · last checked <stamp>`; run 3 (`--no-network`) the same stamped sentence rather than a bare `--no-network`. Eight cases, both directions on the release-tag paths so this cannot quietly turn a warn into a skip, plus the window's other half: an expired empty outcome asks again and finds a release published meanwhile, a one-hour-old one asks nothing. **No banner consequence, checked rather than assumed:** the nudge needs `behind === true` AND a `latestTag` (`hooks/session-start.mjs`), and a cached empty outcome has neither. Worth recording beside C31: that hook's own comment already stated the clock convention C31 had to enforce — *"`nowMs` is a NUMBER … a second convention inside one module is how a `now()` gets called on a number"* — so the number contract was the house rule all along, and E-28 was the one place that departed from it |

| ARC-09-C31 | **E-28 (release currency) crashed the doctor the moment the remote had a tag to compare against, and `v2.0.0-rc.1` was that tag.** Every `bootstrap (…)` cell on PR #164 went red with `E-28 FAIL release currency: check crashed: now(...).toISOString is not a function`, and `assert-doctor` refused the snapshot — *1 unexpected FAIL*. The cause is a clock contract with two readings: the doctor supplies `ctx.now = () => Date.now()`, a NUMBER (and `doctor/index.mjs` wraps it in `new Date(now())` in three places), while E-28 wrote `const now = ctx.now ?? (() => new Date())` — a fallback that returns a `Date`. So the check behaved one way against its own fixtures, which passed no `ctx.now`, and another in production. **Measured on isolated fixtures, the crash has TWO paths, not one:** `writeUpgradeCheck` → `now().toISOString()` when a release tag resolves, and `isFresh` → `now().getTime()` **whenever a cache exists, with no tags at all** — so any machine that had ever completed one successful check would have failed every networked run afterwards. rc.1 exposed the first; the second was waiting for anybody. (`v2.0.0-rc.1` alone does not reach either: it is a prerelease, filtered by `t.pre === null`, and a cacheless run skips with *advertises no release tags*.) | **Closed** — E-28 wraps (`() => new Date(ctx.now ? ctx.now() : Date.now())`) and the contract is stated where the context is built: `ctx.now()` is epoch milliseconds, because that is what the runner does arithmetic on. `tests/doctor/clock-contract.test.mjs` makes it mechanical — every `ctx.now` in `checks/` must sit inside `new Date(ctx.now`, comments stripped, both directions. **That scan caught itself first:** its first rule asked for `new Date(` anywhere on the line, which the defective line contains, in its fallback — the control asserting that the crashing line is a finding is what found it. `tests/doctor/release-currency.test.mjs` adds seven cases against an injected `ls-remote`, each on its own root (a shared root had a later case reading the cache an earlier one wrote): a newer release warns, a prerelease alone is **not** "available" (the existing rule everywhere in the product, recorded now as the decision — a maintainer's candidate must not nudge every user to it). **The alternative reading was considered and declined:** offering a newer PRERELEASE to a checkout that is itself a prerelease or a dev build is defensible, and it is a behaviour change nobody asked for — the product filters prereleases identically in `releaseTags()` and in E-28 today, and a fix for a crash is not the place to change what the check reports, a prerelease beside a release reports the release, junk tags are ignored without crashing, a cache is read rather than crashed on, a fresh cache costs no remote call, and `--no-network` asks nothing. Crash containment already held and is cited rather than duplicated: `tests/doctor/framework.test.mjs` AC 1 — a throwing check becomes `check crashed: …` and the other checks still run |
| ARC-09-C1 | `tests/helpers/temp.mjs` fixture sweep: `rmSync` defaults to zero retries, and one throw aborted the sweep loop *and* skipped `pending.clear()`, so a single undeletable fixture stranded every directory after it and the diagnosis never printed (`process.stderr.write` from an `exit` handler to a pipe is asynchronous — it needs `writeSync(2, …)`). Each directory is now removed independently, with retries, and survivors are reported with path, errno and the Node version | **Closed pending confirmation** — 48/48 clean on macOS / node 24, which is where it was reproduced, exit `1/null` matching CI. It closes for real the first time ubuntu / node 24 runs green three times in a row |
| ARC-09-C34 | **`upgrade-e2e (macos-latest)` failed once in thirteen runs with `bad tree object`, `git upload-pack: git-pack-objects died` and `early EOF`** — reading the harness's own bare origin, in a test the failing PR did not touch, while ubuntu passed the same test in the same run, and green again on the very next head | **Closed as fixture hygiene, and the row says exactly what was established.** **Confirmed:** `git grep 'gc\.auto\|maintenance\.auto\|autoDetach' tests/upgrade/` returned **nothing** — the harness disabled none of them, so a detached `git gc --auto` was live in every repo it creates (`gc.autoDetach` is true by default, and a world makes many commits, two tagged releases and a bare clone). **Not established:** that gc actually fired in that run — the job log carries git's client-side error, not the source repo's housekeeping, and the temp tree is gone. So: `gc.auto 0` and `maintenance.auto false` on all three repos at creation, read back the way `persistLongPaths` does. **Considered and left:** the bare clone's hardlinked objects. They are safe against what gc does — git writes new packs and unlinks old NAMES, never rewriting an object file in place, so the origin's links keep their inodes; `--no-hardlinks` would only make the phase timings incomparable with every run recorded before it |
| ARC-09-C37 | **E-28 nudges toward a tag that can never be a release: it counts lightweight tags.** rc.2's own release run uploaded a doctor snapshot on all three OSes reading `E-28 warn — v9.9.8 available — run ./snowarch upgrade`, while `v9.9.8` was the negative drill's **lightweight** tag sitting on the remote — the very tag `release.yml` refuses by design (`verify-tag`: "tag v9.9.8 is not annotated"). So E-28 advertised an upgrade to a version the release path would reject. **The fix shape needed one correction before it can be written:** the discriminator is the peeled `refs/tags/<name>^{}` line that only an annotated tag produces, but `host.mjs:314` runs `ls-remote --tags **--refs** …`, and `--refs` strips exactly those lines. Measured on a throwaway local repo with one lightweight and one annotated tag: **with** `--refs` both tags print one identical line each and are indistinguishable; **without** it, only the annotated tag gains the `^{}` line. So the change is (a) drop `--refs`, (b) treat a tag as a candidate only when its `^{}` line is also present, and (c) skip the `^{}` lines themselves when parsing versions, or `v9.9.9^{}` enters the comparison. Test both directions: a lightweight `v*` tag is not "available"; an annotated one still is. | **Open — 2.0.x.** Not a release blocker: it needs a junk lightweight `v*` tag on the remote, which only the drill creates and cleanup removes. Numbered **C37** because C35 and C36 are already spent in merged commit `7e3bfae`'s message (the B09-03 and B09-07 record corrections), and one identifier must not name two chores |
| ARC-09-C38 | **A leftover fixture told nobody why — and the reason had already been printed and thrown away.** Two single occurrences in one day: CI ubuntu/node24 and a local run, each reported as a directory name and a file count. **The CI one exited 0**, so nothing was killed and the exit sweep DID run. `temp.mjs` reports a failed `rmSync` with its errno and the Node version — its own comment says that is *"what the three CI occurrences never told anyone"* — and `fixture-cleanup.test.mjs` used the child's output only in the STATUS assertion, so the leftovers assertion printed a listing and dropped the diagnosis. The listing cannot tell the two cases apart either: `N entries` is what a removal that **ran and failed** leaves, exactly as a removal that **never ran** does. | **Fixed, and the cause is named on evidence rather than on a rate.** Two mechanisms proved by direct experiment, not by waiting: (a) SIGKILL a process holding a context-less fixture and it survives — real, and it matters because **118 `makeCheckout()` call sites pass no test context**, so the exit sweep is all they have; (b) make `rmSync` fail (a 0500 parent) in a child that **exits 0** and the sweep prints `… survived on node 24.16.0: <dir>: ENOTEMPTY` — **the CI signature exactly**. The assertion now carries that output, the wording says `not removed` instead of claiming `removal never ran`, and a new test drives the failing-removal case and requires an errno. Instrumentation first: an owner record is written BESIDE each fixture at creation (not inside — `makeCheckout` builds a checkout tests walk and run `git` in), so a survivor names its creator even when the process dies before any cleanup. **20 full-suite runs in an isolated TMPDIR reproduced nothing** — consistent with an environment-bound `rmSync`, and said plainly rather than dressed up as a diagnosis. **One correction to my own fix, caught by its control:** the first version read only `child.stderr`, but the probe runs under `node --test`, whose runner captures the file's output to **stdout** — it would have surfaced nothing, the same shape as the bug it repairs. Both streams now |
| ARC-09-C39 | **The teardown deleted the record of the fixture it had just failed to remove.** ARC-09-C38 made a leftover arrive with its reason; the very next occurrence used that to name a mechanism C38 had not considered. `test (ubuntu-latest, node 24)` on #188 reported `snowarch-bootstrap-GrwiZp (9 entries — not removed)` and, underneath it, the line C38 added: **`(neither stream mentioned the sweep)`**. The silence was the diagnosis. Three facts settle it: the child **exited 0**, so the exit handler had its turn; the sweep **printed nothing**, so it did not try and fail — it had nothing in `pending` to try; and the **`.owner` record was gone**, which only the after-hook or a SUCCESSFUL sweep does, and a successful sweep would have taken the directory with it. So `trackTempDir`'s `t.after` ran, its `remove(dir)` failed, it deleted the owner record anyway, and `pending.delete(dir)` struck the directory off the register of things still owed. The comment on that very line read *"the sweep's message is where a survivor gets reported"* — and the line itself guaranteed the sweep would never see this survivor. **The reporting path was cut off by the code that handled the failure**, which is the same shape as `wizardAvailable`, `plan === null` and C38's own discarded errno | **Fixed in both places that had it, and the instrument is fixed with them.** The after-hook keeps a failed fixture on the register — `if (remove(dir)) return;` — so the exit sweep retries it (500 ms once, at exit, only for something already known to be stuck: a fair price for the difference between a diagnosis and a directory name) and reports it with its errno. The sweep had the SAME defect one level up and it was found by the new test rather than by reading: it removed `${dir}.owner` unconditionally, so a stuck fixture lost its record anyway; it now deletes the record only when the directory has actually gone. `leftBehind` stops counting `.owner` files as fixtures and instead **prints the creator's name** from the record, so the next leftover says who made it in the same line that says it survived. **Negative control, run:** with the old after-hook restored the new test fails and says *"the after-hook failed to remove it and the sweep said nothing — the survivor was struck off the register by the code that failed to remove it"*. Both directions on the instrument too: every surviving fixture keeps its record, and no record outlives the fixture it names. **This red was not #188's**; it is the pre-existing leak, diagnosed at last because C38 made the silence visible |
| ARC-09-C41 | **The preflight failed a whole install on one unlucky probe, one step before the code that already knew better.** ARC-03-C1 gave B02's corpus checkout three attempts for exactly this class of transient in 2026-09. B00's network probe, which runs *earlier in the same bootstrap and contacts the same host*, had none — so the flaky minute B02 survives by waiting 3 s could stop the run before it started, as `EXIT_PREREQ`. That is the question ARC-09-C40 raised and did not decide; this row is the ruling carried out | **Implemented, and one part of the ruling was changed on evidence.** The **schedule** is B02's, imported (`RETRY_WAITS_MS`) rather than re-typed, and so is the **closing sentence**: `stopSuffix` was refactored over a new shared `attemptSuffix(attempt, stillTransient)` so the preflight says `(3 attempts)` and `(stopped after 2 attempts — this failure is not retried)` in #186's words, from #186's definition — a second copy of a sentence is the thing this repo keeps removing. **One rule, two readers, because the two sources differ in kind.** The CLASSIFIER is deliberately NOT shared: the ruling said *"same transient classifier"*, but `transientReason` reads libcurl's words **as git surfaces them**, and this probe returns structured errno/DNS codes and an HTTP status. Reusing it would have meant rendering codes back into prose so a text matcher could read them — discarding better evidence to reconstruct worse. `probeRetryReason({code, status})` applies the SAME RULE (408, 429, any 5xx are the upstream asking to be asked again; any other 4xx is a statement about the request) to the codes, over `probe-net.mjs`'s own `DNS_CODES` / `REFUSED_CODES` / `isTlsCode` sets rather than a second copy of them. `NOTFOUND` and an intercepted TLS chain are permanent and stop at once; `EAI_AGAIN` is not, and is the DNS case worth waiting out. **Cost, stated rather than assumed:** a genuinely broken setup stops on attempt 1 and pays nothing; only a flaky network spends the two waits. **Four tests, both directions, schedule proven without being spent** (injected `sleep`): transient→success passes and says *after 2 attempts*; permanent stops at once with no count, because a count of one is noise; exhaustion says `(3 attempts)`; and a class CHANGE says it **stopped**, which is ARC-03-C3's defect caught at the probe before it could happen. **Negative control run:** with the single-attempt version restored, three of the four fail |
| ARC-09-C40 | **A step that captured a log in order to print it printed nothing on the one run where the log mattered.** `bootstrap (macos-latest, node 24)` failed on #193 with `##[error]Process completed with exit code 3` and **not one line of explanation**. The step is `./bootstrap.sh … > "$RUNNER_TEMP/second.log" 2>&1` followed by `cat` — under `bash -e` the failing call aborts the step, so the `cat` never runs and the redirect is the only copy. **And the number alone cannot name the cause:** `exit.mjs` defines `3` as `EXIT_PREREQ` while the docs family defines `3` as *corpus missing*, an overlap its own comment calls tolerable — **I read the CI log and mapped it to the wrong one**, which is the evidence that it is not tolerable without the log. The Windows twin already prints its output but **collapsed every failure to `exit 1`**, discarding the same number on the cells where reading a log is hardest | **Fixed, and two parts of the brief corrected against the tree.** The POSIX step captures the code (`|| code=$?`), always prints the log, then exits with what the run actually returned; the Windows twin exits `$rc` instead of `1`. **(a) The `doctor.json` upload was already `if: always()`** — line 619, with a comment saying why — so it is not the defect: *"No files were found"* means the second bootstrap failed before the assertions step wrote the file, and the upload behaved correctly with nothing to upload. **(b) The Windows branch was already printing its log**; only POSIX swallowed it. **What the failure actually was, as far as evidence goes:** `EXIT_PREREQ` means a B00 preflight check FAILED, and on this cell only two can — **disk** and **network** (git present, root fine, `--skip-claude-check` makes Claude Code a warn, and Node is design-only so `ok`). Both are runner conditions, neither is #193's content, and **#192 with the same base and pin passed the same cell**. Not reproduced locally: two consecutive bootstraps on `c1f1fe8` in a throwaway clone on macOS/node 24 both exited 0, the second `ok (cached)` throughout. **Which of disk or network it was cannot be recovered** — that log is gone, and that is the whole point of this row. **Raised, not decided:** B00's network probe has no retry, while ARC-03-C1 gave the corpus checkout one for exactly this class of transient — whether preflight should retry is a product decision for the architect |
| ARC-09-C42 | **The PowerShell linter could not say which kind of failure it had, and its own guard proved the diagnosis wrong.** #198's `windows launcher` cell printed three lines — `Value cannot be null. Parameter name: source` and exit 1 — with no version, no path and no findings table, so a **bad module release**, a **partial extraction** and a **real finding about `bootstrap.ps1`** were indistinguishable. The first cause named was *"the module did not install/import"*; **the step's own `Get-Module -ListAvailable` guard refutes it** — that guard exits 1 with its own sentence, and the sentence never appeared, so the manifest WAS there. `-ListAvailable` reads a manifest; it does not load the assemblies that threw | **Fixed by making the step able to distinguish its three failures.** It now `Import-Module … -ErrorAction Stop` (a half-extracted install fails HERE, with its own sentence and the module list printed), records `PSScriptAnalyzer <version> from <path>` **on every run including green ones**, and wraps the analysis in try/catch so a crash exits **3** with *"this is a CRASH, not a finding about bootstrap.ps1"* while findings exit **1** with their count — they shared exit 1 before, which is ARC-06-C10's shape in a workflow. **`-RequiredVersion` is pinned to `1.25.0`, and the number is evidence rather than a choice.** It is what the **first green run after the version-printing landed** actually resolved: run `35186868730`, job *windows launcher*, which printed `PSScriptAnalyzer 1.25.0 from <the runner's user modules path>` and `bootstrap.ps1: no findings`. Verified in that job's log rather than taken from the report of it. **Both install calls carry it** — the first and the retry — because a retry that fetched a different version would defeat the pin on exactly the run where it mattered. The version line stays: when the pin is raised, the next run says what it got, and the raise is evidence the same way this one is. **Why it could not be pinned in the same change:** the ruling asked to pin *"whatever the last green run resolved"*, and that value **does not exist in any log** — the last green job's five PSScriptAnalyzer mentions are all echoed script source, and the step produced **no output at all** on success. Pinning to today's latest would risk pinning the very release that may have crashed, on no evidence either way. The version printing lands first; **the first green run after this makes the pin evidence-based**, and that is a one-line follow-up. **Three negative controls, and the third is the finding:** removing the import and removing the crash sentence each fail the test, but the first attempt at the exit-code control **did not fire** — it had replaced the first `exit 3` in the file, which is prose in ARC-09-C40's comment, not the step. Re-aimed at the step it fails with *"found: 1, 2, 1, 1, 1"*. **A control that does not fire has to be investigated rather than accepted**, because a silent control and a passing one look identical — the same shape this arc keeps finding. The behavioural half is honestly unverified: breaking the import for real needs a Windows runner, and `pwsh` is not on the machine this suite runs on, so the assertions are structural and say so. **And then the fix broke the job itself, which is the second half of this row.** #201's first head failed to **parse**: `ParserError … TerminatorExpectedAtEndOfString`. GitHub writes a `shell: powershell` step to a `.ps1` with **no BOM**, Windows PowerShell 5.1 reads it in the ANSI code page, and a UTF-8 em dash (`E2 80 94`) decodes there as `â€"` whose `0x94` is the curly double quote `”` — **a string terminator 5.1 accepts**. So my new sentence *"failed — this is a CRASH…"* closed its own string and the parser died before a line ran. **One line broke it and ten did not:** the other ten em dashes inside 5.1 steps are `#` comments, where a stray quote is harmless because a comment runs to end of line, and four of those had been shipping green for months. The rule shipped is still the whole block — **ASCII only inside any `shell: powershell` step** — because *"ASCII except inside comments"* is a rule nobody can apply while writing. `pwsh` (7) reads UTF-8 and is deliberately exempt; 5.1 is what that job exists to test. **This is the structural control the docblock said was missing, and it needs no Windows runner.** Three controls: the em dash back in the STRING fails naming `ci.yml:1141`; an em dash in a COMMENT inside a 5.1 step also fails, which is the rule being deliberately wider than the defect; and an em dash in a BASH step does **not** trip it — the scope control, proving the rule does not over-reach into the ten `bash` and eight `cmd` steps that carry em dashes happily |
| ARC-09-C43 | **`upgrade` tells a detached HEAD to return to a branch it may never have been on.** `upgrade.mjs:401` prints *"HEAD is detached at <tag> — return with: git checkout ${branch.branch ?? 'main'}"*. The fallback fires **exactly when the information is missing** — HEAD was already detached, so `branch.branch` is null — and that is the owner's case on rc.4: they upgraded **from the `v2.0.0-rc.2` tag**, and were told to check out `main`, which is not where they were. Following the advice moves them somewhere they have never been, silently | **Open — agreed a defect, checked in the source rather than taken on report.** The sentence is right whenever a branch is known and wrong in the one case the fallback exists for. Shape when it is done: capture where HEAD actually was **before** the checkout — the branch if there was one, otherwise the tag or sha `git describe` gives — and name that. A fallback that guesses `main` is a guess presented as instruction, which is the class this arc has been closing all week |
| ARC-09-C44 | **The upgrade plan promises steps the run then skips.** `stepsThatWillRun` computes from the INPUTS table and `git diff HEAD <tag>` and **never consults the mode** — so on a design-only checkout it can list `B04 deps` and `B08 verify` under *"steps that will re-run"*, and the bootstrap it then invokes skips both, because `runsWhen` is false for them in that mode. Observed on the owner's rc.2→rc.4 upgrade, and **again on rc.4→rc.5**: the plan named `B04 runtime dependencies (package-lock.json changed)` and `B08 the doctor (…)` under *"steps that will re-run"*, and U6 then printed `[B04/09] deps … skipped (design-only)` and `[B08/09] verify … skipped (design-only)`. Two releases apart, the same two steps, so it is neither a one-off nor something a release quietly fixed. **The mode is available at plan time** — `finish()` reads it as `state?.mode === 'live' ? 'live' : 'design'` and hands it straight to the bootstrap it spawns — so this is not a thing the plan could not know | **Open — agreed a defect.** A plan is a promise about what is going to happen, and one that names two steps which then do not run teaches a reader to discount it. Shape when it is done: filter the listed steps by the mode the upgrade is about to pass to the bootstrap, or mark them *"skipped in design-only"* rather than dropping them silently — the second is probably better, since a reader upgrading a design-only checkout may want to know that live mode would re-run them |
| ARC-09-C45 | **The fix for ARC-09-C39 shipped, and the identical signature came back with it in the tree** — printed by ARC-09-C38's instrument, which is the one part of the chain that did its job. `d5b3f0e`, `test (ubuntu-latest, node 24)`: `snowarch-bootstrap-IKQFnB (9 entries — not removed; made by: no owner record)`, child exit 0, and `(neither stream mentioned the sweep)`. The teardown fix landed 2026-09-17 (`339471d`) and this ran on 2026-09-18, so the explanation written into `temp.mjs` — that the old hook struck a failed removal off the register and deleted its owner record — is retired as the whole story. The same code under `tools/snowarch` was **green on `29e039b`**, so it is intermittent on that cell and a rate proves nothing either way | **Fixed at the one path the four facts leave standing, with the two rivals ruled out on evidence rather than on taste.** **(c) a creation path that never reaches `trackTempDir` — dead by scan:** exactly three creators of a `snowarch-bootstrap-` fixture exist (`helpers/workspace.mjs:19` and the two probe bodies in `fixture-cleanup.test.mjs`), all through `tempDir`; every other raw `mkdtempSync` in the engine tests uses a different prefix and could not produce this name. **(a) something recreated it after the teardown — dead on the instrument:** `recordOwner` writes the `.owner` file at **creation**, so anything that recreated a fixture would have left a record beside it, and this survivor had none. It survives only if `recordOwner` **also** failed silently — two independent failures, against one for (b). **(b) a removal that returned without removing — the only single-failure route, and it fits all four facts exactly:** both places that delete an owner record and both that would have reported the survivor are downstream of `remove` reporting success, so one unverified `return null` disarms every instrument at once and leaves precisely a populated directory, no record, a clean exit and a silent sweep. **The fix is the check that was missing:** `rmSync` returning is a report, not a fact, so `remove` now confirms the path is gone and otherwise returns `ESURVIVED`, shaped like the errno errors the callers already print — the handling was always right, it was just never reached. `rm` became a parameter because no filesystem produces a successful-but-ineffective removal on demand, and a defect that can only be waited for is a defect with no test. **NOT REPRODUCED ON DEMAND, said plainly: 240 concurrent probes fired nothing** — 48 on macOS/node 24 (APFS) and 192 on Ubuntu 22.04/node 22 (ext4), the filesystem match being the closer one. ubuntu/**node 24** was not reachable locally: that snap needs GLIBC 2.38 and the VM is 22.04 with 2.35. So this is the cause the code makes possible and the evidence names, not the cause caught in the act. **Control:** with the check reverted, exactly one test fails, and it is the new one; the control's other half proves the old shape was blind to the same survivor, and that a real removal is still reported as success |
| ARC-09-C46 | **U7 printed a number and not what it counted.** The owner's rc.4 → rc.5 upgrade ended `[U7/7] doctor` → `DOCTOR: 31 ok, 3 warn, 1 fail (4 skip)` and the Mode line, nothing else, so they had to run `./snowarch doctor` a second time to learn the failure was **E-27**. The lines were in the parsed report the whole time — `commands/upgrade.mjs` reads `report.summary` and drops `report.checks` with the rest of it. This is **ARC-08-C3 one command over** (*"a count is what you write when you have the list and do not print it"*), and B09 had already closed it for the bootstrap | **Fixed with B09's renderer, not a second one.** `failureLines` was already exported and already the right sentence, so it is imported rather than re-typed — a second copy of a sentence is the thing this repo keeps removing. It is now one implementation (`nonOkLines`) behind two accurately-named views: `failureLines` stays fail-only and **its output is unchanged**, which a test asserts in both directions so that fixing U7 could not quietly alter what the bootstrap prints. **The tally moved INSIDE the renderer, and that is the part that closes the row rather than patching it.** The defect was that the count and the list were *separable* — a caller could take the number and leave the list, and for one release that is exactly what it did. `doctorLines(report)` returns both as one array, so there is no longer anything to skip. **Warnings are printed too, which is wider than the brief and deliberate:** ARC-09-C47's evidence is an E-28 **warn** printed by this very step whose wording nobody can now recover, because the tally was all that reached the transcript — a run of this command is evidence about a warning as often as about a failure. Failures render first so a reader scanning for what stopped them does not pass two warnings to reach it; `ok` and `skip` are not rendered, because 31 ok lines would bury the one that matters. **Controls, and they are aimed:** with the list dropped again all three new tests fail and the five pre-existing ones still pass; a healthy report is still exactly one line, so a clean upgrade grows no noise; and `null`, `{}` and a summary with no `checks` array each return what U7's old `if (report?.summary)` guard did, because moving a guard has to bring its behaviour with it |
| ARC-09-C47 | **After an upgrade, `E-28 release currency` reports the upgrade's own assertion back to the user, for 24 hours, as a measurement.** The owner's `.local/upgrade-check.json` on the rc.5 checkout reads `"localTag": "v2.0.0-rc.5"`, `"latestTag": "v2.0.0-rc.5"`, `"behind": false`, `"checkedAt": "2026-09-18T19:21:27.839Z"` — and **E-28's own rule could never have written it**: the live path keeps only NON-prerelease tags (`sortTags(names).filter((t) => t.pre === null)[0]`) and returns `skip` when there are none, so an rc can only reach `latestTag` from `upgrade`. Both of that command's writers put one there — `writeUpgradeCheck(root, { latestTag: latest, localTag, behind, remote, now })` in the plan phase, and `writeUpgradeCheck(root, { latestTag: latest, localTag: target, behind: false, remote, now })` in `finish()`, whose `behind` is a literal rather than a comparison. The standalone doctor 1 h 40 m later duly printed `E-28 ok release currency: up to date (v2.0.0-rc.5) · last checked 2026-09-18T19:21:27.839Z`. **Two definitions of "behind" share one field and the last writer wins:** the upgrade's is "relative to the target I was asked to move to, prereleases included", E-28's is "relative to the newest release", and only the second is what the sentence `up to date` claims. **The banner reads this file too**, so the claim outlives the terminal it was printed in | **Open — the mechanism is established, and it is wider than one overwrite.** `finish()` runs U7's doctor with `--no-cache`, but that flag only suppresses `.local/doctor-last.json` (*"--no-cache do not write .local/doctor-last.json"*) and does not reach this cache — so E-28 calls `needsRefresh`, finds the record the plan phase wrote minutes earlier against a 24 h `REFRESH_AFTER_MS`, and takes the **cached branch without measuring anything**. That is the real defect: an upgrade does not merely overwrite E-28's answer, it **silences E-28 for a day**, and every doctor run in that window repeats the upgrade's assertion. It also settles the U7 tally's third warn, which was recorded as unidentified: with the plan phase's `behind: true` in the cache, E-28's cached branch returns `warn(<tag> available — run ./snowarch upgrade)`; `finish()` then writes `behind: false`; and the tallies move exactly as observed — `31 ok, 3 warn` → `32 ok, 2 warn`, with **skip unchanged at 4 because the live path, and therefore its no-release-tags `skip`, was never reached in either run**. **Shape when it is done:** either the upgrade stops writing a field E-28 owns the meaning of — write its own `localTag`/`checkedAt` and leave `latestTag`/`behind` to the check that defines them — or the record carries WHO wrote it and E-28 declines to report currency from a record it did not measure, which is the same distinction as `refreshed: true/false` already drawn in its own `data`. Either way the sentence `up to date` must not be reachable from an assertion. **CLOSED, and the shape had to be three checks rather than one, because the two sightings are different failures.** A SECOND sighting arrived from the owner's interrupted rc.5 → rc.6 upgrade: `E-28 warn release currency: v2.0.0-rc.6 available` on a tree that WAS rc.6, because U5 moved the checkout and `finish()` never ran, so the plan phase's `localTag: rc.5, behind: true` outlived the tree by an hour. The first sighting AGREED with its tree and was still wrong; the second DISAGREED. A fix for either leaves the other standing. So: (1) `finish()` stamps `source: 'upgrade'` and writes **no** currency verdict — it knows which tag it moved to and never asked what exists; (2) the cached branch refuses a record whose `localTag` disagrees with `git describe`, which catches the interrupted case; (3) it refuses a `latestTag` that is a **prerelease**, which E-28's own live rule never stores — and this is the only one of the three that works on records written by a build predating the fix, since they carry no marker. **THE PLAN PHASE KEEPS ITS VERDICT, deliberately: it fetched the tags and compared, so its `behind` is a measurement, `upgrade --check` exists to refresh exactly that record, and the banner's nudge reads it. Do not strip it for symmetry with `finish()` — the asymmetry between a measurement and an assertion IS this row.** Omitting `latestTag` instead of marking the source was considered and rejected: absence already means *a check ran and found no releases* (ARC-09-C32), so it would have produced a different false statement, quieter than the one being removed. **`--no-network` runs checks 1 and 3 only** — that flag's contract is that the check spawns nothing, and `git describe` is a spawn even though it is local; the offline path says nothing about a disagreement it did not look for. **Controls, one per check, each failing its own test alone — and the second one was VACUOUS at first:** written with the owner's real record, whose `latestTag` is a prerelease, it passed whether or not check 2 existed, because check 3 caught the record first. Disabling check 2 failed nothing, which is how it was found; the test now uses release tags, which only check 2 can catch |
| ARC-09-C48 | **A test file added to `tests/upgrade/` would run nowhere, and both suites would report health.** `tests/run.mjs` walks the tree so a new test is picked up without anybody remembering to add it, and excludes exactly one directory — `tests/upgrade/`, whose harness builds a world per case and needs a full-depth checkout with tags. The `upgrade-e2e` job that covers it names its three files **by hand**. So a fourth file there is excluded from `npm test` by the walk and absent from the job's list, and it passes both by being in neither. `run.mjs` already promises otherwise in prose — *"an exclusion is a thing that rots, so it is a LIST of one with the job that covers it named: anything added here that no job runs is documentation, which is what the recursive walk above exists to prevent"* — and the walk it relies on is precisely what cannot reach inside the directory it is told to skip. **Found while reporting gates for #206:** `npm test` read 1378 before and after that PR merged, though it added three tests; they were real and passing, and simply not in the suite whose number was being quoted | **Closed with a test, not by making the job walk.** The job keeps its explicit list — `run.mjs`'s own opening states why a glob is refused in CI (*"on Windows npm runs scripts through cmd.exe, which does no glob expansion, and whether `node --test` expands a glob argument itself varies by Node line"*) — and the list is now **verified** rather than trusted, which is the difference between a memory and a checked memory. It enforces `run.mjs`'s stated guarantee at the one place its mechanism cannot: every `.test.mjs` under the excluded directory must be named by the job that claims to cover it. **Both directions, because the rot faces both ways:** a file on disk that no job names fails BY NAME with the two ways to fix it, and a list entry whose file is gone fails too — a list that outlives what it describes is the same defect, and would otherwise surface as a missing path on three OSes. **Non-vacuity on both sides:** an empty parse and an empty directory agree perfectly, and that agreement is exactly what must not read as health, so the directory count and the parsed count each carry a floor; a renamed step makes the parser return `null` rather than silently matching some other job's file list. **Controls:** the predicate is driven directly for both directions without planting anything, since a test that created a file to prove a comparison works would be testing the filesystem — and then the real test was run against a genuinely planted fourth file, which failed it by name, exactly once, and passed again once removed |
| ARC-09-C49 | **rc.10 was cut locally with every gate green and went red on all three `verify` cells.** One test: `no assertion spells the current version of record (ARC-09-C12a)` — `tests/semver.test.mjs` used `2.0.0-rc.9` / `2.0.0-rc.10` as its two-digit example, *the case that blocked rc.10*, and on the release commit that string IS the version of record. Green on develop (`2.0.0-dev`), red on every tag, by construction — a test about release ordering coupled to the release it was written during. **Diagnosis, which is the half worth keeping:** `npm test` WAS in the gate plan. The gate plan runs at `release.mjs`'s gates stage, **before** the writes; only two checks ran after them (`version-consistency` and the contract gate, ARC-09-C12b). So the suite tested a tree whose `package.json` still said `-dev`, and `version-literals.test.mjs` — whose own message reads *"a literal here fails on the release commit itself"* — was the one check never run against the release commit. It could not have caught this locally; it was not asked. | **Fixed in both halves.** The semver tests use a `9.x` series this product will never be at, said at the top of the file so the next author does not reach for the real incident's numbers again; the specification's own example stays `1.0.0-…`, which is the spec's and never ours. And `release.mjs`'s post-write list now runs `npm test` — what `release.yml`'s verify runs — so a tree CI would refuse cannot be tagged locally first. It is the slowest gate and it now runs twice; a release is rare and a dead tag costs more, and the pre-write run keeps its own value: it fails before anything on disk is touched. **The post-write test asserts what the manifest SAID AT EACH CALL** (`2.0.0-dev` then `2.0.0`), not the call order — ordering in a list proves the list, this proves the suite saw the tree that ships. **Controls:** a literal of the version of record planted in a test file → C12a fails; the post-write `npm test` removed → both C49 tests fail. **Also found, and not fixed here:** the same sweep against `2.0.0` (the final release) reports **19 test files**, measured with the product's own `literalLines`. Cutting 2.0.0 will hit this again, nineteen times, and renaming literals will not be the answer at that size |
| ARC-09-C50 | **The version sweep matched a SUBSTRING, so at a release triple it would flag every line naming any prerelease of it.** Found while measuring the 2.0.0-final exposure rather than by a failure: `literalLines` was `line.includes(version)`, and `v2.0.0-rc.6` contains `2.0.0`. Against the record version `2.0.0` the sweep reported **18 files, 172 lines**, of which **21 lines and four whole files** named only `2.0.0-rc.6`, `2.0.0-dev` or `2.0.0-test` — versions that are not the record and never will be. Those are not what C12a is for, and a guard that cries at 21 lines nobody can act on is a guard people learn to wave through | **A version boundary, not a word boundary**, because `v` is part of how a version is written and a digit is not: not preceded by a digit or a dot (`12.0.0` is a different version; `v2.0.0` must still be caught), not followed by `-` or a digit (`2.0.0-rc.6` and `2.0.01` are different versions). **A trailing dot is deliberately allowed** — `released in 2.0.0.` ends a sentence, and excluding it would make the sweep MISS a real literal; between over- and under-flagging this errs toward over, because a false positive is an argument and a false negative is a dead tag. That choice cost two lines against my own earlier estimate — predicted 151, measured **153** — and the two are sentence-ending literals the estimate wrongly dropped. The rule holds when the record is itself a prerelease: `2.0.0-rc.11` does not match inside `2.0.0-rc.110`, which is rc.10's defect one turn on. **Measured effect:** at record `2.0.0`, 18 files/172 lines → **14 files/153 lines**; the remainder is the 9.x fixture migration, not this row. `literalLines` has exactly one consumer, checked rather than assumed. **Control:** the substring match restored → `ARC-09-C50 — the sweep matches a version, not a substring of one` fails by name |
| ARC-09-C51 | **The tag-measurement harness lived in a cache directory, and three of its own properties were the ones it checks for everybody else.** The harness re-measures every row's control on a release tag in a clean clone from GitHub — the stranger's path — and it was never committed, so the instrument that judges a tag was itself unversioned and unreviewed. Measuring **v2.0.0** with it found one FAIL out of 37 controls, and the FAIL was **the harness's**: the ARC-09-C49 control planted the literal `2.0.0-rc.11` to prove the version sweep catches the tag's own version of record. That is correct at an rc tag, where the version of record IS `2.0.0-rc.N`, and WRONG at a final one — on v2.0.0 the record is the bare `2.0.0`, `2.0.0-rc.11` is a different version, and the sweep declined to flag it **because ARC-09-C50 made it decline**. So the harness reported FAIL against the product being right, which is the one thing a control may never do. Two more, found by using it: `main()` ran at MODULE SCOPE and three `sys.argv` reads preceded it, so importing the file to read its `CONTROLS` either raised `IndexError` or **started a network clone** — and ARC-08-C25's own control is *every exporting script can be imported without running*, which this file carried for others while failing it. And it never passed `-u`, so a long run redirected to a file shows NOTHING until it exits: measured, a v2.0.0 run sat at 0 bytes for four minutes while `npm ci` worked, which is indistinguishable from a hang | **Committed as `scripts/acceptance/tag-controls.py`, with the three defects fixed and the measurement recorded.** The C49 plant is **derived** from the clone's `package.json` through a `@@VERSION_OF_RECORD@@` placeholder resolved when the patch is applied, so it is right at an rc, right at a final, and needs no edit at the next cut — the same read-it-never-spell-it rule as C12a, applied to the harness. Argument parsing moved into `parse_argv()` and the call behind `if __name__ == '__main__':`, so an import needs no argv and runs nothing; verified by importing it with `sys.argv` empty and confirming zero `rc-controls-*` work directories appear. The header now states what a reader must know BEFORE running it: hand-run only, never in CI, that it deliberately degrades a working tree, that `-u` is not decoration, which two positive checks write outside the clone (`~/.rc-controls-probe` and `/tmp/rc-controls-linked-<pid>` — a TMPDIR-under-HOME check has to be under HOME) and that **an interrupted run leaks its work directory**, tens of MB once the clone starts, which is a caveat on any *TMPDIR leftovers* figure quoted nearby. `__pycache__/` is now ignored, because importable-by-design is what writes one. **The v2.0.0 measurement, from a fresh GitHub clone:** `describe --tags --exact-match` = `v2.0.0`, HEAD `c15c9c4` (tag object `3d82e3d` — peeled vs annotated, the C16 distinction); gates under `submodules: false`, CI's own condition — lint, type-check, contract, gen:check, test, upgrade-unit, commitlint **all exit 0**, tree dirty 0, TMPDIR leftovers 0; **5/5 positive checks PASS**; **37 controls, 36 PASS, 0 ANCHOR NOT FOUND** — every anchor still resolves on the tagged tree, and re-verified statically against `develop` (5e3dbff) as well, 37 patches, 0 missing. **Controls for the new tests:** a `run:` line naming the harness added to `ci.yml` → *no workflow references the acceptance harness* fails; `main()` returned to module scope, and separately a module-level `sys.argv` read restored → *the acceptance harness says it is hand-run, and can be imported without running* fails on each. That second test also failed on its own first run, for the reason `validation-tests-shape` collapses whitespace — the header wraps, so `DELIBERATELY DEGRADES A\nWORKING TREE` contains the sentence and not the string; it matches against a collapsed copy now **A FOURTH defect, found by rebasing this PR onto the tree that fixed E-28 — and it is the same class as the C49 plant.** The ARC-08-C30 control anchored on the literal `"ok": 27,` in `snapshot-darwin.json` to break the tally. E-28's fix legitimately took darwin from 27 to 28, so the anchor went missing on the very next tree and the harness reported **ANCHOR NOT FOUND** — a finding about itself, dressed as one about the tree. Any count in an anchor is a value that moves when the product correctly changes. So the patch derives it: `@@SUMMARY_OK@@` and `@@SUMMARY_OK_MINUS_1@@` resolve against the snapshot the patch targets, giving a tally that is always exactly one off whatever the truth currently is. **Both halves of a patch are resolved now**, the anchor as well as the replacement — resolving only the replacement would count a placeholder literally and report ANCHOR NOT FOUND for every control that used one. Re-verified on the rebased tree: 37 controls, 37 patches, **0 ANCHOR NOT FOUND**. Three of this row's four defects were the harness spelling a value it should have read, which is ARC-09-C12a's lesson arriving in the instrument that measures it. |
| ARC-09-C52 | **The version sweep could not see a version spelled as a regex, and that blind spot bit twice.** `literalLines` searched for the version as a LITERAL STRING, so `/^## 2\.0\.0/m` was invisible: the characters on the line are `2\.0\.0`, which does not contain `2.0.0`. **First bite:** `tests/predecessor-notice.test.mjs` pinned the record version in an escaped regex — the sweep was silent, and the file needed an exemption for its failure MESSAGE while its actual assertion sailed past unnoticed (ARC-09's 9.x migration). **Second bite:** `tests/install-page.test.mjs` asserted the README head matched `/2\.0\.0/` — the CURRENT version, which the post-release bump is GUARANTEED to move — and it broke the moment `develop` went to `2.0.1-dev`, with both sweeps silent through it. Two bites make it a class; a third would be a dead tag, because the release commit is where a version literal fails and that run cannot be retried | **One version, two spellings, each turned into a pattern that matches itself.** `escapedSpelling` returns the TEXT a source file carries (`2\.0\.1`), and `asPattern` escapes it once at the point of use — the first version of `escapedSpelling` returned the PATTERN instead, and the control built from it planted the wrong characters and **passed for the wrong reason**, which is the whole defect class in miniature. The boundaries are unchanged and hold on both spellings for the same reasons: `12\.0\.0` is a different version, `2\.0\.10` is, `2\.0\.0-rc\.6` is (ARC-09-C50, in the escaped form too). Measured: **0 governed lines** at target 2.0.1 on this tree; at a 2.0.0 shape the extension makes four previously invisible lines visible. **NO ALLOW-LIST FOR THOSE FOUR, and the reason is a property rather than a preference.** They name 2.0.0 as a RELEASED FACT — the successor's first `## 2.0.0` CHANGELOG heading, the `## Before 2.0.0` frozen heading, the notice's prose — and `scripts/lib/release/preflight.mjs:66` refuses a version *not greater than the latest tag*, so the version of record only ever moves FORWARD and a line naming a release that has already happened can never be the version the sweep asks about. An allow-list here could never fire, and it would be exactly the place somebody parks a reference to the version BEING CUT — the one case that must fail. So `RELEASED_FACTS` binds each site to its reason and a test asserts the property: every entry names a version strictly behind the record, and the sweep is quiet on it. **Controls:** the escaped form dropped → `C12a — the escaped spelling is caught, and the control is a regex literal` fails by name; and the REAL regression re-planted — `install-page`'s old `/2\.0\.1/` line — fires `no test file spells the version the NEXT release will carry`, which is the proof this closes the hole that actually bit rather than a hole shaped like it **Two findings on the first cut of this row, both the architect's, both additive.** (1) **The binding was hollow.** The property test checked that each `RELEASED_FACTS` entry had a reason, that its file existed, that the version it names is behind the record, and that the sweep is quiet on it — and **never that the site still names the version at all**. Re-pointed at `tests/semver.test.mjs`, whose only mentions are comment lines, every assertion passed: it is the stale-binding case the `is gone` check was written for, one step later, and it is how an exemption outlives its reason. Each entry now asserts `literalLines(content, names)` is non-empty — *`<file>` no longer names `<version>` — this binding is stale*. **Control:** the entry re-pointed at a file with no hits → `C12a — a reference to a PAST release cannot fire…` fails by name with that message. (2) **The harness had no C52 control**, while C49 and C50 are in it and C51's own promise is that every row's control is re-measured on a tag. Two are added, from opposite sides of the same hole: the escaped spelling dropped from `forms`, and **the regression that actually bit re-planted** — `install-page`'s escaped pin of the record version. The plant is DERIVED from `@@RELEASE_TARGET_ESCAPED@@`, the release target with its dots escaped, because the NEXT-release sweep asks about the TARGET: spelled, or derived from the version of record instead, it would be right at a final tag and silent at an rc — the C49 mistake this harness has already made once. The harness now carries **39 controls; re-verified statically against this head: 39 patches, 0 ANCHOR NOT FOUND**, and both new controls fire through the harness's own resolver. **The `37` figures in ARC-09-C51 are deliberately left alone**: they are what was measured on v2.0.0 and on the rebased tree at the time, and a record edited to match today is not a measurement any more. |
| ARC-09-C53 | **U7 printed the JSON boundary's redaction mask to the user's own terminal.** The owner's sitting (2026-09-23): U7's closing line read `Mode: live — instance=<label> (<label>) preset=custom — doctor 2026-09-24 14 ok` while `./snowarch doctor` a second later printed `instance=pdi (pdi)`. **`<label>` is not an unfilled template** — it is `LABEL_MASK` from `doctor/json-boundary.mjs`, and masking `--json` is deliberate and RIGHT: that is the form which travels, an issue template asks a stranger to paste it, so labels and hosts leave as `<label>` / `<host>` (ARC-08-C1/ARC-10-S10, masked BY VALUE across seven fields). The defect is that U7 ran `doctor --json`, parsed the masked stdout, and printed its `modeLine` to the one audience that OWNS the words | **U7 reads the copy meant for this machine.** `doctor/index.mjs` caches the report at line 331 — **before** `maskForJson` runs at 514/543 — and U7's spawn already passed `--write-cache`, so the unmasked line was on disk and already asked for. `readCachedModeLine()` reads `.local/doctor-last.json`; no second doctor run, and no reconstruction of a line the doctor owns (`modeLine` carries the preset and the doctor's own tally, which this command does not know and must not invent). **No fallback to `report.modeLine`:** a mask reads as a value, so it is worse than silence — there is a case for the empty-cache path asserting that nothing masked reaches the terminal. **Survey by PATH, and it cleared four of five:** the doctor's own text path is unmasked; `./snowarch mode` builds the line with `modeLine()` from state; the SessionStart hook reads `cache.modeLine` — the same unmasked cache this fix now uses; and **B09 was safe for the right reason**, taking only the tally (numbers) from the JSON and the instance from unmasked state, which is exactly the split U7 got wrong. **Tests written FIRST and watched to fail**, driving `finish()` — the step that prints the line — through its own injected `run`/`log` rather than a formatter; `finish` is exported for that. **Control:** the read reverted to `report.modeLine` → both cases fail by name. **RAISED, NOT FIXED HERE:** masking is by VALUE, so the two check `detail` strings that name the label are masked too, and `nonOkLines` prints `c.detail` for every fail/warn — so a non-ok check naming the instance (e.g. SV-03's `instances: pdi: …`) would reach the terminal masked through the same path. It is NOT a drop-in to read those from the cache: `writeReportCache` stores `{id,status,detail,remedy}` and drops `title`, which `nonOkLines` prints. Left for its own row rather than widened silently **AND THE FIX OPENED THE SAME DEFECT ONE LAYER DOWN — the architect's finding, and it is ARC-07-C9's class arriving inside the change that cites it.** `readCachedModeLine` had no notion of WHICH run wrote the file. Every bootstrapped tree carries a cache from an earlier doctor, so when U7's own doctor FAILED — the upgrade that broke something, the moment the closing line matters most — U7 closed with yesterday's line: measured on a probe, `Mode: live — instance=old-pdi (old-pdi) preset=custom — doctor 2026-09-23 12 ok`, tally and all, rendered as this run's answer. I had praised B09 in this very row for taking the tally from the run and the label from state, and then shipped the recorded-as-fresh half of exactly that split. **The read is bound to this run by four conditions**, each a way the line could belong to a different one: the doctor exited 0, its report parsed, it reported no `cacheError` (it tried to write and could not), and the cache is stamped at or after `startedAt`, taken from the injected clock **before** the spawn. Otherwise silence. **`>=`, not `>`:** the doctor stamps the cache from its own clock and a run landing on the start instant is this run — there is a case for that boundary, because an off-by-one here would discard every fast run as stale. The writer's field was **checked, not assumed**: `writeReportCache` stamps `at` from `new Date(now())` at the doctor's write, which is what the comparison reads. **Controls:** the binding removed → the three staleness cases fail by name; the read reverted to `report.modeLine` → the two mask cases fail plus the `cacheError` one, which is the right overlap since a masked line is also not this run's honest answer | |
| ARC-09-C54 | **The 2.0.0 release commit's changelog never came back to `develop`, and the 2.0.1 cut would have shipped the damage twice.** `release.mjs` writes the released section on the release branch — for v2.0.0 that was c15c9c4, **not an ancestor of `develop`** (verified with `merge-base --is-ancestor`) — and runbook step 9 brought back only the version bump. So develop carried **no `## 2.0.0` section at all**, while its `## Unreleased` → `### Notes` was **byte-identical** to v2.0.0's notes (measured: develop's Unreleased body is a 1,915-line prefix of the tag's 1,932-line section, the difference being the generated groups and the trailer the release added). `writeChangelog` CARRIES the Unreleased block into the new section, so cutting 2.0.1 would have (a) published 2.0.0's notes under 2.0.1's heading and (b) published a changelog with no 2.0.0 in it. Neither is visible in the diff of the cut. `packages/snowarch/CHANGELOG.md` was stale the same way, still reading `## 2.0.0 — Unreleased` with *"the package version is `2.0.0-dev` until the release is cut"*, six days after it was | **Ported, dated, and guarded.** The `## 2.0.0 — 2026-09-24` section is brought over **verbatim from the tag** and placed where `release.mjs` would have put it; `## Unreleased` gets 2.0.1's own notes, hand-written, with a `### Fixed`/`### Internal` shape rather than prose — which is the file's own convention and is what keeps **C12c** non-vacuous, since that guard needs a development-shaped block to prove hand-written groups survive a release. The package changelog's heading is dated and its sentence moved to the past tense; the predecessor notice's `/^## 2\.0\.0/` link still resolves, asserted. **The guard is CONDITIONAL, like the ARC-10 tripwire:** when a `v*` tag is reachable, `docs/CHANGELOG.md` must carry `## <version> — <date>` **and** `## Unreleased`'s notes must not equal that section's — the carry-over shape, not merely a missing heading. No tag, no claim: it defers with the reason printed, because CI's `test` job clones shallow without tags and a guard that passed quietly there would be worthless. **Written first and watched to fail on develop as it stood. Controls:** the ported section removed → fails by name; 2.0.0's notes pasted back under Unreleased → fails by name. Runbook step 9 now says to port the changelog back in the bump PR, with this incident named |

### Acceptance

The acceptance pass against `docs/plans/06-ACCEPTANCE-PLAN.md` §9. One row per backlog item.

**This arc's items are a different shape from the five before it.** Three of the seven are *"a
throwaway run or PR was supposed to happen and was never recorded"* — not a missing test but a
missing **observation**. Two of those are settled by measurement here. The third was authorised as a
deliberately red pull request — and then **declined before it was opened**, because the premise
failed measurement: the file it names has no goto label, and breaking it is not isolated to the
cells the claim is about. No branch was ever created.

| Item | Outcome | Evidence |
|---|---|---|
| B09-01 — S08 AC: password-stdin on every platform | **rework** | `assert-password-stdin.mjs` had **exactly one caller**, in `windows-native` (`ci.yml`) — and nothing about its three properties (the secret never in argv, no socket opened, the store written 0600 with the password in it) is Windows-specific. A credential door tested on one platform is a door nobody has tried. It runs on the POSIX `node-cli` cells now. **The proxy half could not be written as asked:** the criterion wanted `lastProbe: null` in the store under `HTTPS_PROXY=http://127.0.0.1:9`, and there is no store to read — `instance add` always makes a reachability request (`--no-probes` skips the CAPABILITY probes, not that one), so the run ends `reachability: FAIL PROXY_UNREACHABLE — ECONNREFUSED` and `Nothing saved.`, exit 1. Measured here before it was written. That is better than what was asked for — an instance whose proxy does not connect must not be saved half-verified — so the proxy case asserts **that**, and `lastProbe: null` is asserted in the ordinary run where a store exists |
| B09-02 — S08 AC: no `git.exe` on PATH | **rework** | B00 says git is required and prints a remedy; **nothing had ever run the launcher on a PATH where git is genuinely absent**, so that sentence was written and never read back. `strip-git-bash.mjs` gains `--no-git` for one step only — a job-wide export would break every later step, since git is a prerequisite of everything else that cell does. **The criterion is stale twice:** the launcher says `git not found` (the version sentence is the other branch, for a git that is too old), and the floor is **2.34.1**, not the 2.25 quoted |
| B09-03 — S08 AC: the windows-native signal | **record — unproven, and the live proof declined with its reason** | The criterion asks for a PR that breaks `bootstrap.cmd` with a **goto label typo**, turning *"all three windows-native cells red while other cells stay green"*. A throwaway draft PR was authorised for exactly that experiment, and it was **held before it was opened**, because the premise fails twice. **There is no goto label:** `bootstrap.cmd` is a six-line wrapper — `@echo off`, `setlocal`, call `bootstrap.ps1` through PowerShell, capture `%ERRORLEVEL%`, a pause guard, `exit /b` — with no `goto` and no labels; the logic the criterion imagines is in `bootstrap.ps1`. **And breaking it is not isolated**, which is the one thing the experiment exists to show: it is invoked **eleven times across four jobs** — `bootstrap` 4 (its Windows cells), `windows-launcher` 5, `windows-native` 2, `eol` 1 — so a one-character break turns about a dozen cells red and *"other cells stay green"* is simply false. Re-aiming it at something only `windows-native` uses would be a **new experiment**, which deserves its own design rather than inheriting this one's authorisation; keeping the target and recording the real blast radius would **spend a full 54-cell run to learn what the grep already says**. So: unproven, recorded. The signal is asserted statically by `tests/workflows.test.mjs` (*"the exit-code check after a .cmd is reachable, proven by running cmd"*), and the live proof is declined for the reason above rather than quietly skipped. **This is the first of the thirteen unrunnable checks that would have cost a CI run to discover** — the other twelve were found by reading a file, and the difference is the argument for the preamble's rule |
| B09-04 — the workflow's wall time | **record — measured: 12 min 26 s against a 25 min cap** | The criterion also asks CONTRIBUTING's CI-matrix table to list measured durations per job; it had **Job / Cells / Shell / What only this job can answer** and no durations. Measured on **this PR's own green run** — run `34736611978` on `c186c88`, 54 jobs, `03:54:06Z` → `04:06:32Z` = **12 min 26 s** — rather than reused from an earlier tree, which is B05-06's lesson applied. The column is a **dated snapshot, not an assertion**: nothing enforces the numbers, because a test that failed when a job got thirty seconds slower would be a test about GitHub's week. The shape is the finding — `upgrade-e2e` 11.1, `test` 10.3 and `release-dryrun` 9.6 are almost the whole budget, and they run in parallel, which is why the wall time is half their sum |
| B09-05 — the Q-B fallback simulation | **record — not applicable** | The criterion asks the workflow to switch to a `windows-gitbash` variant "with Q-B fallback in force (env flag in a throwaway branch)". **Q-B passed**: `windows-native` exists and no fallback ever shipped, so the simulation branch and the env flag never existed and never will. The row was never marked not-applicable, which is why it survived to be asked. Recorded as not applicable, with the reason and the date, in the plan row and here |
| B09-06 — `version --json`'s five-key superset | **closed as found** | The assertion the reader could not locate **exists**: `tools/snowarch/tests/cli.test.mjs`'s `version --json is the shape the doctor will read`, *"version --json is the shape the doctor will read"*, asserting ARC-06-S02's five keys present and unchanged in name and type — with a comment stating the superset reasoning outright: *"a consumer written against the old object still reads the same values out of the new one."* It was missed because the comment says **FIVE KEYS**, not "superset", and it lives in `cli.test.mjs`, not the `version-tag.test.mjs` that was searched; the `contractMatches` half is at `version-tag.test.mjs:214-219`, as the row itself says. No new test |
| B09-07 — the release under pwsh on Windows | **rework** | The cell ran `shell: bash`, and on windows-latest **`shell: bash` is Git Bash** — so it proved the release works *with* Git Bash, which is the case the criterion excludes. That one cell is `shell: pwsh` now; ubuntu and macOS keep the bash step they have always run, so a failure would have been about the shell and not about three steps moving at once. **It did not fail: the pwsh cell passed on run `34735548240`, job `103666215729`.** The release runs on Windows without Git Bash, the criterion is met, the cell keeps pwsh — and **until this pass the cell had been proving Git Bash**, which is the case the criterion excludes. No `PLATFORM-NOTES.md` sentence, no chore, no product change |
