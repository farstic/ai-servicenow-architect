# ARC-07 — Instance wizard, presets and credentials

Status: **COMPLETE 2026-09-10 — all 11 stories merged.** Everything provable without a live instance is proven and green in CI; the eight items that need a real PDI, a real legacy file, a managed Windows machine or a person reading are listed below and each names its row in `docs/spikes/OWNER-SITTING.md` · Stories drafted 2026-09-04, verified 2026-09-04 · Depends on: ARC-04-S01/S02/S03/S04/S10/S11/S12 (dependency prune, store module, presets, reload, audit writer, R-3 HTTP layer, `Probes` interface), ARC-06-S02/S05/S07/S08 (CLI skeleton, `.local/config.json` mirror, B06 slot + `--instance-file`, handshake helper), ARC-02-S09/S11 (`docs/MODES-AND-PRESETS.md` page, `/snowarch` skill skeleton), ARC-05-S05/S06 (generators, error-code registry), ARC-08-S01 (doctor JSON); ARC-00 S-02/S-04/S-16/S-19 verdicts; owner decisions D-04, D-05, D-06, R-2, R-3 · Blocks: ARC-08 (live checks reuse the probes and the cloud-sync fixture), ARC-10 (`import --from-legacy`)

## Goal

Give the user a guided path to add a ServiceNow instance "with the right permissions": non-secret choices collected inside Claude Code, the secret typed once with masked input in the user's own terminal, the instance proven by live probes before it is saved, the preset explained in plain language and enforced as six explicit flags, production protected by default, and the new instance usable in the same session without a restart.

## Why it exists

Closes P-03 (absent flags; presets always write all six), P-06 (preset vocabulary), P-23 (wizard stores secrets with default file mode, `npm link`, unquoted unscoped `claude mcp add`, never writes FLUENT, "save anyway"), P-25 (tool package pinned), P-34 (no credential in argv or `~/.claude.json`), P-38 (auth methods labelled honestly). The evidence base established that no in-window channel is safe for secrets (`01` §6.1), so the wizard's terminal half is the product's credential boundary.

## Scope

**In:** `snowarch instance add|list|test|remove|set-preset|set-flags|set-credentials|set-default|import` (thin wrappers over the server package's CLI module); masked input; URL validation; reachability probe with DNS / TLS-CA / proxy diagnosis (R-3); auth probes with bounded re-entry; per-preset capability probes; preset table and flag explanations (from ARC-05's generated table); the D-05 per-flag review screen (Propose → Review → Apply, principle 10); prod safeguards (`--ack-prod`, label confirmation); `--global` store and the D-04 cloud-sync-folder WARN; `--password-stdin`; the `/snowarch setup-instance` skill behaviour (AskUserQuestion flow, command printing, `--resume` with `snow_core_instances_reload` and doctor); `docs/MODES-AND-PRESETS.md` final text; `AUTHENTICATION_FAILED` runtime rule in the generated rule file; a live E2E suite behind `RUN_LIVE_E2E=1`.
**Out:** the OS-keychain backend, the client-credentials grant and the localhost secret form (roadmap `01` §17); any UI outside the terminal and the skill.

## Deliverables

- `packages/snowarch/src/cli/instance.ts` (store-backed commands; unit-tested with a fake REST layer) and the `instance` forwarder in `tools/snowarch/bin/snowarch.mjs` (S05, S06).
- Masked input module `packages/snowarch/src/cli/tty.ts` (`process.stdin.setRawMode`; backspace, Ctrl-C, paste) with an S-04-verified Windows path; `--password-stdin`; the no-TTY rule; secrets rejected on argv (S01).
- URL normalisation (`url.ts`) and reachability diagnosis (`reachability.ts`) with the `URL_*` / `ENV_REQUIRED` codes and the wizard-grade remedies for ARC-04-S11's six network codes (`DNS_FAILURE`, `TLS_CA_UNTRUSTED`, `PROXY_UNREACHABLE`, `PROXY_AUTH_REQUIRED`, `NETWORK_TIMEOUT`, `CONNECTION_REFUSED`) (S02); the probe library `packages/snowarch/src/servicenow/probes.ts`, bound to ARC-04-S12's `Probes` interface and reused by `instance test` and the ARC-08 doctor (S03).
- Wizard flow exactly as `01` §6.2 steps 6–7: label, environment (proposed `pdi` for `devNNNNN` hosts, otherwise asked), URL validation (bare https origin; `/api` rejected with reason; trailing slash stripped; HEAD reachability with DNS/TLS/proxy diagnosis), auth method (`basic`, `oauth_ropc` labelled legacy), credentials, `sys_user` probe with a 3-attempt re-entry loop on 401 and no silent retry, role hints on 403, `OAUTH_ROPC_DISABLED` hint, per-preset probes, atomic 0600 write, secret-free summary (S05).
- Preset proposal and the per-flag review screen (`preset-ui.ts`, S04) exactly as `01` §6.3 / D-05: `full` proposed for `pdi`/`dev`/`test` with every flag pre-set ON and annotated with its probe result (a failing probe changes the recommendation text, never the toggle); `read-only` proposed and capped for `prod` unless `set-preset --ack-prod` with the label typed; `--yes` / `--preset` / `--flags` for CI.
- `instance list` (label, environment, preset, default marker, last-probe results — usernames masked, never secrets), `test` (incl. `test --all --json` for ARC-06-S08's B08), `set-credentials`, `set-preset`, `set-flags`, `set-default`, `remove` (S06); `--global` store, precedence messaging and the cloud-sync WARN (`detectCloudSync()` in the store module plus the shared `cloud-sync-paths.json` fixture, S07); `import --from-legacy` (reads `~/.config/servicenow-mcp/instances.json`, migrates with explicit `FLUENT_ENABLED: "false"`, advises deletion — used by ARC-10) (S08).
- The `setup-instance` section of `.claude/skills/snowarch/SKILL.md` (S09): prerequisite check via `./snowarch doctor --json --section prereqs`, three `AskUserQuestion`s, URL/label in chat, printed command in the OS-appropriate form (`./snowarch …` on macOS/Linux/Git Bash, `snowarch.cmd …` in PowerShell/cmd), `--resume` branch calling `snow_core_instances_reload`, `snow_core_capabilities_read`, `./snowarch doctor --json`, printing the Mode line and the §2.1/§2.2 reminder; `allowed-tools` = ARC-02-S11's four entries (doctor commands, `cat .local/bootstrap-state.json`, `Read`) plus the four core `mcp__servicenow__snow_core_*` tools, nothing else; `metadata.version: 2.0.0`.
- `docs/MODES-AND-PRESETS.md` final; error-registry entries (ARC-05-S06's `ERROR_CODES`) feeding `docs/TROUBLESHOOTING.md` for `AUTHENTICATION_FAILED`, `INSUFFICIENT_PRIVILEGES`, `PROD_WRITE_NOT_ACKNOWLEDGED`, `OAUTH_ROPC_DISABLED`, URL-shape and network errors; the three `showInRule: true` runtime entries rendered into the generated rule file (S10).
- Live E2E suite `packages/snowarch/tests/e2e/instance-wizard.e2e.test.ts` and the nightly `e2e-live.yml` workflow behind `RUN_LIVE_E2E=1` (S11).

## Dependencies

ARC-04-S01 (prompt/colour libraries removed; `commander` kept), S02 (store module `src/store/`), S03 (presets, flags, `checkProdPosture`), S04 (`snow_core_instances_reload`, `snow_core_capabilities_read`), S10 (audit writer), S11 (R-3 `snFetch` + `classifyNetworkError`), S12 (`Probes` interface); ARC-05-S05 (rule file, PRESETS block) and S06 (error-code registry, TROUBLESHOOTING renderer); ARC-06-S02 (`snowarch.mjs` skeleton), S05 (`.local/config.json` mirror, `lib/cloud-sync.mjs`), S07 (B06 slot and `--instance-file` — interface-first, stubbed until S05 lands), S08 (`lib/mcp-handshake.mjs`); ARC-02-S09 (`docs/MODES-AND-PRESETS.md` page) and S11 (`/snowarch` skill skeleton); ARC-08-S01 (`doctor --json --section prereqs`) and S03 (E-24 prints `import --from-legacy`; E-25 parity-tests the cloud-sync fixture). Story numbers are as they stand in each ARC's `STORIES.md` on 2026-09-04. ARC-00 verdicts: S-04 (Windows masked input) before S01, S-02 and S-16 before S09, S-19 for the skill lint; per D-06, no story starts before ARC-00-S12 has moved ADR-0006 to Accepted. Owner decisions D-04 (store, auth methods, cloud-sync WARN), D-05 (proposal flow, prod cap), D-06 (terminal hand-off), R-2 (skill name), R-3 (proxy diagnosis).

## Acceptance criteria

- [~] **A fresh user with a PDI** runs `./snowarch instance add pdi --url https://devNNNNN.service-now.com --env pdi --auth basic --preset pdi-developer --default`, types username and password (nothing echoed; `ps` during the run shows no secret; the shell history contains no secret), reviews the per-flag screen, and receives the summary with all five probes `ok`; `.local/instances.json` has mode 0600, directory 0700, and contains all six flags as strings, `toolPackage: "full"`, `maxRecords: 100` (S01, S05, S11). *(deferred — the live run: OWNER-SITTING "The wizard against a real instance (ARC-07-S05)"; every exit path around it is proven in `tests/cli/instance.test.ts`.)*
- [x] A wrong password produces `AUTHENTICATION_FAILED — <the registry's meaning> Re-enter? (attempt 2 of 3)` *(the reason was the literal "wrong username or password" until the ARC-08-S10 review ruled it renders from the registry — narrower than "wrong, expired, or the account is locked" was a second definition of one condition)* and, after three failures, exits 1 with nothing saved — exactly one login request per attempt; the instance's user is **not** locked out (verify on the PDI: no lockout after the run) (S03, S05, S11). *(proven in `tests/cli/instance.test.ts`; the instance's own lockout policy is case 3 of the live suite — OWNER-SITTING "The live E2E suite (ARC-07-S11)".)*
- [x] Every configuration step is Propose → Review → Apply (principle 10, D-05): for `pdi`/`dev`/`test` the wizard proposes `full` with all six flags pre-set ON and annotated with their probe results; a failing probe changes only the recommendation text; any flag is toggleable; `--yes` accepts every proposal (S04). *(S04's review screen and its snapshots; `tests/cli/preset-ui.test.ts`.)*
- [x] `--env prod --preset full` in the wizard is rejected with the D-05 explanation (`PROD_WRITE_NOT_ACKNOWLEDGED`, exit 3); `set-preset prod full --ack-prod` asks for the label and then succeeds with `prodWriteAck: true`; the server loads it only then (S04, S06, S11). *(S05/S06; the server half is asserted against a spawned `dist/server.js` in `tests/cli/instance-manage-server.test.ts`.)*
- [x] An unreachable host is diagnosed as DNS / TLS-CA / proxy / timeout / refused with the exact remedy (R-3); the wizard offers no "continue anyway" (S02). *(S02's classifier and its remedies, from the registry.)*
- [~] Inside Claude Code, `/snowarch setup-instance` never asks for a password, prints a command identical to the one a user would type by hand, and `/snowarch setup-instance --resume` ends with `Mode: live — pdi (pdi) · preset pdi-developer · …` **without** restarting the session (S-02; fallback text otherwise) (S09). *(the command it prints is asserted byte-for-byte in `tests/handoff-command.test.mjs`; the skill itself is T-20/T-21 — OWNER-SITTING "T-20 and T-21, the four manual passes (ARC-07-S09)".)*
- [x] `./snowarch instance list` output pasted into a chat contains no secret and a masked username (S06, S11). *(S06's every-byte sweep; the masking lives in the serializer, so no printer can leak.)*
- [x] A checkout under OneDrive / Dropbox / iCloud Drive / Google Drive triggers `WARN STORE_IN_CLOUD_SYNC_FOLDER` before any store write (D-04); `--global` writes the per-user store and the project store wins when both hold a label (S07). *(S07; the provider list is one fixture three implementations answer to. Known Folder Move on a managed machine is OWNER-SITTING "Known Folder Move (ARC-07-S07)".)*
- [~] Windows (PowerShell 5.1 and cmd, no Git Bash): the masked prompt works (S-04) or `--password-stdin` is documented as the fallback in the same output (S01). *(deferred — the S-04 matrix is eight cells on a Windows machine: OWNER-SITTING "S-04, the masked-input matrix (ARC-07-S01)". `--password-stdin` is documented and tested as the fallback.)*
- [x] `import --from-legacy` migrates a legacy wizard store entry (fixture) including its flags and adds explicit `FLUENT_ENABLED: "false"` (S08). *(S08, against the committed fixture; a REAL 1.x file is OWNER-SITTING "A REAL legacy store (ARC-07-S08)".)*
- [x] Unit tests cover URL validation, preset expansion, dependency rule, probe result mapping, prod cap; no test performs real network calls without `RUN_LIVE_E2E=1` (S02–S06, S11). *(and AC 1 of S11 asserts the live suite is reported SKIPPED with `fetch` stubbed to throw — not merely absent.)*
- [x] `docs/MODES-AND-PRESETS.md`, the regenerated `docs/TROUBLESHOOTING.md` and the generated rule file carry the final text; no "Tier" vocabulary remains (S10). **Ticked 2026-09-12 (acceptance item B07-03): proven all along, the box simply never moved.** `tests/modes-page.test.mjs` AC 1/AC 4 (5 cases), `packages/snowarch/tests/errors/codes.test.ts` for the rendered registry and the three `showInRule: true` entries, and `tests/no-legacy-surfaces.test.mjs` for the vocabulary sweep (17 cases). Re-run in the acceptance pass: all green, and `grep -rniE '\bTier [012]\b'` over both pages returns nothing.

## Risks

> **Amendment 2026-09-06 (from `03` §F S-14a).** On every channel, the instance URL and the user name are treated as confidential engagement data, not merely the password: nothing the wizard or the skill writes may land in user-scope files (`~/.claude/settings.json`, `~/.claude.json`); the per-checkout store is the only home for all three. This is a test, not a guideline.

- Corporate laptops with shared/recorded terminals (R-04) — roadmap localhost form; `--password-stdin` from a password manager CLI is documented meanwhile.
- OAuth ROPC disabled by instance hardening → the probe fails with a clear `OAUTH_ROPC_DISABLED` hint (detect the instance's error text).

## Stories

Full write-ups: [`STORIES.md`](STORIES.md) (11 stories, 20–25 engineer-days). Mapping from the original titles-only list: 1 → S01 · 2 → S02 (R-3 folded in) · 3 → S03 + S05 (probe library split out for reuse by `instance test` and the ARC-08 doctor) · 4 → S04 · 5 → S06 · 6 → S07 (D-04 cloud-sync WARN folded in) · 7 → S08 · 8 → S09 · 9 → S10 · 10 → S11.

| ID | Title | Size |
|---|---|---|
| ARC-07-S01 | Masked-input module (`process.stdin.setRawMode`), `--password-stdin`, the no-TTY rule | M |
| ARC-07-S02 | URL normalisation and validation; environment proposal; reachability probe with DNS / TLS-CA / proxy diagnosis (R-3) | M |
| ARC-07-S03 | Probe library: auth probe (basic, ROPC), 401/403 mapping, `OAUTH_ROPC_DISABLED`, role hints, per-flag capability probes, `lastProbe` record | M |
| ARC-07-S04 | Preset proposal and the per-flag review screen (Propose → Review → Apply); prod cap in the wizard; `--yes` / `--preset` / `--flags` | M |
| ARC-07-S05 | `instance add` end to end: bounded credential re-entry, atomic 0600 save, secret-free summary, the `./snowarch instance` forwarder, exit codes | L |
| ARC-07-S06 | `instance list · test · set-credentials · set-preset (--ack-prod) · set-flags · set-default · remove` | M |
| ARC-07-S07 | `--global` store, project-wins precedence messaging, cloud-sync-folder warning (D-04) | M |
| ARC-07-S08 | `instance import --from-legacy`: dry-run plan, field and flag mapping, explicit `FLUENT_ENABLED`, prod cap, deletion advice | M |
| ARC-07-S09 | `/snowarch setup-instance` skill body: prerequisite check, three `AskUserQuestion`s, printed command per OS, `--resume` with reload + doctor, S-02 fallback | M |
| ARC-07-S10 | `docs/MODES-AND-PRESETS.md` final text; error-registry entries; runtime rule text for `AUTHENTICATION_FAILED` / `INSUFFICIENT_PRIVILEGES` / `PROD_WRITE_NOT_ACKNOWLEDGED` | M |
| ARC-07-S11 | Live E2E suite behind `RUN_LIVE_E2E=1`: wizard end to end, three-failure exit with lockout check, prod cap, ROPC-disabled fixture, import | M |

### Acceptance

The acceptance pass against `docs/plans/06-ACCEPTANCE-PLAN.md` §2. One row per backlog item.

| Item | Outcome | Evidence |
|---|---|---|
| B07-01 — S02 AC 5: the `network:` line appears exactly once per run | **rework** | Format and masking were asserted in `reachability.test.ts`; the COUNT was documented at the call site (*"ONCE PER RUN, whether the probe succeeds or not"*) and nowhere else. Now asserted on three paths: the `instance add` happy path, the unreachable path (the line is printed **before** the probe, so a failing run prints it too — that is the point of it), and the **retry** path, which re-probes and must still print one. `instance test` gets the same assertion, since re-probing an existing entry is where a second line would be least noticed. The retry case caught its own first draft: menu answer `[1]` is *re-enter the URL*, not retry, and the precondition `expect(probes).toBeGreaterThan(1)` reported one probe rather than passing on a run that never retried |
| B07-02 — S07 AC 3: E-25 parity-tests the cloud-sync fixture | **rework, and a gap recorded** | The case was titled *"E-25 answers to the same provider list as the server and the bootstrap"* and carried **three hand-written segments** — true of those three, unchecked for the rest. It is a table over the fixture now: all 8 `synced` rows (status and provider, except where the fixture itself says the provider is unknown) and all 6 `quiet` rows. **The gap:** the fixture has a third section, `env[]` — Windows Known Folder Move, where the policy redirects Documents into OneDrive and, in the fixture's own words, `%OneDrive%` is *"the only detector there is"*. The server's `detectCloudSync(path, { env })` reads it and its test iterates those rows; the bootstrap's `cloudSyncProvider(path)` takes **no env**, and E-25 calls it with `ctx.root` alone — so on such a machine E-25 reports *not under a cloud-sync folder* for a checkout that is one. Asserted as it behaves today so the gap is visible and cannot move unnoticed; **not fixed here** — it is a product behaviour change on a Windows path, and ARC-06/ARC-08's call. (Note: this ARC's own README dependency line already claimed *"E-25 parity-tests the cloud-sync fixture"*; it is true as of this PR) |
| B07-03 — README criterion 12 | **closed** | Proven all along; the box simply never moved. Ticked with its three witnesses re-run in the pass: `tests/modes-page.test.mjs` (5 cases), `packages/snowarch/tests/errors/codes.test.ts`, `tests/no-legacy-surfaces.test.mjs` (17 cases) — and `grep -rniE '\bTier [012]\b'` over both pages returns nothing |
| B07-04 — S11 AC 5: captured ROPC bodies | **record + rework** | The evidence cannot exist until the sitting (OWNER-SITTING *live E2E suite* item (d)) toggles `glide.oauth.inbound.ropc.grant_type.disabled` against a PDI. AC 5 is marked post-sitting. **The defect found while recording that:** the guard asserted `expect(fixture.observed).toEqual([])` — it pinned the fixture EMPTY, so the sitting's own commit would have turned it red and read like a regression. Rewritten to the rule that is actually meant — every row that exists maps to a code the table knows, which is true of an empty list too — so the capture can land in a green tree and AC 5 becomes true without editing a test |

### Chores

Work that is not a story: a defect found while building one, fixed in the same arc.

| ID | What | Status |
|---|---|---|
| ARC-07-C1 | **The two strings that name PowerShell give the one spelling PowerShell refuses.** `packages/snowarch/src/cli/tty.ts` prints *"On PowerShell/cmd use: `snowarch.cmd instance add … --password-stdin`"* and `docs/snippets/terminal-handoff.md` prints *"(Windows PowerShell/cmd:  `snowarch.cmd instance add …`)"*. A bare name runs in cmd.exe; **PowerShell does not resolve commands from the current directory** and answers *"… exists, but not as a cmdlet … type `.\snowarch.cmd`"*. Nothing in the bootstrap puts the checkout on PATH, so the current directory is where the launcher is. **The proof is in one line of our own code, not in an argument about PowerShell:** `spellings()` in `tools/snowarch/lib/text.mjs` returns `{ bootstrap: '.\\bootstrap.cmd', cli: 'snowarch.cmd' }` — the POSIX branch one line below prefixes BOTH (`./bootstrap.sh`, `./snowarch`), and the Windows branch prefixes only the bootstrap. The author knew the rule and applied it to one of the two. `docs/MIGRATION.md`'s own PowerShell fences agree with the rule and disagree with the product (**23 `.\snowarch.cmd` spellings in the tree**). From there `scripts/gen-readme.mjs`'s `HANDOFF` mirrors the hand-off into README, `.claude/skills/snowarch/SKILL.md`, `docs/INSTALL.md` and `docs/MODES-AND-PRESETS.md`, and `npm run gen` copies `text.mjs` into `text.json` for the Node-free launchers — one wrong spelling, six surfaces | **CLOSED by ARC-07-W7 (2026-09-26), two releases after it was opened.** The count grew while it waited, which is the argument for not leaving a row open: 88 bare spellings against 16 correct ones by the time it was picked up. **`spellings().cli` is `.\\snowarch.cmd`**, and every renderer reads the spelling instead of writing one: B06's eight literals, `preset-ui.ts`'s locked footer, `PROD_LOCKED` and `prodRefusal`, and `noTtyMessage` — which carried **two** defects in one message, the win32 line's bare name and a `./snowarch` in the sentence it appended to, so a Windows reader got a POSIX example and a spelling PowerShell refuses at once. **The server has its own copy, deliberately**: the engine may import the server's `dist/` (`cloud-sync.mjs` does) and the server must never import the engine, so `cliSpelling` re-states the rule and a case walks BOTH over four shells — win32, Git Bash, MSYS, POSIX. It keeps the condition that matters: `isWindowsShell` is `platform === 'win32' && !env.SHELL && !env.MSYSTEM`, because Git Bash on Windows runs `./snowarch` perfectly well. **AND THE MIRROR OF THAT TRAP BROKE THE WINDOWS CELLS, which is the part I got wrong.** The row recorded one direction — a `win32` fixture inheriting `process.env` renders POSIX on a mac — and missed the other: `spellings()` with NO fixture renders **Windows on Windows**, so three test assertions that spelled the POSIX form passed on every mac and Linux cell and failed on all four Windows ones, where `SHELL` is unset. Two were red in CI (`b06-migration` at the migration remedy and the upgrade remedy); **the third was latent** — `preset-ui.test.ts`'s locked-production line — because `npm test` is `node tests/run.mjs && npm test --workspaces`, so B06's failure stopped the chain before vitest ran at all, and fixing the first two would have revealed it as a second red round. All three derive from the same function the product reads now, so they state the property on whichever platform runs them. **A fragment sweep over all ten derived messages confirms zero remaining literal-launcher assertions** — the three were the whole class, measured rather than hoped. **Forcing `process.platform` locally is not an instrument for this**: it makes `win32.resolve` run on POSIX paths and every unrelated case fails, so the check is the sweep, not a simulation. The rule this leaves: a row that makes output platform-dependent has its Windows cells read before it is called green. **AND THERE WAS A SECOND WINDOWS ROUND, which the fragment sweep could not have found.** The locked-production screen is asserted BYTE FOR BYTE against `docs/snippets/review-screen-prod.txt`, and its footer read the platform inside the renderer — so it printed `.\\snowarch.cmd` on the runner against a POSIX snapshot. A sweep for launcher FRAGMENTS cannot see that assertion: its expected value contains no launcher, it contains a whole screen. `ScreenInput` takes an optional `cli` now — defaulting to `cliSpelling()`, so production is unchanged — and both byte-for-byte cases STATE their platform and derive the spelling from it. **The instrument was wrong twice, and the right one is smaller than either sweep:** enumerate the product sites that read the platform from DEFAULTS. There are exactly four — `PROD_LOCKED`, `prodRefusal`, `renderReviewScreen`, B06's `SPELL` — and every other producer passes `platform` and `env` explicitly: `text.mjs` always does, `import-legacy.ts` never reads it, and `panel.mjs` cannot, because the purity walker forbids it. That last one is why `status-template.md`'s thirteen launcher spellings were never at risk, and why guessing from the spelling count would have sent me to the wrong files. **THE FIXTURE TRAP, and it bit for real.** `env` defaults to `process.env`, so `spellings({ platform: 'win32' })` returns the POSIX strings on any machine with `SHELL` set — a case written that way asserts the POSIX spelling and **passes against the unfixed product**. It caught me mid-row: the `noTtyMessage` call sites passed `platform` and not `env`, and a win32 message rendered `./snowarch` until its own test failed. Every case states `env: {}`. **What this row does NOT claim, as C1 said first:** the refusal is still not measured — no PowerShell on this machine — so the evidence is PowerShell's documented behaviour, `spellings()`'s own `.\\bootstrap.cmd` beside the `cli` that lacked it, MIGRATION's 16 already-correct spellings, and **ci.yml, which has run `call .\\snowarch.cmd` all along**. The S08 Windows sitting is the live check. **Five of C1's named pins moved with reasons**, including the one written to catch exactly this — `assert.equal(win.cli, 'snowarch.cmd', 'the premise moved: Windows no longer spells the launcher this way')` — which is what brought me to the right line. **And the extractor that would have blocked it:** `handoff-command.mjs`'s regex spelled `snowarch.cmd` exactly, so correcting the snippet made it throw *no command template found*. The prefix is optional there now — a strict matcher that fails closed on the one change it exists to carry is not a guard. **Controls:** `spellings().cli` reverted → the agreement case; a bare invocation planted on a page → the sweep; `cd /d` restored → b00-checks by name; the server's copy dropping the Git Bash condition → the agreement case, naming `{"SHELL":"/bin/bash"}`. **The fourth was inert on the first attempt** — my edit silently did not apply and the case passed — which is why each control now asserts its own anchor before running. `lib/steps/B06.mjs` puts this row in the e2e trigger set: 45/45, then `assert-clean` |
| ARC-07-C2 | **The interactive install had never worked, and CI could not see it.** An owner running `./snowarch mode live --register local` on rc.4 reached B06 and got `instance add needs a label`, **exit 2, before a single question was asked**. B06 spawns `instance add --from-bootstrap` with no label; the CLI required one as a positional **even on a terminal**. Disambiguated by hand: `./snowarch instance add` alone in a real terminal fails the same way, so it is the wizard's argument rule and not the spawn. **Why nothing caught it:** CI has no TTY and never reaches B06, and rc.2 never got that far — so the one path that exercises it is a person at a keyboard. Release-blocking: Sitting C's A1 cannot start | **Fixed: on a terminal a missing label is a QUESTION, not a usage error.** `instance add` with no label now asks, proposing `pdi` — ADR-0005 propose-don't-impose, Enter accepts and typing wins — and then continues into the existing prompts. **Without a terminal, or with `--yes`, the usage error stands exactly as it was**: a run that cannot ask must not hang waiting for an answer nobody can type. B06's spawn stays label-less, as ruled. **The label is RE-PARSED rather than patched in**, so a typed label meets the same `LABEL_RULE` as one given on the command line — one enforcement point, one sentence. `isTty` is injected on `AddIo` like every other terminal fact in that file, because a test that needed to own a TTY would not have been written, **which is exactly how this reached an owner's machine**. **The test that would have caught it:** B06's argv is now an exported constant (`WIZARD_ARGV`) driven through the built CLI's own `runInstance` — the gap was ARC-06-C6's precisely, *nothing ever ran B06's spawn against the real command line*. The test pins the routing assumption too (commander strips the command word), so an argv that stopped saying `instance` cannot pass by accident. **Negative control run:** with the ask removed, two of the four fail, the first with *"it refused without asking anything — the C2 defect"*. See `ARC-06-C13` for the B06 half. **CLOSED on a real terminal — the owner's step 2, rc.5, 2026-09-19.** `./snowarch mode live --register local` ran B00 through B05 and B06 then printed `Label for this instance [pdi]` and **waited for a keystroke**. That is the whole of this row: the install that had never once asked a question now asks it, on the one path that can prove it. No CI cell reached this line and none can — there is no TTY — so the evidence had to come from a person at a keyboard, which is what the row said when it was opened |
| ARC-07-C3 | **The product's own remedy produced the harm its own first sentence warns about.** Sitting C, T-19, rc.5. One character was changed in the stored password; a read returned `AUTHENTICATION_FAILED`; the session stopped — correctly — and printed the remedy with the label filled in, no `<label>`, no suggestion to edit the store. The owner ran `./snowarch instance set-credentials pdi` (*Credentials updated, all probes ok*) and typed `done`. The session then called `capabilities_read` to confirm, was told everything was fine, retried the read, and got `AUTHENTICATION_FAILED` again — while `./snowarch instance test pdi` passed in the terminal at that same moment. **Two failed logins on an account whose remedy says, in its first line, "repeated failed logins can lock the account".** The store was right; the running server still held what it read at startup, and the confirm the session makes after "done" reported that startup state, so the confirm was the sentence that authorised the retry. Confirmed as a server-side cache by the owner: `/mcp` → Reconnected, then the same prompt once → the record rendered, with no store change in between | **Fixed on both halves, and the striking part is that the fix already existed, unnamed.** `InstanceManager.reload()` was implemented, `load()` idempotent, and **`snow_core_instances_reload` already shipped as one of the five tools in `CORE_TOOLS_UNCONFIGURED`** — available even with nothing configured. Nothing said its name where it was needed. So: (a) the `AUTHENTICATION_FAILED` remedy in `errors/codes.ts` now continues *"and then call `snow_core_instances_reload` before you retry, because this server still holds the credentials it read at startup and a retry without it is a second failed login"* — it is `showInRule: true`, so `npm run gen` carries it into `.claude/rules/00-mode-and-mcp-gate.md`, which every session loads, and into TROUBLESHOOTING, the governance protocols and the server README; and (b) `currentCapabilities()` reloads the store before it reports, so the confirm cannot describe a state the server has left. `reloadInstances()` was deliberately NOT reused there: that one also re-advertises the catalogue and notifies the client, and this call needs only the credentials to be current. **The warning was extended, never softened** — a test asserts the remedy still carries `stop immediately` and `lock the account` alongside the new sentence, because a remedy that named the reload and dropped the stop would trade one harm for another. **Controls:** reverting the reload fails exactly the two store-reading tests and leaves the remedy-text test green, which is the right split — one is about a call site, the other about a string. **One correction made while writing the test, and worth keeping:** the first version asserted that `capabilities_read` reports the changed username. It does not, by design — it returns label, url, environment, preset, flags, toolPackage, maxRecords field by field and never the credential. Asserting on a field that does not exist would have been a test passing for the wrong reason; the observable is `preset`/`maxRecords`, proving the runtime was rebuilt from the file, and the test says so rather than implying it |
| ARC-07-C4 | **A `--yes` install printed a preset its own probes had just argued against, and explained nothing.** Sitting C, A6, rc.5: `instance add pdi2 … --password-stdin --yes` probed, reported `fluent not installed`, and then printed `Applying: preset full — … FLUENT=on` followed by `Saved instance "pdi2" (… preset full). Probes: … fluent not installed.` One sentence saying FLUENT=on and fluent not installed. The interactive path shows *"(recommend: off)"* and the owner had taken that advice on `pdi` minutes earlier — because the probes were computed and handed **only** to the review screen. The path with nobody to ask displayed nothing at all | **Annotation only — the toggle is untouched, and that is an owner ruling rather than a preference.** ADR-0005 (D-05, owner, 2026-09-04) says *"a failing probe changes only the recommendation text on that line, never the toggle"*. The first version of this fix turned the flag off on `--yes` and was **stopped before it shipped** by `preset-ui.test.ts`'s own *"criterion 7 — a failing probe does not change the proposal"*, whose comment reads "D-05 in one assertion" and which covers `yes: true` explicitly. **Re-opened with the owner and confirmed on 2026-09-19 (this sitting, relayed by the architect): D-05 stands, no superseding ADR — on `--yes` the product saves exactly what was asked and the line carries the annotation.** Nobody should re-open the toggle question without a new decision. **What was actually missing was the other half of the same ruling.** D-05 requires every flag to be *"annotated with its live probe result"*; the non-interactive path annotated none of them, so it was failing D-05 in the opposite direction — missing the sentence the ruling requires, not missing a toggle the ruling forbids. The Applying line now reads `FLUENT=on (probe: not installed — tools will fail until @servicenow/sdk is on PATH)`, so the Saved line's `fluent not installed` confirms a stated choice instead of contradicting one. **`probeNote` is a sibling of `annotate`, not a reuse of it:** `annotate` asks *"keep on?"*, which is right on a screen and wrong on the one line a `--yes` run prints, because there is nobody to ask — so the non-interactive phrasing states the consequence instead, and returns `null` for exactly the statuses `probeRecommendsOff` rejects. A test walks every `ProbeStatus` and requires the predicate, the screen's "(recommend: off)" and `probeNote` to agree, so a new probe outcome cannot be added to one of the three alone. **`skipped` and an unprobed run annotate nothing** — a probe that did not run is not a probe that failed, and a line reporting absence as a finding is the same defect facing the other way. **Controls:** dropping the annotation fails the new test AND criterion 7's line assertion. Criterion 7's substance is untouched and still asserted above that line — same preset, same flags, with and without a failing probe; only its literal string moved, because the line now says more and decides nothing more |
| ARC-07-C5 | **`add` discards the probe results it just took.** Sitting C, A6, rc.5. `instance add pdi2 … --yes` ran the probes at `[5/6]`, printed `Probes: auth ok · write ok · cmdb_write ok · scripting ok · atf ok · now_assist ok · fluent not installed.`, reported the same results in `--json` — and then built its store entry **without a `lastProbe` key at all**, on every path, interactive or not. `instance list` immediately afterwards showed `LAST PROBE —` for an instance probed seconds earlier. **pdi's `lastProbe` came from a later probe, so the interactive path only looked right by accident** — that sentence is what makes the gap invisible: the next `./snowarch doctor` runs SV-04, SV-04 calls `instance test`, and `instance test` **does** write `lastProbe`, so any instance that had seen one doctor carried a value somebody else had measured. Only an instance listed before anything else ran shows the truth, and pdi2 was added and listed with nothing in between. Same shape as ARC-09-C46's discarded tally: the data was in hand and thrown away | **Fixed — the entry keeps the probes the run took.** The `add` entry now carries `...(probeResult?.last ? { lastProbe: storedProbe(probeResult.last) } : {})`, the same `storedProbe` shaping `instance test` and `set-credentials` already used at their two `writeEntry` sites — a third reader of one definition, not a fourth spelling of it. **Conditional, not unconditional:** `--no-probes` must still store nothing, because a record of a probe that did not happen is worse than no record — the same rule that keeps `skipped` unannotated in ARC-07-C4. The test asserts the value that reaches **the store**, `toEqual(result.lastProbe)`, so it is the run's own measurement rather than a second one, and it asserts the serialised entry carries neither credential — this lands in a 0600 file a person may `cat`. A second test pins the `--no-probes` store to null; the existing `--no-probes` test pins only the **reported** value, which is the reader-vs-writer split this programme keeps finding. **Control:** reverting the spread fails exactly the new test, 26 of 27 still green **Live on the owner's rc.9 checkout (sitting, 2026-09-23):** `instance add pdi2 … --yes` printed `Probes: auth ok · write ok · cmdb_write ok · scripting ok · atf ok · now_assist ok · fluent not installed.` and `instance list` **immediately** showed `pdi2` with LAST PROBE filled — the gap this row closed, on the same command that exposed it. |
| ARC-07-C6 | **The `LAST PROBE` column showed five of the seven facts it was holding.** `probeCell` in `packages/snowarch/src/cli/format.ts` named them in a hand-written list — `auth`, `write`, `scripting`, `cmdb`, `atf` — so `nowAssist` and `fluent` were probed, written to the store and never printed. **An instance whose Now Assist probe returned `not licensed` listed as a row of `ok`s**, and the two dropped fields are the two a reader most needs: a capability that is off is the reason a tool will refuse, and being refused is what sends somebody to this table. **The evidence was already in the test file.** `AC 1 — list` has built its fixture with `nowAssist: 'not licensed'` and `fluent: 'not installed'` since it was written and asserted on neither — because nothing printed them, so nothing could. Two renderers of one record also disagreed about the vocabulary: the wizard's Saved line says `cmdb_write` and `now_assist`, the flags' own names, where the table said `cmdb` and nothing. And the column explained neither of its two glyphs — a bare `LAST PROBE (<at>)` header carrying the NEWEST time in the store reads as a claim about every row, and the row it is wrong about is the stale one, which is the row a reader opens this table to find; `—` was never defined anywhere | **Fixed — one definition, every reader.** `PROBE_FIELDS` in `servicenow/probes.ts` is the order and the names, **derived from `FLAG_NAMES`** rather than spelled again, so a seventh capability added to the flags reaches both surfaces or neither and cannot arrive under two names. `auth` leads because it is not a capability — it is the question the other six presuppose. `probeSummary` (the Saved line) and `probeCell` (the table) both read it; what stays local to the Saved line is the only thing that is its own, that a DISABLED flag reads `off` instead of its probe result, because the wizard is reporting the choice just made rather than the instance. **PRESENT, not merely declared:** a record written by an older build has fewer keys, and `now_assist undefined` would be a renderer inventing a result for a probe that never ran — this row's own defect facing the other way — so an absent key is left out and the dash is reserved for a record that does not exist. The header says `LAST PROBE (most recent: <at>)`, which is the honest statement of an aggregate over rows, and a footnote under the table reads `— = never probed. ./snowarch instance test <label> probes one; --all probes every one.` — printed **only when a dash is on the screen**, because a sentence explaining a glyph that is not there is a line a reader has to rule out. `list --all` gets both: it showed a bare `LAST PROBE` and no footnote, so the one view spanning two stores said least about the column they share. **The agreement test asserts against `probeSummary` rather than a second literal** — `expect(saved).toBe(\`Probes: ${cell}.\`)` — so the two surfaces are pinned to each other through the definition, not to two copies of a string. **Controls:** the five-field list restored fails exactly the three cell tests (3/37 of 40); the unqualified header with the footnote removed fails exactly the two column tests (2/38). The whole suite passed before these tests existed, which is the measurement of how untested the column was |
| ARC-07-C7 | **The nightly live suite has never tested a tree with the live-suite rows in it, and a dispatch that tested nothing reported nothing.** Sitting E2. The owner dispatched `e2e-live.yml` on `develop` with `allow-writes` off (run 35900912032): `conclusion=skipped`, matrix job skipped, secrets in place, nothing tested. Cause: the job guard was `github.ref == default_branch`, so a manual dispatch on any other ref is silently skipped — **and a skipped job runs none of its steps, so no step could say why**. A skipped run reads as a calm nothing rather than as a refusal. The second half is worse and was found by measuring rather than by reading: GitHub runs a schedule on the DEFAULT branch, which is `main`; `origin/main` was 16fe2df and `origin/develop` 0760df1, **`main..develop` = 289 commits**, without a single row of ARC-08 or ARC-09 in it. **The nightly had been aimed at main since this workflow was written, and no scheduled run has yet tested a tree with the live-suite rows in it** — a green nightly there says nothing about the release candidate | **Three changes, one workflow.** The guard becomes `github.event_name == 'workflow_dispatch' \|\| <default-branch>`: a dispatch runs on the ref the person chose, because they chose it; the schedule keeps the default-branch requirement, which it satisfies by construction. The checkout takes `ref: ${{ github.event_name == 'schedule' && 'develop' \|\| github.ref }}`, so the nightly tests the branch under development and a dispatch tests what it was given — when ARC-10 makes the release line the default, that constant moves once. And a first step prints event, ref, sha and subject, because two paths that resolve to different trees make "it was green" unreadable without one. **The skip explains itself from a SEPARATE JOB** (`explain-skip`, carrying the negated guard), since the skipped job cannot: that is the whole shape of the finding, and putting the sentence inside `live` would have reproduced it. **Controls:** the guard restored to ref-only → `the live suite never runs on a proposed change, and asks for the branch it needs` fails; the checkout's `ref:` removed → `ARC-07-S11 — the schedule tests develop, a dispatch tests the ref it was given` fails. Asserted over the workflow TEXT like every other test in that file — a YAML parser would read better and would mean adding a dependency to a test whose subject is a file CI reads literally |
| ARC-07-C8 | **The live suite asserted a probe label the product does not print.** First honest nightly after ARC-07-C7 aimed it at develop: ubuntu `expected '…' to contain 'nowAssist ok'` while the product printed `now_assist ok`. `PROBE_FIELDS` derives `label` from the FLAG name (`NOW_ASSIST_ENABLED` → `now_assist`) and carries `key` as the store field (`nowAssist`); the test retyped the key as though it were the label. One constant, a second writer, nothing to notice until they differ — the `sortTags` shape, and invisible for as long as the nightly ran against main | **The assertion reads `PROBE_FIELDS`** — label from the product, value from the store — and asserts the field still exists, so a renamed key fails by name rather than by a missing substring. **macOS is NOT fixed in this row and is measured rather than guessed:** `script: tcgetattr/ioctl: Operation not supported on socket` reproduces locally in one spawn — Node's default stdio pipe is a socketpair on darwin and BSD `script(1)` calls `tcgetattr` on it. A `/dev/null` stdin fixes the spawn (`exit 0`) but ends stdin, so the prompts cannot be driven; a FIFO does **not** fix it, still reporting `socket`. So the driver needs a real pty rather than a non-socket fd, which is a change to how every pty case is driven and is raised rather than patched. **macOS fixed in a follow-up commit, same row:** `runInPty` no longer spawns `script(1)` at all — `tests/helpers/pty-driver.py` allocates the pty itself with the stdlib's `os.forkpty()`, gives the CHILD the pty as its controlling terminal, and never calls `tcgetattr` on this driver's own stdio (a plain pipe to Node, the exact shape `script(1)` choked on). python3 was already a doctor prerequisite on POSIX (`docx` generation), so nothing new is required; the two-dialect `scriptArgv` branch and the workflow's "Is script(1) here" step are both gone, replaced by a step that prints the python3 version and whether ITS OWN stdin is a tty. The e2e cases (`instance-wizard.e2e.test.ts`) assert exactly what they asserted before — only the mechanism underneath changed. **Control:** `tests/helpers/pty.test.ts` is new — it drives a small python3 echo child through a prompt/answer exchange and asserts a real tty (`os.isatty(0)` in the child reads `True`) and a clean exit; reverting `runInPty`'s spawn line back to `script(1)` fails all three of its non-trivial cases (`drives a prompt, sends the answer once it appears, and returns the whole transcript`, `reports the exit code of a child that fails, with no answer to send`, and `never calls tcgetattr on this process's own stdio, so a socket/pipe stdin is fine`) with exactly the measured `tcgetattr/ioctl: Operation not supported on socket`, restored afterwards. **Closed live on 2026-09-24:** e2e-live run 35964429588 on `develop` d90f5bd, dispatched with `allow-writes` off, was green on **windows-latest, ubuntu-latest AND macos-latest** — the first fully green live suite on a tree carrying the live-suite rows, and the judge this row asked for. The unit test proves the driver; only the nightly on macOS could prove the row, which is why it was raised rather than patched and why the fix was not called done until this run. §E4 (one scheduled night, unattended) is still outstanding — a dispatch and a schedule resolve different refs by construction (ARC-07-C7), so a green dispatch is not yet a green nightly |
| ARC-07-C9 | **`set-preset` printed `probe: ok` for a probe it never took.** The owner asked the question in the 2026-09-23 sitting — *`set-preset` shows `probe: ok` beside each flag while `list` keeps the previous LAST PROBE; either the probes are fresh and should be written, or they are the recorded ones and the word should say so* — and the answer is the second. `runSetPreset` passes `readProbe(entry.lastProbe)`: the store's value, of any age, rendered by `annotate` in **exactly the bytes** a probe taken a second ago produces. A reader deciding whether to keep a flag on is being shown a recommendation (`keep on? (recommend: off)`) computed from a measurement that may be days stale, with nothing on the line to say so | **The line names the day, and only where it has to.** `annotate` takes the provenance and renders ` (recorded 2026-09-19)` **beside the status** rather than at the end — `probe: role missing (recorded 2026-09-19) — …; keep on? (recommend: off)` reads, while the same qualifier after `(recommend: off)` attaches a date to the advice. A **DATE, not an age**: *recorded 2026-09-19* stays true tomorrow and in a pasted transcript, where *5 days ago* has to be recomputed to stay honest — asserted, with `not.toMatch(/ago|day/)`. `not run` is never qualified: there is no record, so a provenance would describe one that does not exist. **The survey came back with ONE site, not several.** Every other printer of this field measures live — `instance add`, `instance test`, `set-credentials` and `import --from-legacy` all pass what they just probed, and `instance list`'s LAST PROBE column already says `most recent: <at>` with its footnote (ARC-07-C6). So the change is additive and the fresh path is **byte-identical** to before, which is its own assertion. **`set-preset` still does not probe**, deliberately: making a local store edit reach the network is a different command, and the honest fix for a stale value is to say it is stale. **Controls:** the screen stopped forwarding the provenance → `the screen carries the provenance through to every flag row` fails alone; the suffix removed from `annotate` → that plus `a recorded probe names the day it was taken, beside the status` and `every status is qualified except the one with nothing to date` fail **Two findings on the first cut, both the architect's, and both about the same word "survey".** (1) **The survey missed the same command's second surface.** `set-preset --yes` does not draw the screen at all — it goes through `resolveFlags`'s non-interactive branch, which builds the `Applying:` line from `probeNote` and ignored the provenance entirely. So the line rendered recorded-as-fresh exactly as the screen had, and it is **the line a transcript pastes**, because `--yes` is what a sitting record runs. `probeNote` carries the provenance now, beside the status as on the screen: `Applying: preset full — WRITE=on (probe: role missing (recorded 2026-09-19) — those tools will fail until the account has the role) …`, with the fresh line byte-identical. A survey that asks *which files print this* misses *which paths through one file* — the interactive branch was read and the `--yes` branch eleven lines above it was not. (2) **The one real site was untested.** Every test in the first cut sat below `annotate`/`renderReviewScreen`; the architect deleted `runSetPreset`'s wiring line and ran preset-ui + instance-manage + instance-manage-server + prod-ack — **101 passed, 0 failed**. The mechanism was proven and its only caller was not, which is **#254's hollow binding in a second place**: a seam is not covered by testing the thing below it. Three site-level cases now drive `runSetPreset` itself against a store entry carrying `lastProbe.at` — the screen qualifies **all six** flag rows, `--yes` qualifies the `Applying:` line, and an entry with no recorded probe says nothing about when, because there is nothing to date. **Controls:** the wiring line deleted → both site tests fail by name (2 of 43); the `--yes` branch stopped passing it → `--yes says it too, which is the line a transcript pastes` fails alone (1 of 89). |
| ARC-07-C10 | **The wizard asked for a label, refused the answer, and ended the install — then B06 told the owner their typing was a defect in the bootstrap and asked them to file a bug.** Live at the ARC-10-S06 sitting (2026-09-25, v2.0.2 dry-run clone, macOS): at `Label for this instance [pdi]` the owner typed `testPDI`. The validation sentence was right; everything after it was wrong. One bad answer exited 2, B06 rendered `the wizard rejected its arguments (exit 2)` and a remedy reading *"this is a defect in the bootstrap, not in what you typed"* — **for a capital letter in a name the wizard had just asked for**. Two defects in one line of output, and the second is worse: B06 sees only the status, and a rejected ANSWER and a bad ARGV both exited 2, so its message had to guess whose fault it was and guessed the product's | **The convention was already in the file, six hundred lines down.** The credential prompt has re-asked up to `MAX_ATTEMPTS` and returned `EXIT_FAILED` since ARC-07, and `EXIT_CODES` documents 1 as *"nothing saved — a refusal, an abort, **three failed attempts**, or a probe that failed"*. **The label was the one interactive answer in this wizard that did not follow it** — so this needed no new exit code and no new constant, only the loop the prompt beside it already had. Measured after: `testPDI` → the rule, asked again, `pdi` accepted, and the run reaches `[1/6] Instance URL`, the wizard's own next question. **`EXIT_FAILED` and not `EXIT_USAGE` on exhaustion is the half B06 needs**: exit 2 now means only what it says, so its usage remedy may be confident again — narrowed to *"the arguments B06 passed were refused"* — and exit 1 gains the remedy it never had, since a refusal, an abort or three failed attempts are all the operator's answer and all recoverable. The two remedies say opposite things about whose fault it is, which is why they are separate classes rather than one merged sentence. **A test asserted the defect as correct behaviour**, which is why it shipped: `answers: ['NOT A LABEL']` → `assert.equal(code, 2)`, true of giving up and of asking again then running out of input. The exit code alone cannot tell those apart, so the prompt COUNT is asserted now. **And the plan header's verb**, from the same sitting: *"type a number to change that line"* read as a sub-prompt, and the owner pressed `1` seven times waiting to be asked something while each press silently toggled Mode and redrew the plan — `toggle` now, the behaviour was always right. **EVERY CONTROL HERE NEEDS A REBUILD, AND A FORGOTTEN ONE LOOKS LIKE A CONTROL THAT DID NOT FIRE.** The `b06-*` tests are `node --test` files importing `packages/snowarch/dist/cli/*.js`, while the fix is in `packages/snowarch/src`. Editing the source and running them proves nothing: I ran them after the first edit and got 4 pass / 0 fail, which reads as *the change had no effect* when it means *the change was not there*. A control that degrades a `src` file and then runs a dist-driven test **always passes**, so it is indistinguishable from a control aimed at the wrong mechanism — the failure mode this programme has hit five times. `npm run build --workspace @farstic/snowarch` before the run, every time, and it is the reason the controls below were re-run rather than trusted. **Controls, one change each:** (1) the loop reverted to a single ask → the bad-then-good and three-bad cases fail by name, 3 of 6. (2) exhaustion returning `EXIT_USAGE` again → `three bad labels exit 1` fails on `B06 cannot tell them from bad argv`. (3) B06's exit-1 branch disabled → `exit 1 is the operator's answer, and it carries a remedy` fails by name. My first cut of (1)'s case read `MAX_ATTEMPTS` from the dispatcher, which does not export it — `undefined !== 3` failed loudly, which is the fourth time an undefined import has produced a wrong answer here and the first time the assertion caught it rather than passing vacuously |
| ARC-07-C11 | **The plan screen answered a keystroke with a silent redraw, and the owner pressed the same key fourteen times asking why it kept asking again.** Twice in one sitting, with two different headers, which is what makes it the prompt's fault and not the wording's: seven presses against *"type a number to change that line"*, then — after **ARC-09-C10 reworded it to "toggle"** — fourteen against the new wording, with *"why does it ask me again when I press 1"*. **My own C10 change is the failed first attempt, and it is the evidence**: the header was never the missing part. What the screen gave back after a press was a full redraw and a bare `> `, which answers neither question a person has at that moment — *did that do anything*, and *what does Enter do now*. A numbered list invites the model "the number is my choice"; this one is "the number flips a line and Enter runs it", and nothing said so at the moment it mattered | **The owner's ruling, not an inference from the presses:** *"everything very easy and understandable; there can even be information for each option so the user knows what it is instead of wondering"* — so this is a redesign of the screen's answers rather than a third wording. Three parts. **(1) `?` explains each line and each key**, mirroring `preset-ui.ts`'s `"?" explains the flags` and its line-per-flag block rather than inventing a second convention for "tell me what this is"; the keys are in it because a header long enough to say what Enter, a number and q each do is a header nobody finishes. **(2) The header offers it.** An explanation the screen never mentions is not available on demand. **(3) After a press the prompt itself is the acknowledgement** — `Mode → live · Enter runs it · 1 toggles back · q quits` — carrying what changed, in the plan's own label and value for that line, and what Enter will now run. The first draw carries none of it, because a claim about a press nobody made is the same class of untruth as the silence it replaces. **Two house rules caught this while it was written, both left at full strength.** ARC-08-C7 flagged the Mode explanation for containing the phrase it reserves for the unconfigured STATE — reworded to "skips ServiceNow" rather than allowlisted, which is plainer for a first-time reader anyway. And the explanations are SHORT because the engine has no wrapper and must not grow one: `preset-ui.ts`'s belongs to the server package, which the engine may not depend on, and the doctor's renderer already ruled that a line folding mid-word is the part people stop reading — so `COLUMNS` is named and the test asserts every line against it, because prose grows. **Controls, one change each:** `?` no longer a question → the explain case fails by name; the explanation computed but not printed → the same case, which is why it asserts the offer AND the content; the acknowledgement removed from the prompt → both toggle cases fail; one explanation lengthened to 149 columns → the budget guard fails naming the width. **My first cut of the acknowledgement's assertion failed against a correct fix** — it asserted `> ` was ABSENT after a toggle, when `> ` is where the cursor waits and always follows; the property is that the acknowledgement immediately precedes it |
| ARC-07-C12 | **Three screens, three sittings, and the owner read the number the same way every time.** S06 on v2.0.2 with *"change that line"*: seven presses of `1`. On develop after ARC-07-C10 reworded it to *"toggle"*: fourteen. On **v2.0.4 with the whole ARC-07-C11 redesign rendering exactly as built** — the header offering `"?"`, the five explanations printing, `Mode → live · Enter runs it · 1 toggles back · q quits` appearing after the press — their words were *"I press 1 and get the message again, **I choose 1** again and nothing happens"*. **"I choose 1" is the whole finding.** A number in a numbered list is a CHOICE; ours was a toggle, and no sentence on the screen overrides that — C10 proved a verb could not fix it, C11 proved an acknowledgement could not either, and my own two rows are the evidence. Owner's ruling: *"I accept it, but I want you to fix it"* | **THE COUNTER-MEASUREMENT IS WHAT DECIDED THE DESIGN, and it was on the same run.** The wizard's own numbered questions — `What is this instance?  [1] pdi  [2] dev  [3] test  [4] prod` and `Authentication?  [1] basic  [2] oauth_ropc` — were answered **first time, on both dry runs, with no confusion**. The pattern this user reads correctly already existed one screen later, so the plan adopts it rather than explaining itself a fourth time. A number now OPENS that line's question in the wizard's shape — `Mode:  [1] design-only — no instance (current)  [2] live — configures one, needs Node 20+` — the answer sets the value by number **or by name**, and the plan redraws with it. `?` stays; Enter still runs as shown; **a bare redraw never follows a keystroke**. The parser hands back the line and changes nothing, because it has no io and the question is the caller's to ask. **Enter at a line's choices means leave it**, never run: opening a line by mistake must not start an install, and end-of-input there is still a quit. **Two house rules caught this mid-build, both kept at full strength**: `no instance` rather than the phrase ARC-08-C7 reserves for the unconfigured state, which is the same trap C11's Mode explanation hit; and both choice lines measured against `COLUMNS` — Mode's first draft was 103 and was shortened rather than left to wrap. **Controls, one change each:** the number flipping again instead of opening → 10 cases fail, four of them by name; Enter at the choices resolving to a value → the leave-it case fails; the per-option meanings removed → the wizard-shape case fails on the meaning. **And four of my five new cases first passed against the toggle they were written to replace** — `['1','2','']` reaches `mode: live` under either model, so each gained the assertion that distinguishes them (Docs untouched, the choices line printed, opened twice). A case that cannot tell the old behaviour from the new one is not a case |
| ARC-07-C13 | **A line asked a question, recommended an answer, and did not say how to give it.** From the owner's v2.0.5 dry run: `[x] FLUENT  probe: @servicenow/sdk not on PATH — keep on? (recommend: off)` over a footer reading `Enter = accept as shown`. They pressed Enter and the Applying line printed `FLUENT=on`. **Both sentences are literally true and together they mislead** — the line asks *keep on?* and advises *off*, and the only way to act on that is to already know the footer's *type a flag name to toggle* applies to this line. The same class as ARC-07-C12 one screen earlier: the words and the behaviour each describe something real and the reader is left to reconcile them | **THE TOGGLE STAYS ON, AND THAT IS AN OWNER DECISION, NOT A DEFECT.** ADR-0005 — decided 2026-09-04, *confirmed with a modification from the owner* — rules that each flag is pre-set ON and that **"a failing probe changes only the recommendation text on that line, never the toggle"**, and ARC-07-C4 asserts exactly that on the `--yes` path, where applying silently would overrule a user on the path whose whole promise is *no review screen*. **The brief for this row originally asked for the box to show `[ ]` so Enter would accept the recommendation; that was withdrawn once the ADR was quoted**, and the question of amending it went to the owner rather than being decided here. So the fix is the one thing the ADR leaves open: **the text**. The recommendation now names the word that acts on it — `(recommend: off — type FLUENT to turn it off)` — using `labelOf`, which is the label the box already shows and the word the toggle loop matches case-insensitively; **measured rather than assumed**, because `FLUENT_ENABLED` is the constant and `FLUENT` is what a reader can type. One definition, in `annotationParts`, so all three recommending statuses gain it together and none can drift. **Controls:** the instruction removed → `ARC-07-C13 — a recommendation says how to take it, on the line that makes it` fails by name **and so does the byte-for-byte review-screen snapshot**, which is two independent witnesses for one change. C4's `--yes` assertions are untouched and green, which is how ADR-0005 is known to be intact. **My first cut of the case asserted against a single physical line and failed on a correct fix**: `wrapRow` folds at the 100-column budget — deliberately, as the footer does — so the instruction spanned the fold. The case reads the logical row now, and every line is still inside the budget. `docs/snippets/review-screen-nonprod.txt` is regenerated from the real screen, `gen` carries it into `MODES-AND-PRESETS.md`, and `MIGRATION.md`'s hand-pasted copy was re-pasted — the drift guard caught that it is not generated |
| ARC-07-C14 | **The recommendation named a word to type, and the owner was reading numbers.** ARC-07-C13 fixed the silent half of the FLUENT line by naming how to act on it — `(recommend: off — type FLUENT to turn it off)` — and that was the right property carried by the wrong token. The owner's ruling, 2026-09-26, in their own words: *"by giving the numbers of the rows I choose whether it is on or off and so I set what rights it has."* **The label was a second grammar a reader had to learn on one screen.** The plan screen numbers its lines and the owner read those correctly at ARC-07-C11 and C12; this is the same reader meeting the same grammar one screen later, which makes it the fourth screen in this product to be told that a number in a list is a choice | **Every row is numbered, a number opens that flag's question, and Enter applies.** The number is **DERIVED from the flag's index and never spelled**, so a flag inserted tomorrow renumbers every line below it and the recommendation's `(type N)` follows; the question is `FLUENT:  [1] on (current)  [2] off — recommended: @servicenow/sdk not on PATH`, Enter there means *leave it* (opening a row by mistake must not change a permission), and the ack names what changed, that Enter applies, and the number that reopens it. **A flag NAME still resolves and is deliberately undocumented**: the footer teaches one grammar and nobody's habit breaks. The number path calls `toggleFlag`, so the dependency conversation is the same function, not a second one. **ADR-0005 IS THE REASON THE BOXES STILL START `[x]`** — owner-decided 2026-09-04, *"a failing probe changes only the recommendation text on that line, never the toggle"*. This is an interaction change, not a policy change: the user now has a documented way to *act* on the recommendation and the recommendation still does not act on its own. The `[ ]` amendment asked for at C13 was withdrawn then and is **still not built** — it needs the owner and a brief of its own. **Controls:** the number handler removed → five cases fail including `(a)` by name **and** *a number-driven change runs the dependency conversation*, which is what proves the number reuses the toggle rather than duplicating it; the question's reason dropped → the reason case fails by name; the ack removed → `(a)` fails; **a failing probe pre-setting the box off → the ADR-0005 guard *...and every box is [x], even where the probe recommends otherwise* fails AND so does the byte-for-byte snapshot** — two independent witnesses that the guard still bites after renumbering (its regex reads `/^\s+\d+\s+\[x\]/` now, and the property it asserts is unchanged). **I added a control because the set was weaker than it looked**: dropping the reason and dropping the ack both failed only `(a)`, which asserted the two in one transcript, so one red case could not say which broke. The reason has its own case now, and it reads the expected words off the rendered ROW rather than spelling them — the question and the row cannot describe one probe in two ways, and `annotationParts` stays private. **A control that was already inert, found by the grep:** `packages/snowarch/tests/e2e/instance-wizard.e2e.test.ts` waited on `/Enter = accept as shown|Applying:/` — the footer changed and the case still passed, through the second alternative. Its only call site is an `--env pdi` add with no `--yes`, so the unlocked screen always prints; the alternative was slack that hid exactly this drift and is **gone rather than updated**. **That case is not proven green here** — it is `describe.skipIf(!ptyCases)` and needs a live instance, so it skips in design-only; reported as tightened and unrun, not as passing. **Docs:** the snippet regenerated from the real screen, `gen` carrying it into `MODES-AND-PRESETS.md`, `MIGRATION.md`'s hand-pasted copy re-pasted (its drift guard is not generated and is 11/11), and `01` §6.3 spliced from the same snippet with its prose now reading *toggleable by the row number printed beside it*. `tests/modes-page.test.mjs` and `tests/no-legacy-surfaces.test.mjs` move to the new footer, and no-legacy additionally requires `a number opens that flag`: a page that kept only the accept half would leave a reader no way to change anything. **The locked production footer is untouched** — a locked screen has no toggle to open, so `Enter = accept` is still the whole of its offer. **What I did NOT rewrite, and why:** the C13 row above quotes the old footer as that defect's evidence, and ARC-07-S04's delivered acceptance screen, its dated 2026-09-08 amendment and ARC-02-S09's design notes record what the screen was when they were written. Rewriting a record to match today's code destroys the finding it holds. The two that made **present-tense claims about a live test file** gained a superseded-by note beside the quotation instead, leaving the quoted strings intact. Suite 58/58; lint, type-check, test, contract, `gen:check`, `upgrade-unit`, `assert-clean`, commitlint and a clean tree all green. The five-file upgrade e2e set is **not** triggered: nothing under `upgrade.mjs`, `bootstrap.mjs`, `lib/steps/` or `tests/upgrade/` changed |
| ARC-07-C15 | **The one advertised way back from a mistyped host threw the run away.** `reachabilityMenu()` has offered `[1] re-enter the URL` since it was written (`servicenow/reachability.ts:171`), and `runAdd` tested `if (choice !== 'retry')` — so `[1]` fell into the same branch as `[3] abort`, printed `Nothing saved.` and exited 1. **The most likely thing a user does with a typo was the one answer that discarded the label, the URL and the environment they had already given.** The defect had already been MET and worked around inside this repository: the retry case in `instance.test.ts` carries the comment *"[2] is retry — [1] is 're-enter the URL', which the first version of this test chose and the precondition below caught: one probe, not two"*. That author was right to switch answers for their own case, and the menu stayed broken — a reminder that a test working around a product defect records it without reporting it. ARC-07-W1 of the owner's wizard-UX programme (ruling 2026-09-26) | **`[1]` re-asks the URL, normalises it, says so, and probes again.** The ack is `URL → https://<host> · probing again`, and the URL step became a FUNCTION because it is now needed twice — one definition, so the normaliser's notes, the `Proposed URL` step and its `!options.yes` condition cannot drift between the first URL a user types and the second. **`instanceUrl` became mutable, and that is the half a transcript cannot see**: the client, the probes and the saved entry all read it, so a handler that printed the ack while leaving it `const` would have saved an instance nobody can reach — the outcome P-23 exists to prevent — and every transcript assertion would still have passed. The case asserts the PROBE LOG (`[typo, good]`) and the stored `instances.pdi.url`, not just the ack. **Bounded at three probes**, because `[1]` and `[2]` both loop now and a stdin that keeps answering must still end: unbounded is a hang in a test and an unreadable timeout in CI. `--yes` aborts on the first failure exactly as before — there is nobody to ask — and the pre-existing retry case still measures two probes and exit 1, untouched, which is how the bound is known not to have changed `[2]`. **Controls:** the `reenter` branch removed → both new cases fail by name **and the other 29 in the file stay green**, which is how the control is known to have removed only the half it names. **Left for ARC-07-W2 on purpose:** a re-entered URL that does not normalise still returns `EXIT_USAGE`, exactly as the first prompt does today. W2 owns that class for every URL prompt, and it will change the one `resolveUrl` this row extracted, so both callers gain the re-ask together — doing it here would have been W2's work done twice, in one of the two places. **No docs change**: the menu appears in no snippet, page or skill — grepped, not assumed — so this row carries its status row alone. Gate green (lint, type-check, test, contract, `gen:check`, `upgrade-unit`, commitlint, `assert-clean`, clean tree); the five-file upgrade e2e set is not triggered — nothing under `upgrade.mjs`, `bootstrap.mjs`, `lib/steps/` or `tests/upgrade/` changed. **A process note for my own record:** the first commit message used `-m "…"`, and zsh command-substituted the backticked `` `choice !== 'retry'` `` out of it, leaving a sentence with a hole. Amended, and the quoted heredoc (`-F - <<'MSG'`) is the only form to use for a message that quotes code |
| ARC-07-C16 | **A typo at the URL prompt made the wizard blame itself.** An interactive answer that did not normalise returned `EXIT_USAGE`, and exit 2 is precisely what makes B06 print *"a defect in the bootstrap rather than in anything you typed … please report"*. **ARC-07-C10 had already found and fixed this class one prompt earlier** — the label re-asks up to `MAX_ATTEMPTS`, exits 1 on exhaustion and keeps exit 2 for argv — and its own comment says the convention was *"already here, six hundred lines down"*. The URL prompt was simply left behind: same wizard, same reader, same wrong exit code, and the fix for it existed in the file. ARC-07-W2 of the owner's wizard-UX programme | **The loop wraps the `resolveUrl` seam ARC-07-C15 extracted, so `[1/6]` and the re-enter path gain it together** — which is why C15 deliberately left this open rather than closing it in one of the two places. `--url` on argv keeps exit 2 and the normaliser's own message: there is nobody to re-ask, and the two paths are **asserted apart** rather than assumed to differ, because a loop written one level too high would re-ask a value that came from the command line. `URL_EXHAUSTED` joins `LABEL_EXHAUSTED` and `AUTH_EXHAUSTED` — one family, one exit code, and `EXIT_CODES` already documents `EXIT_FAILED` as *"nothing saved — a refusal, an abort, three failed attempts"*, so the row needed no new code and no new constant for the count. The prompt now names the shape (`Instance URL (https://<host>, no path): `), because the two commonest wrong answers are a browser URL with a path and an `http://` one. **THE BRIEF'S OWN EXAMPLE INPUT WOULD NOT HAVE TESTED IT.** The row's case used `foo` as a bad URL; measured, `foo` is a BARE-HOST PROPOSAL — `https://foo.service-now.com`, `proposed: true` — so a case built on it would have driven the `Proposed URL` step and never the re-ask, and passed against the unfixed product. The cases use `not a url` (`URL_INVALID`) and `http://x.service-now.com` (`URL_NOT_HTTPS`). **A second deviation, deliberate:** the brief spells the scheme message with an em dash where the normaliser already says *"served over https only; use https://<host>"* with a semicolon. I did not change the punctuation — the row's substance is the re-ask, the message is the normaliser's own, and it reaches the generated `TROUBLESHOOTING.md` entry, so the churn would have travelled further than the fix. **THE COMPILER CAUGHT A CHECK THAT COULD NEVER HAVE FIRED:** I wrote `typed === undefined` for EOF, copying the label loop, where `(await io.ask(...))?.trim()` makes `undefined` correct. Here `io.ask` resolves to `string | null`, so EOF is `null` and the guard was dead — the same shape as every control this programme has caught aimed at the inert half, except a type error found it before a test could pass over it. **And a double-print I removed rather than shipped:** `askUrl` first wrote the exhaustion line itself while the caller also wrote the returned message. The last message is now RETURNED and not written, so there is exactly one write site and the argv path — which needs that write — shares it. **The reachability bound gained its own sentence** at the architect's request on C15. It REPLACES `NOTHING_SAVED` rather than preceding it, and that is a measurement, not a preference: both siblings replace it, the stated goal was that the exhaustion paths read alike, and two consecutive lines each saying *nothing saved* would not. **An explicit `[3] abort` still prints the bare line, and the abort case asserts `not.toContain(REACH_EXHAUSTED)` — a decision is not an exhaustion, and the two are held apart by assertion.** A rejected answer at the `Proposed URL` step re-asks the whole prompt rather than just that step; simplest, and it satisfies the row. **Controls:** the loop removed → both interactive cases fail by name while the argv case and C15's cases stay green (32 of 34); the bound's sentence removed → only the bounded case fails, the abort case green. **No docs change, grepped not assumed**: the prompt appears in no snippet, page or skill, and `TROUBLESHOOTING.md`'s `URL_*` entries are generated from the error registry and describe meanings and remedies, never exit codes. Gate green; the five-file upgrade e2e set is not triggered. **Stacked on C15** (`fix/reenter-the-url`), because the seam it extends is not on develop yet |
| ARC-07-C17 | **A stray Enter at the credential prompts threw the whole run away — and it was also the only way out.** `readCredentials` returned `null` for an empty username or an empty password, and the caller turned that into `Nothing saved.`, discarding the label, the URL, the environment and the auth method already given. **The sharper half is that an empty answer was the prompt's ONLY exit**: abandoning the wizard and pressing Enter by mistake were the same gesture, so the product could not tell them apart and neither could the user. `Username: ` also did not say WHOSE account — the instance's, or this machine's. ARC-07-W3 of the owner's wizard-UX programme | **Empty re-asks; `q` abandons; the prompts say what is expected.** `askRequired` sits beside W2's `askUrl` — two loops, one idiom — and is **unbounded on purpose**: a non-answer costs nothing, no network call and no step toward a lockout, so there is nothing for a bound to protect. It reads EOF as `null`, which is **W2's lesson applied rather than rediscovered**: the label loop compares against `undefined` because it reads `(await io.ask(...))?.trim()`, and copying that idiom to a prompt read without `?.trim()` writes a guard that can never fire. **THE PASSWORD GETS NO `q` CASE, and that is a decision with a case of its own.** A password may legitimately BE `q`, and the prompt is invisible — a user whose password is `q` would be told nothing was saved with no way to see why — so the password names Ctrl-C, which `promptSecret` already handles by ending the process from inside itself. The inverse control proves the case guards the decision rather than merely describing it: **a `q` branch ADDED to the secret prompt turns that case red.** **The secret loop IS bounded, for a reason that is not the user's.** `io.secret` has no end-of-input value, and `promptSecret` handles Ctrl-C and Ctrl-D by exiting, so a real user always has a way out and the bound is not it. What the bound terminates is an input that can only ever answer empty — a closed pipe, or a test stub. Measured before it was written: the stub returns `''` forever, so the bound is the difference between a failing assertion and a **hung CI cell**, and a case now asserts the run ends and says why `MAX_ATTEMPTS` times. **`--username` and `--password-stdin` stay outside both loops** — neither has a terminal to re-ask — the same split W2 drew for `--url`, and asserted apart rather than assumed. The Client ID became required too: an empty one cannot authenticate, and accepting it only moved the failure to the probe where the reason is harder to read. **That one is a behaviour change, not a fix — an empty client id was ACCEPTED before — so its changelog entry belongs under Changed rather than Fixed** (architect's ruling, 2026-09-26); every other item in this row is a fix. `[5/6] Probing` became `[5/6] Checking the login and what this account may do (read-only, a few seconds) …` — it named the machine's activity, where the reader's question is whether anything is being done TO their instance. **Five controls, each landing on its own case alone:** the echoed re-ask removed → the empty-username case; the masked re-ask removed → both password cases; the `q` exit removed → the `q` case; the header reverted → the header case; and the inverse above. **Docs:** one line of `MODES-AND-PRESETS.md` quoted the old prompt verbatim while describing `--password-stdin`; the behaviour it describes is unchanged (EOF aborts exactly as an empty answer did), so only the quotation moved — and it now refers to *the Username prompt* rather than quoting text that would go stale again. No snippet, skill or other page shows these prompts or the step headers: grepped, not assumed. **A correction to my own report:** I told the architect the brief's `[4/6]` and `[5/6]` disagreed. They do not — the file prints `[4/6] Credentials` and `[5/6] Probing`, and the row's two numbers refer to different steps. Gate green; the five-file upgrade e2e set is not triggered. **Stacked on C16**, which is stacked on C15, because each extends the last one's seam |
| ARC-07-C18 | **The question listed two options by name and accepted exactly one string.** `method = answer === '2' ? 'oauth_ropc' : 'basic'` — so `oauth_ropc`, `oauth`, `02`, a typo and Enter all became **basic**, silently. Every answer that was not the literal `2` was read as agreement with the option the user had not chosen. Nothing said Enter picked `[1]`, and nothing confirmed what had been chosen, so a user who typed the name of the option they wanted got the other one with no line anywhere saying so. ARC-07-W4 of the owner's wizard-UX programme | **`askOption` — one numbered question for every question with a list of answers.** It lists the options once, accepts a number or the option's own name (or an alias: `oauth` for `oauth_ropc`), marks the default, acks what was chosen, and on a wrong answer prints WHY and returns to the `> ` prompt rather than reprinting the question — which is the defect ARC-07-W5 inherits next. **THE GRAMMAR IS THE PERMISSIONS SCREEN'S, RE-STATED AND HELD TO IT BY ASSERTION.** The plan screen's `resolveChoice` lives in the ENGINE, across a package boundary this package must not cross, so `resolveOption` re-states the rule — and a re-statement with nothing holding it to the original is exactly how two surfaces come to answer one keystroke differently. A case walks `resolveFlagAnswer` and `resolveOption` over the same nine answers and requires them to agree; the control that removes the name match **fails that case and the by-name case together**, which is what makes it a guard rather than decoration. Enter is the one place they differ, deliberately: the flag answer returns `null` because the screen's default is the box as shown, while an option list with **no** default treats Enter as not-an-option — `askEnvironment` has carried that decision in a comment since it was written, because the environment decides which preset a write is checked against. **`02` resolves to option 2**, because the digit test passes and `Number('02')` is 2 — deliberate, not an accident of parsing: the brief listed `02` among the answers that silently became basic, and a reader who types a leading zero has still named a row. It is written down here so nobody later reads it as an accepted typo and "fixes" it. The default's marker is DERIVED from `defaultKey`, never written into an option's text (the ARC-07-C14 rule), and appended with ` · ` rather than folded into the text's own parenthesis, which would nest a bracket inside a bracket. **A HARD CONSTRAINT I FOUND BY ASSERTING IT:** the `oauth_ropc` row is 120 columns — and was 120 before this row, the text is unchanged — but `askOption` is now the one place option rows are rendered, and 100 columns is a constraint rather than a preference. It folds through `wrapRow`, the repository's own definition of folding, so W5's environment meanings and any later wording row inherit it instead of each retrofitting it. **The budget case's SCOPE is itself a measurement:** asserting the whole transcript found three other over-budget lines, none of them this row's — `Applying: …` at 101, `Saved instance … Probes: …` at 166, and `Store: <path> …` whose length depends on where the checkout lives. **ARC-07-W16 owns the Saved and Store wording**, so they are recorded here and fixed there; the case asserts the slice between `[3/6] Authentication` and its ack, which is what this row owns. **And my first continuation assertion spelled 21 spaces where the prefix is 19** — the ARC-07-C13 lesson a third time: assert the logical row and derive the column, never count it by hand. **Controls, each on its own case:** the resolver reverted to `=== '2'` → the by-name, not-one-of and Enter cases fail; the derived marker removed → the Enter case; the name match removed → the by-name and grammar cases; the wrap removed → the budget case. The argv case (`--auth` given → ARC-08-C23's skipped-step line) stays green throughout, which is how that path is known untouched. **No docs:** nothing asserts `AUTH_QUESTION` or `AUTH_CHOICES`, and no snippet, page or skill shows this question — grepped; the only other occurrence is a `CHANGELOG.md` entry recording what the question used to be, which is a record and stays. **A PROCESS FAILURE OF MINE, recorded because the rule already existed and I still broke it.** I committed the fix, ran controls A–C restoring with `git checkout -- instance.ts` each time, then wrote the wrap and the budget case, then ran control D and restored the same way — which reverted to the commit and **destroyed the uncommitted wrap**. The budget case then failed in the full suite while passing alone, because the test file survived and the source did not. Fourth occurrence in this programme of a destructive control eating uncommitted work. The rule I had been applying was *checkpoint before the controls*; the rule that actually holds is **checkpoint before EACH control, because the work written between two controls is exactly what the next restore throws away**. Gate green; the five-file upgrade e2e set is not triggered. **Stacked on C17** |
| ARC-07-C19 | **A wrong answer reprinted the question and said nothing, so the screen looked frozen.** `askEnvironment` looped on `What is this instance?  [1] pdi  [2] dev  [3] test  [4] prod`, and Enter, `production` or `5` produced the identical line again with **no message** — the one output that looks like a program that has stopped responding. The four values were bare: `pdi` means nothing to a reader who has not met ServiceNow's developer programme, and the reason the answer matters — production is saved read-only — surfaced four steps later at `[6/6]`, where it reads as a surprise rather than the consequence of a choice already made. **AND FOR A `devNNNNNN` HOST THE ANSWER WAS NEVER ASKED OR STATED:** measured, `resolveEnvironment` returns `pdi` without calling `ask` and without printing, so `[2/6] Environment` was followed by nothing at all. ARC-07-W5 | **It is `askOption` now**, which is ARC-07-C18's helper earning its keep: the message, the ack, the number-or-name grammar and the 100-column folding all arrive without being written again, and the silent reprint disappears because that helper prints the question ONCE. **No default, and the reason is older than this row** — `askEnvironment` has carried it in a comment since it was written: *"`pdi` is right often enough to be tempting and wrong in exactly the case that matters, because the environment decides which preset a write is checked against."* So Enter is an unknown answer here, which is precisely what `resolveOption` does with no `defaultKey`: the one place its grammar differs from the permissions screen's, and now the reason that difference exists is load-bearing rather than incidental. `ENV_CHOICES` is a second list beside `ENVIRONMENTS`, so **a test asserts they are the same keys in the same order** — otherwise a fifth environment could arrive with no meaning or leave one behind, and the control that empties the meanings fails that case as well as the rendering one. The `devNNNNNN` path **keeps its behaviour and states its decision**: `Environment → pdi (from the URL)`. The question is still not asked. **THE ROW'S OWN PROPOSED LINE CANNOT BE HONEST, and this is the measurement the brief asked for.** It proposes `Environment → pdi (from the URL; type 2–4 to change)` — but nothing is being asked on that path, so there is no prompt at which to type 2–4. Making it true means ADDING a question to the commonest path (a PDI), which is a flow change on the happy path and not mine to make. **The argument for adding it is stronger than it looks, and is recorded here so the decision is made on the facts:** there is **no `set-env`** — the subcommands are `add`, `list`, `remove`, `set-credentials`, `set-flags`, `set-preset`, `test` — so the environment is the ONE choice in the wizard with no later edit, changeable only by `remove` then `add`. The owner's ruling was that changing choices already made must be easy; for this choice, the moment it is made is the only moment there is. **RULED BY THE ARCHITECT, 2026-09-26: ask it.** `askOption` runs on the proposal path too with `pdi` as the marked default — the row carries ` · Enter picks this`, Enter gives `Environment → pdi`, and a number or a name overrides the proposal. The reasons are the owner's own: no later edit, it decides the production cap, and a choice must be easy to make AND to change, so the moment it is made must be a question. It costs one Enter on the happiest path and adds no new screen — the same helper, the C12 shape the owner approved. **`--yes` is unchanged**, because there is nobody to ask, and it prints `Environment → pdi (from the URL)` so the decision is still stated; the `(from the URL)` suffix is what distinguishes a DECISION from an ANSWER and appears on that path alone. **`resolveEnvironment` was left alone**: it is a resolver, not a conversation, and asking inside it would put an interaction behind a function whose other answers are pure. The answer is handed to it as though it came from `--env`, so a value that is not an environment still fails its own validation rather than a second copy of it. The follow-up control: **the `defaultKey` removed → the Enter case fails by name**, and nothing else moves. **Controls:** the meanings emptied → the rendering case and the one-definition case; the auto-proposal's line removed → the `devNNNNNN` case; the question reprinted every round → the silent-reprint case, by name. **My own mistake, worth the line:** my first version of the wrong-answer case answered `4` (prod) and asserted `EXIT_POLICY`. Wrong — the production cap **offers** read-only and the trailing Enter accepted it, so the run saved and exited 0. A case about a message had pulled in a policy branch it did not mean to test; it answers `2` now. **And the checkpoint rule from C18 was applied:** committed before the first control, and each control restored with `git stash` against a clean tree rather than `git checkout --` against a stale commit. **No docs:** the only other occurrence of this question is a dated `CHANGELOG.md` entry recording what it used to be — a record, and it stays. Gate green; the five-file upgrade e2e set is not triggered. **Stacked on C18** |
| ARC-07-C20 | **The question that could throw the run away was asked last.** `cloudSyncGate` ran after `[6/6]`, last among the questions and first among the writes — a defensible place for a WRITE gate and the wrong place for a QUESTION. By the time it appeared the user had given a URL, an environment, an auth method, **a password** and six reviewed permissions; `Continue and write the store here anyway? [y/N]` then made **Enter** discard all of it and exit 3. On macOS, where Desktop and Documents are in iCloud by default, that is the common case rather than the unlucky one. And `[y/N]` hid the consequence in a convention: it tells a reader the default is No only if they already know the convention, and what the default DID was written nowhere. ARC-07-W6 | **The gate runs before the wizard's first question**, so the only thing a `stop` still costs is the LABEL — and that is accepted rather than overlooked. The label is asked in `instance-command.ts` before `runAdd` is ever called, so making the gate literally the first output would mean moving the label prompt into `runAdd`, or running the gate in the outer command and adding an acknowledged-flag so `runAdd` does not ask twice — and that second shape also loses the gate for `addInstance`, which calls `runAdd` directly. Both are larger than the finding: what the row is about is a password and six reviewed permissions, and those are gone. One defaulted word is the residue, and the architect ruled it stays (2026-09-26). The warning is still printed again after the save — the `warnings` loop was already there — so the last thing on screen for somebody who continued is still the reason they should not have, and the pre-existing case that asserts the WARN is in the final two lines needed no weakening: **measured before moving anything**, because if that property had depended on the call's position the move would have silently traded one finding for another. `[y/N]` became the C12 menu through `askOption`: `[1] stop — nothing is saved; clone outside the synced folder and start again · Enter picks this` / `[2] continue — write the store here anyway`. `askOption` gained one thing — **an empty ack prints nothing** — because `stop`'s caller already prints `Nothing saved.` and two consecutive lines saying it is the mistake ARC-07-W2 removed once already. **MEASURED, AND THE ROW GUESSED THE OTHER WAY.** Its text says *"measure: today it refuses? keep exactly that"* — it does **not** refuse: `--yes` returns `{ ok: true, warning }` and proceeds, which `MODES-AND-PRESETS.md` also states. Unchanged, and now asserted explicitly, because *keep exactly that* is only safe once *that* is written down. **`set-credentials` keeps its own gate, and the guard for that is structural**: both commands call one `cloudSyncGate`, so the property is WHERE each call sits. A case reads the source, requires exactly two `await cloudSyncGate(` sites, and asserts the first is before `[1/6] Instance URL` and the second after it. **Measured while writing it: `set-credentials` has no cloud-sync test anywhere in the suite**, so nothing else would have noticed its call moving — and `runSetCredentials` has no `[6/6]` after it and no permissions to lose, so an early gate would protect nothing there. **Controls:** the call moved back after `[6/6]` → three cases fail including the structural one, which is how that one is known to work; the menu reverted to `[y/N]` → the two behavioural cases fail and the structural one stays green, which separates the two halves of this row. **Docs, and one of them was wrong before I arrived:** §6's hand-maintained example carried the old `[y/N]` line and now carries the menu, its WARN elided with `…` where the prose above already describes it so the section stays inside its 60-line guard — which **caught the overflow and made me compress my own additions rather than cut the page's content**. The `--password-stdin` paragraph no longer lists cloud-sync among the *later* questions, because it is now the first. Two things I did NOT change, having checked them: `warned about BEFORE anything is written` was and remains true, and `§6's cloud-sync question` is a SECTION reference, not a step number. **AND A DEFECT IN THE PAGE THAT TOLD ME TO DO IT:** `docs/CONTRIBUTING.md` said *"`npm run build` and commit `dist/`"*. There is no `npm run build` — the script is `build:dist` — so the command errors with *Missing script*. I had been running it all programme, silently, believing dist was rebuilt; **the contract gate had been doing the rebuild all along** (it says so: *"this step already rebuilt it, so COMMIT the change shown above"*), so every row's `dist/` is correct and no PR is affected. Found because W6 was the first row whose doc block I rendered FROM dist, and the menu was missing from it. Both the wrong line and a note naming the shorter path are fixed on that page. Gate green; the five-file upgrade e2e set is not triggered. **Stacked on C19** |
| ARC-07-C21 | **The sites ARC-07-C1 did not name.** C1 listed four sources and five pinning assertions and was right about all of them; the same defect sat in four more places it never reached. `tools/snowarch/lib/steps/B06.mjs` spelled `./snowarch` in **seven** remedies and `./bootstrap.sh` in an eighth. `packages/snowarch/src/cli/preset-ui.ts` spelled it three times — the locked-production footer, `PROD_LOCKED` and `prodRefusal` — so the screen that tells a user how to raise a production instance told a Windows user to type a command their shell refuses. `tools/snowarch/lib/remedies.json` gave the win32 root remedy as `cd /d "{root}" && .\\bootstrap.cmd`: **`cd /d` is cmd-only and `&&` arrived in PowerShell 7**, so the line a Windows reader is most likely to paste was a parse error in PowerShell 5.1. And `bootstrap.ps1` — the PowerShell script itself — ended by telling the reader to `run snowarch.cmd mode live`. ARC-07-W7 | **Fixed with C1, from the same two definitions, plus a guard shaped like ARC-09-C60's so the eighty-ninth cannot arrive.** Two checks: no renderer builds a command out of a literal path, and no user-facing page carries a bare `snowarch.cmd` or a `cd /d`. **THE REMEDY TOOK THREE ATTEMPTS, AND THE SECOND WAS THE SAME DEFECT THIS ROW CLOSES.** `cd /d "{root}" && .\\bootstrap.cmd` was cmd-only syntax; I replaced it with `pushd "{root}" then .\\bootstrap.cmd`, which puts PROSE inside a command — a PowerShell user pasting it gets `pushd : A positional parameter cannot be found that accepts argument 'then'`. The architect caught it in review. It is **two lines** now, the only form both shells run: `&&` arrived in PowerShell 7, `&` is PowerShell's call operator, and `;` is not a separator in cmd. Measured that two lines survive: `gen-launcher-text`'s targets are `bootstrap.sh` and `bootstrap.ps1` — **not `bootstrap.cmd`** — and both take a literal newline inside a single-quoted string; launcher parity is 25/25. **And the case now asserts the SHAPE, not the string:** no ` then `, no `&&`, no `cd /d`, exactly two lines, each one something you can paste. Two wrong forms got past a string-equality assertion, which is the argument for asserting the property instead of the value. `docs/plans/**`, `docs/validation/**` and the changelog are exempt — they record what was true when written, and rewriting one to match today's code destroys the finding it holds. **THE GUARD WAS WRONG BEFORE IT WAS RIGHT, and that is the row's real lesson.** Its first version flagged 16 lines; **five were not defects**. `docs/ARCHITECTURE.md`'s file tree, its component table and two sentences about what the wrapper does all name `snowarch.cmd` as a FILE, and `docs/PLATFORM-NOTES.md` names the artifact S-04 tested — `.\\snowarch.cmd` would be **wrong** in every one, because that is the file's name. So the rule fires only when a subcommand or a flag follows, which is what makes it a command somebody types, and the negative case pins both directions: a planted invocation is seen, a file tree is not. A check that had reported those five would have needed an allow-list, and an allow-list is how a guard stops being one. **The lookbehind matters too:** the correct spelling CONTAINS the wrong one, so a plain `includes` reported all 104. **Six invocations on user pages were fixed**, and one of them was a documentation defect of its own: `docs/CONTRIBUTING.md` described its own CI as running `snowarch.cmd --help` when `ci.yml` has run `call .\\snowarch.cmd --help` all along. **B06's eight sites read `spellings()` at module load and stay STRINGS** — correct because a process's platform does not change, and deliberate because making them functions would have turned the existing `detail === NO_TERMINAL` assertions into comparisons of a value with itself: green forever, and blind to the spelling they appear to check. **Two guards I had not met caught my own work:** the `.ps1` CRLF policy (my first edit rewrote the file with LF; it is applied byte-exactly now), and the env-example check, which required `SHELL` and `MSYSTEM` to be declared — they are in `NOT_OURS` with their reason, because the shell sets them and a project `.env` inviting a reader to set `SHELL` would be the most confusing thing this package could publish. The `--yes`, POSIX and Git Bash paths are byte-identical throughout: the default spelling on this machine is unchanged, which is why 268 CLI cases passed without one edit |
| ARC-07-C22 | **The install page named a release three versions out of date, on the one path where the reader never sees the command.** Every `--branch` line said `v2.0.5`; the paste-a-prompt block said *"from release tag v2.0.2"*. `writeInstallTag`'s pattern is `--branch v<x.y.z>`, so the prose was invisible to the writer — **and to ARC-09-C61's page guard, whose `cloneTagsIn` matched only `git clone --branch`**. Two mechanisms built to keep this exact value current, and the value sat stale between them for three releases. It matters most on Path C: there the reader pastes a SENTENCE into Claude and Claude runs the clone, so the tag is the one thing they cannot check. Two smaller things on the same page: a correct clone earned fourteen lines of git advice about *detached HEAD* — a tag is a tag, so checking one out is exactly that — and the plan block still showed `type a number to change that line`, the verb **ARC-07-C10 removed because "change" was the defect**: the owner pressed `1` seven times at the S06 sitting waiting to be asked something, while each press silently toggled Mode. ARC-07-W8 | **Both grammars are written, and both are read.** `writeInstallTag` rewrites `--branch v…` and `release tag v…`, counts both, and refuses only a page that names no tag in either — ARC-09-C60's rule, that a writer reporting success on a page it did not change is how a stale tag ships. A page naming one only in prose is the case that WENT stale, so it is written rather than rejected. **C61's guard reads both too, which cost no new case: the release's own *did the writer run* assertion goes through `tagsNamedIn`, so it now covers the prose for free.** `cloneTagsIn` stays as an alias, so a reader of C61 finds what that row described. **A NEAR-MISS WORTH THE ROW.** Adding `-c advice.detachedHead=false` to every clone line **broke the guard's own pattern**: `git clone --branch` no longer matched, so it found nothing and would have failed with *names no release tag* — blind in the direction that reads like a page defect rather than a guard defect. That is the same shape as the handoff extractor in ARC-07-C1, one row earlier: a matcher too strict to survive the change it exists to carry. Options are tolerated between `git` and `clone` now. **Guards so neither half can go stale again:** the page's plan header is asserted equal to `plan.mjs`'s exported `HEADER` in both copies, and the retired verb is asserted absent by name; every pinned clone on INSTALL, MIGRATION and README must carry the option. **Controls:** the prose pattern dropped from the writer → both writer cases; the page left at `v2.0.2` → C61's guard plus the composition check; the header reverted → the header case by name; the option removed from one clone line → the clone case, naming the line. Each asserts its own anchor before running, after ARC-07-W7's control went inert. **The page-shape guard caught me and I compressed my own text, not the page's** — 298 lines is the criterion, my sentence made it 300, and a standalone paragraph costs a blank line I could not afford, so the explanation folded into the paragraph that already explains `--branch`. Same resolution as ARC-07-W6's 60-line section guard, and the right way round both times. Gate green; the five-file upgrade e2e set is not triggered — nothing under `upgrade.mjs`, `bootstrap.mjs`, `lib/steps/` or `tests/upgrade/` changed. **Stacked on C21** |
