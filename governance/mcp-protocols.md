<!-- GENERATED by scripts/gen-governance.mjs from packages/snowarch/dist/contract.json (sha256 a96863b1104b) — do not edit; edit packages/contract/gen/protocols.mjs and run npm run gen -->
# MCP protocols — the long form of §2.1 and §2.2

The always-loaded short form is `.claude/rules/00-mode-and-mcp-gate.md`. This document is the same
two rules at length, with the reasons. Both are generated from the server contract, so they cannot
disagree; `governance/governance-rules.md` §2 points here and restates nothing.

The server is registered as `servicenow`. Its tools are named `mcp__servicenow__snow_*`, and that
prefix is written in exactly one place in the engine — `engine.config.json` — which is what makes
renaming it a one-line change rather than a sweep.

## §2.1 — The write gate

**Which tools it covers.** Any tool whose contract entry says `mutates: true` or
`sessionMutates: true`: **161** of 397 today. That is the definition — not a list of
names, and not a guess from a name's suffix. Naming tools by suffix was tried and leaves more than
thirty state-changing tools ungated, among them approving a request, rolling back a deployment,
retiring an article, completing a task and setting system properties.

`sessionMutates` is in the definition for one tool. Switching instance changes no record, and it
decides **where every later write lands** — approving it is approving that.

**What counts as approval.** A clear, explicit message in the current conversation authorising the
specific write about to be made.

**What does not.** A previous "yes" to a read. A general go-ahead earlier in the conversation that
did not name this write. The task description, however detailed. And **switching Mode from
`design-only` to `live`, or raising a preset** — that is a configuration change: it makes a write
possible and says nothing about whether this write is wanted.

**Halt protocol.** About to write without approval in the current conversation: stop and ask —
`About to <action> on instance "<label>" — write approved?` — then wait. Approval is a discrete
message; it is never inferred from context, urgency or the logic of the task.

## §2.2 — Update-set capture

**Why a preference and not a "current update set".** REST honours the authenticated user's
`sys_user_preference` row with `name=sys_update_set`; the `is_default` flag on an update set is a UI
concept and does nothing for the API — setting it looks like success and captures nothing. This was
established on a live instance and is recorded as PN-01 in `docs/PLATFORM-NOTES.md`, with the
consequence that capture cannot be verified any other way and cannot be repaired afterwards.

Before any write that produces a configuration object — Script Include, Business Rule, Client
Script, UI Policy, UI Action, ACL, Flow, table or field — four calls:

1. `snow_us_active_update_set_ensure` `{ "name": "<engagement>-<topic>" }` — the name is required, and only the caller's own
   in-progress sets are returned. Without both, a shared instance hands back whoever opened one last,
   and the engagement's objects land in a stranger's update set.
2. `snow_us_capture_target_set` `{ "update_set_sys_id": "<sys_id from step 1>" }` — points capture at it.
3. Do the write. It needs its own approval under §2.1.
4. `snow_us_update_set_preview` — confirm the objects are in the set. This step is the evidence; without it the
   protocol was performed but not verified.

**Not substitutes.** `snow_us_update_set_switch` sets `is_default` and changes nothing for REST.
Writing `sys_update_xml` directly is refused with `INSUFFICIENT_PRIVILEGES`, admin included. And
`snow_deploy_background_script_exec` and `snow_fluent_script_exec` refuse with `UNSUPPORTED_ON_THIS_INSTANCE` — they
are registered so the refusal can name the route that works, not because they work.

Capture cannot be applied retroactively over REST. If steps 1–2 were skipped, stop and say so.

## Required tools

What the engine depends on, from `packages/contract/required-tools.json`, joined with what the
server declares. A rename on the server side moves the sha and fails the pin before it reaches here.

| Tool | Gate | Mutates | Used by |
|---|---|---|---|
| `snow_atf_atf_suite_exec` | atf | yes | §2.1, atf-author |
| `snow_atf_atf_test_exec` | atf | yes | §2.1, atf-author |
| `snow_cmdb_reconcile` | cmdb_write | yes | §2.1, cmdb-csdm-specialist |
| `snow_core_capabilities_read` | none | no | /snowarch status, /snowarch doctor |
| `snow_core_current_instance_read` | none | no | /snowarch status |
| `snow_core_health_dashboard_read` | none | no | itom-discovery-specialist, performance-scale-specialist |
| `snow_core_instance_switch` | none | no | §2.1, /snowarch status |
| `snow_core_instances_reload` | none | no | /snowarch setup-instance, /snowarch doctor |
| `snow_core_natural_language_modify` | write | yes | §2.1 |
| `snow_core_record_add` | write | yes | §2.1, §2.2, VALIDATION-TESTS, developer |
| `snow_core_record_modify` | write | yes | §2.1, VALIDATION-TESTS, developer |
| `snow_core_record_read` | none | no | VALIDATION-TESTS, developer |
| `snow_core_record_remove` | write | yes | §2.1, VALIDATION-TESTS |
| `snow_core_records_query` | none | no | §2.2, VALIDATION-TESTS, developer, technical-designer |
| `snow_core_status_read` | none | no | /snowarch status, /snowarch doctor, bootstrap |
| `snow_core_table_schema_read` | none | no | technical-designer, cmdb-csdm-specialist |
| `snow_deploy_background_script_exec` | scripting | yes | §2.2 |
| `snow_disco_table_discover` | none | no | technical-designer, migration-specialist |
| `snow_flow_flow_action_add` | scripting | yes | §2.1, flow-designer-specialist |
| `snow_flow_flow_add` | write | yes | §2.1, flow-designer-specialist |
| `snow_fluent_script_exec` | scripting | yes | §2.2 |
| `snow_inc_incident_modify` | write | yes | §2.1, itsm-specialist |
| `snow_inc_incident_resolve` | write | yes | §2.1, itsm-specialist |
| `snow_intg_event_register` | scripting | yes | §2.1, integration-specialist |
| `snow_perf_table_completeness_check` | none | no | performance-scale-specialist, cmdb-csdm-specialist |
| `snow_rpt_aggregate_query_exec` | none | no | reporting-analytics-specialist, performance-scale-specialist |
| `snow_rpt_report_add` | write | yes | §2.1, reporting-analytics-specialist |
| `snow_rpt_scheduled_job_add` | write | yes | §2.1, reporting-analytics-specialist |
| `snow_scr_business_rule_add` | scripting | yes | §2.1, developer |
| `snow_scr_business_rule_modify` | scripting | yes | §2.1, developer |
| `snow_scr_business_rules_index` | none | no | developer, code-reviewer |
| `snow_scr_script_include_add` | scripting | yes | §2.1, developer |
| `snow_scr_script_include_modify` | scripting | yes | §2.1, developer |
| `snow_scr_script_include_read` | none | no | developer, code-reviewer |
| `snow_scr_script_includes_index` | none | no | developer, code-reviewer |
| `snow_us_active_update_set_ensure` | scripting | yes | §2.1, §2.2 |
| `snow_us_capture_target_set` | write | yes | §2.1, §2.2 |
| `snow_us_current_update_set_read` | none | no | §2.2 |
| `snow_us_update_set_add` | scripting | yes | §2.1, §2.2 |
| `snow_us_update_set_preview` | none | no | §2.2 |
| `snow_us_update_set_switch` | scripting | yes | §2.1, §2.2 |
| `snow_us_update_sets_index` | none | no | §2.2 |

## Flags and presets

| Flag | What it unlocks | Requires |
|---|---|---|
| `WRITE_ENABLED` | create, update and delete records — incidents, catalog items, users, agile work, update sets. Without it everything is read-only. | — |
| `CMDB_WRITE_ENABLED` | additionally, CI and relationship reconciliation writes into the CMDB. | `WRITE_ENABLED` |
| `SCRIPTING_ENABLED` | unlocks *writing* Script Includes, Business Rules, Client Scripts, ACLs, UI Actions and update-set changes. Reading them is always allowed. | `WRITE_ENABLED` |
| `ATF_ENABLED` | execute ATF tests and suites; authoring and reading are always allowed. | — |
| `NOW_ASSIST_ENABLED` | the Now Assist and generative-AI tools; needs a Now Assist licence on the instance. | — |
| `FLUENT_ENABLED` | the ServiceNow SDK build and deploy tools; deploys also need WRITE. | — |

A flag is on only when its value is the exact string `"true"`; absent means off. Declaring a
dependent flag without its prerequisite is resolved towards less access, never more.

| Preset | On | Choose it for |
|---|---|---|
| `read-only` | none | auditing, reviewing, designing against a real instance — and the only preset a `prod` instance may hold without `--ack-prod` |
| `pdi-developer` | WRITE, CMDB_WRITE, SCRIPTING, ATF | building on a PDI, dev or test instance where Now Assist and Fluent are not wanted |
| `full` | WRITE, CMDB_WRITE, SCRIPTING, ATF, NOW_ASSIST, FLUENT | everything on; the proposal for every non-production instance (D-05) |

`custom` is the absence of a preset: six explicit toggles, with the dependency rule still enforced.

## Error codes

Every code has one meaning and one remedy, in the registry the server, the wizard, the doctor and
this document all render from. The full list is `docs/TROUBLESHOOTING.md`. These 12 are in the
always-loaded rule file, because a session can act on them mid-task:

- `ATF_NOT_ENABLED` — The instance's preset does not enable ATF_ENABLED. raise the preset; a `prod` instance additionally needs `--ack-prod` `./snowarch instance set-preset <label> pdi-developer`
- `AUTHENTICATION_FAILED` — The instance rejected the credentials — wrong, expired, or the account is locked. If a ServiceNow tool returns AUTHENTICATION_FAILED: stop immediately. Do not retry that call or make any other call to the same instance — repeated failed logins can lock the account. Tell the user to run ./snowarch instance test <label> and, if it fails, ./snowarch instance set-credentials <label>. Continue only after the user says the credentials were fixed
- `CMDB_WRITE_NOT_ENABLED` — The instance's preset does not enable CMDB_WRITE_ENABLED. raise the preset; a `prod` instance additionally needs `--ack-prod` `./snowarch instance set-preset <label> pdi-developer`
- `FLUENT_NOT_ENABLED` — The instance's preset does not enable FLUENT_ENABLED. raise the preset; a `prod` instance additionally needs `--ack-prod` `./snowarch instance set-preset <label> full`
- `FLUENT_NOT_INSTALLED` — The ServiceNow SDK is not on `PATH`. install it globally — the doctor checks `PATH`, so a checkout-local install would pass here and fail there `npm i -g @servicenow/sdk`
- `INSUFFICIENT_PRIVILEGES` — The account is authenticated but lacks a ServiceNow role for that table or operation. This is not a flag. The credentials are valid but the account lacks a role for this table. Report the tool, the table and the roles the preset needs (see docs/TROUBLESHOOTING.md); do not switch instances or retry with another tool to work around it
- `NO_INSTANCE_CONFIGURED` — The server is running and healthy; it has no instance to talk to. It stays up on purpose — an unconfigured checkout used to exit at start-up, so the moment you most needed the server to explain itself was the moment it was gone. Five tools still work: status, capabilities, reload, the instance listing and the current instance. add an instance, then call the reload tool — Claude Code does not need restarting, the server re-advertises its catalogue in the same session `/snowarch setup-instance`
- `NOW_ASSIST_NOT_ENABLED` — The instance's preset does not enable NOW_ASSIST_ENABLED. raise the preset; a `prod` instance additionally needs `--ack-prod` `./snowarch instance set-preset <label> full`
- `PROD_WRITE_NOT_ACKNOWLEDGED` — The instance is tagged `environment: prod` and holds a write preset without `prodWriteAck: true`. A production instance is capped at read-only. Do not suggest editing the store; the user raises it with ./snowarch instance set-preset <label> <preset> --ack-prod in their terminal
- `SCRIPTING_NOT_ENABLED` — The instance's preset does not enable SCRIPTING_ENABLED. raise the preset; a `prod` instance additionally needs `--ack-prod` `./snowarch instance set-preset <label> pdi-developer`
- `UNKNOWN_TOOL` — A retired or misspelled tool name. use the `snow_*` name from `governance/mcp-protocols.md`; maintainers: `npm run lint:contract`
- `WRITE_NOT_ENABLED` — The instance's preset does not enable WRITE_ENABLED. raise the preset; a `prod` instance additionally needs `--ack-prod` `./snowarch instance set-preset <label> pdi-developer`
