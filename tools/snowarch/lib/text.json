{
  "$comment": "GENERATED by scripts/gen-text.mjs from tools/snowarch/lib/text.mjs — do not edit. The Node-free launchers print these strings; the parity test reads this file.",
  "expectedDialogs": 1,
  "modeDesign": "Mode: design-only",
  "banner": {
    "firstRun": "No ServiceNow instance configured — /snowarch setup-instance adds one (design-only works without it).",
    "upgrade": "A newer release is available (v2.1.0) — run ./snowarch upgrade.",
    "staleRegistration": "Stale MCP registrations from the old setup found in ~/.claude.json — run ./snowarch doctor --section legacy for the removal commands.",
    "doctorFail": "Doctor: 2 FAIL — run ./snowarch doctor for remedies.",
    "staleSuffix": " (cache stale — run ./snowarch doctor)",
    "timedOut": "Mode: unknown — doctor timed out; run ./snowarch doctor",
    "failed": "Mode: unknown — session banner failed (Error); run ./snowarch doctor"
  },
  "doctorUnavailable": "DOCTOR: unavailable until Node 20+ is installed (design-only is complete)",
  "posix": {
    "spellings": {
      "bootstrap": "./bootstrap.sh",
      "cli": "./snowarch"
    },
    "nextDesign": "Next: run `claude` here. You will see one workspace-trust dialog — answer Yes.\n      (If you are already inside Claude Code in this folder: exit it and start `claude` again.)\n      Add a live instance later with ./snowarch mode live, or /snowarch setup-instance inside Claude.",
    "nextLive": "Next: run `claude` here. You will see one workspace-trust dialog — answer Yes.\n      (If you are already inside Claude Code in this folder: exit it and start `claude` again.)\n      In Claude, /mcp should show: servicenow ✔ connected · /snowarch status quotes the Mode line above."
  },
  "windows": {
    "spellings": {
      "bootstrap": ".\\bootstrap.cmd",
      "cli": "snowarch.cmd"
    },
    "nextDesign": "Next: run `claude` here. You will see one workspace-trust dialog — answer Yes.\n      (If you are already inside Claude Code in this folder: exit it and start `claude` again.)\n      Add a live instance later with snowarch.cmd mode live, or /snowarch setup-instance inside Claude.",
    "nextLive": "Next: run `claude` here. You will see one workspace-trust dialog — answer Yes.\n      (If you are already inside Claude Code in this folder: exit it and start `claude` again.)\n      In Claude, /mcp should show: servicenow ✔ connected · /snowarch status quotes the Mode line above."
  }
}
